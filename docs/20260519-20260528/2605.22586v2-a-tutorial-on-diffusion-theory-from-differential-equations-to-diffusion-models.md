---
title: "A Tutorial on Diffusion Theory: From Differential Equations to Diffusion Models"
title_zh: 扩散理论教程：从微分方程到扩散模型
authors: "Jiayi Fu, Yuxia Wang"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22586v2"
tags: ["query:diff-gen"]
score: 10.0
evidence: 从微分方程推导扩散模型的教程，作为扩散理论的综述
tldr: 扩散模型通过逐步添加噪声将数据分布转换为高斯先验，再从噪声反向去噪生成样本。本教程从微分方程视角出发，将前向扩散过程描述为常微分方程和随机微分方程，并推导出反向时间动态，其中边际分数函数起核心作用。通过分数匹配训练模型估计分数，采样时利用求解器或引导方法生成高质量样本。该框架统一了DDPM和DDIM等经典模型，深化了对扩散模型理论的理解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22586v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1692, \"height\": 315, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22586v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1699, \"height\": 647, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22586v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1712, \"height\": 584, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.22586v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1490, \"height\": 434, \"label\": \"Table\"}]"
motivation: 从微分方程角度系统阐释扩散模型的理论基础，连接不同模型并揭示其内在联系。
method: 利用条件高斯前向过程的ODE/SDE表示，推导反向动态并建立分数匹配训练目标与采样方法。
result: 证明噪声预测目标等价于分数匹配，并展示了DPM-Solver和引导采样等方法的有效性。
conclusion: DDPM和DDIM可统一于反向SDE/ODE框架，其训练目标一致而采样方式分别对应离散化SDE和ODE。
---

## 摘要
本教程从微分方程的角度阐述扩散模型。我们从条件高斯前向过程出发，表明该路径既可以用常微分方程（ODE）表示，也可以用随机微分方程（SDE）表示。对数据分布求平均后，得到边缘化的前向ODE和SDE形式，它们将数据分布 $p_0=p_{\mathrm{data}}$ 传输到高斯先验 $p_1=\mathcal{N}(0,I)$。接下来，我们推导相应的逆向动力学，即逆向SDE和逆向概率流ODE，两者均由边缘得分 $\grad\log p_t(x)$ 支配。由此得出得分估计的训练目标，并证明标准的噪声预测目标与得分匹配等价，二者仅相差一个与模型参数无关的加性常数。然后，我们讨论学习到的逆向动力学的采样方法，包括DPM-Solver，以及通过分类器引导和无分类器引导的引导采样。最后，我们将DDPM和DDIM与逆向SDE/ODE框架进行比较，表明它们共享相同的训练目标，而DDPM采样对应于离散的逆向SDE采样，DDIM采样对应于逆向ODE采样。

## Abstract
This tutorial develops diffusion models from the viewpoint of differential equations. We begin with the conditional Gaussian forward process and show that this path admits both an ordinary differential equation (ODE) representation and a stochastic differential equation (SDE) representation. Averaging the conditional process over the data distribution then yields marginalized forward ODE and SDE formulations that transport the data distribution $p_0=p_{\mathrm{data}}$ to a Gaussian prior $p_1=\mathcal{N}(0,I)$. We next derive the corresponding reverse-time dynamics, namely the reverse SDE and the reverse probability-flow ODE, both of which are governed by the marginal score $\grad\log p_t(x)$. This leads to a training objective for score estimation and shows that the standard noise-prediction objective is equivalent to score matching up to an additive constant independent of the model parameters. We then discuss sampling methods for the learned reverse dynamics, including DPM-Solver, as well as guided sampling through classifier guidance and classifier-free guidance. Finally, we compare DDPM and DDIM with the reverse SDE/ODE framework and show that they share the same training objective, while DDPM sampling corresponds to discrete reverse-SDE sampling and DDIM sampling corresponds to reverse-ODE sampling.

---

## 论文详细总结（自动生成）

## 《扩散理论教程：从微分方程到扩散模型》详细总结

本教程从微分方程的角度系统性地构建扩散模型，旨在统一不同的理论框架，并为后续研究和应用提供坚实的数学基础。

### 1. 论文的核心问题与整体含义
*   **核心问题**：如何从第一性原理出发，用统一的微分方程语言（ODE/SDE）阐释扩散模型的前向加噪、逆向去噪、训练目标及采样过程，并揭示DDPM、DDIM、得分匹配与流匹配等不同范式之间的内在联系。
*   **整体含义**：教程将扩散模型视为一个受微分方程支配的连续时间随机/确定性演化系统。前向过程将数据分布转化为高斯先验，逆向过程则通过学习边际得分（分数函数）来逆转这一过程，从而生成新样本。整个过程的核心是“得分估计”。

### 2. 方法论：核心思想与关键技术
*   **前向过程的双表示**：从一个条件高斯路径 $p_t(x|x_0)=\mathcal{N}(x;\alpha_t x_0,\sigma_t^2 I)$ 出发，证明它既可由一个常微分方程 (ODE) 生成，也可由一个随机微分方程 (SDE) 生成。对数据分布求平均后，得到边缘化的前向ODE和SDE。
*   **逆向动力学推导**：利用福克-普朗克方程和连续性方程，推导出与边缘前向过程相匹配的逆向SDE和逆向ODE（概率流ODE）。两者拥有相同的边缘密度路径，且均由未知的边际得分 $\nabla \log p_t(x)$ 决定。
*   **得分估计与训练目标**：
    *   将得分函数重新参数化为噪声预测器 $\epsilon_\theta(x,t) \approx -\sigma_t \nabla \log p_t(x)$。
    *   证明标准的“噪声预测”训练目标与得分匹配目标等价，二者仅相差一个不依赖于模型参数的常数。
    *   利用正交恒等式，将训练目标转化为可优化的形式：$L(\theta) = \mathbb{E}_{x_0,\epsilon,t}[\|\epsilon_\theta(\alpha_t x_0 + \sigma_t \epsilon, t) - \epsilon\|_2^2]$。
*   **采样方法**：
    *   **基本采样器**：分别给出了学习后的逆向SDE和ODE的一阶欧拉-丸山/欧拉离散化更新公式。
    *   **DPM-Solver风格快速采样**：将逆向ODE转换为积分形式，并利用对数信噪比 $\lambda_t$ 对积分进行近似（一阶近似相当于常见的高阶快速采样器的基础），显著减少采样步数。
*   **引导扩散**：介绍了两种控制生成内容的方法：
    *   **分类器引导**：利用一个额外训练的分类器的梯度来修正逆向扩散过程的得分，引导生成朝向特定类别。
    *   **无分类器引导**：通过随机丢弃条件训练一个单一网络，在推理时混合条件与无条件预测来外推得分方向，增强条件控制力。
*   **模型统一**：将经典的DDPM和DDIM离散过程“连续化”，证明了：
    *   DDPM的前向过程可被一个方差保持（VP）的SDE连续化。
    *   DDPM的训练目标正是网格上限制的逆向SDE得分匹配目标。
    *   DDPM的迭代采样对应于精确的离散逆向高斯链，同时也是逆向SDE的近似离散化。
    *   确定性DDIM采样则是逆向概率流ODE的一阶离散化。

### 3. 实验设计
本文是一篇理论综述与推导教程，**不包含任何原始实验**。教程中举例说明的方法（如DPM-Solver、无分类器引导）是引用自其他研究工作的成熟技术，教程本身未设计新的实验场景、数据集或基准对比。

### 4. 资源与算力
作为一篇纯理论教程，本文**未涉及任何计算实验或算力消耗**。文中未提及GPU型号、数量、训练时长等资源需求。

### 5. 实验数量与充分性
因为本文是教程性质，**不适用常规实验数量与充分性的评价标准**。其价值体现在理论推导的严谨性、系统性和框架的统一性上，而非实验验证。

### 6. 论文的主要结论与发现
*   **框架统一性**：成功地将DDPM、DDIM、得分匹配、概率流ODE和流匹配等看似不同的方法统一在逆向SDE/ODE的框架之下。
*   **训练目标等价性**：证明了噪声预测损失、得分匹配损失和特定权重下的流匹配损失是内在等价的。
*   **采样对应关系**：明确指出DDPM采样在连续极限下对应于离散化逆向SDE，而确定性DDIM采样对应于离散化逆向ODE。
*   **引导机制内在化**：将分类器引导和无分类器引导自然地表达为对逆向动力学中得分项的调整。

### 7. 优点
*   **系统性极强**：从最基本的条件高斯分布和微分算子出发，逐步构建起整个理论体系，逻辑链条清晰。
*   **数学严谨**：几乎所有关键结论都附有详细证明或引理参考，附录中补充了伊藤积分、费舍尔恒等式等数学基础，具有很高的自学价值。
*   **视角统一**：成功揭示了长期并行发展的各技术流派之间的本质联系，为研究者提供了深刻的洞察。
*   **符号清晰**：精心设计了区分连续时间、离散时间、前向和逆向时间等多种对象的符号体系，有效避免了混淆。

### 8. 不足与局限
*   **缺乏实践指导**：作为理论教程，没有涉及具体的实现细节（如网络架构、超参数设置）、计算效率分析或实际应用案例，读者需结合其他资料才能落地。
*   **教学内容覆盖局限**：部分高级技术如高阶DPM-Solver的完整推导、更复杂的引导策略（如CLIP引导等）未被展开详述。
*   **假设前提**：推导基于理想化的函数光滑性和可逆性假设，未讨论实际应用中可能遇到的数值稳定性等问题。

（完）

Title: Continual Learning in Modern Hopfield Networks with an Application to Diffusion Models

URL Source: https://arxiv.org/pdf/2605.27975v1

Published Time: Thu, 28 May 2026 00:47:02 GMT

Number of Pages: 33

Markdown Content:
# Continual Learning in Modern Hopfield Networks with an Application to Diffusion Models 

Ken Takeda 1,2 Masafumi Oizumi 1 Ryo Karakida 2, * 

Abstract 

Generative models, including diffusion models, are increasingly used as foundation models and adapted through sequential fine-tuning, making continual learning an essential problem setting. However, continual learning in such generative models remains poorly understood: after a task change, what aspects of the learned distribution are most easily lost, and what replay samples should be prioritized? We address these questions through the modern Hopfield energy. Recent links between modern Hopfield networks (MHNs) and diffusion models allow analyses in MHNs to be transferred to diffusion models. We introduce intrinsic forgetting as an increase in Hopfield energy after the task change. In tractable settings in an MHN, we prove that high-energy, outlier-like samples undergo a larger energy increase than cluster-like samples, implying that samples located in sharp, isolated basins are more forgettable. We further analyze memory replay and show that replay is particularly effective for high-energy samples, enabling an energy-based selection of replay samples. We validate these predictions in experiments on MHNs and two diffusion models under continual-learning settings: Stable Diffusion and a pixel-space DDPM. In these diffusion models, Hopfield energy tracks reconstruction-based forgetting, and replay experiments reveal energy-dependent mitigation of forgetting that is consistent with the MHN analysis. 

# 1 Introduction 

Generative models form a foundation of modern AI, and in particular, diffusion models have become a default backbone for image generation [ 1 –4 ]. In recent years, generative models have increasingly been used not only after training from scratch, but also undergoing continual knowledge transfer, such as continual pretraining and fine-tuning in different domains. This brings continual learning (CL) of diffusion models to the foreground as a practical engineering problem and a fast-growing empirical literature [ 5 –8 ], Continual learning for diffusion models remains largely empirical: the mechanism of catastrophic forgetting is still poorly understood, and even basic questions that have been extensively studied in supervised models remain open– “ which information of Task-1 will this model forget after training on Task 2?” and “ which Task-1 samples should we replay to prevent it?” Our approach to such fundamental questions is through Hopfield networks, which have played a central role in advancing the understanding and development of generative models [ 9 , 10 ]. A distinctive feature of Hopfield networks is that they admit an explicitly computable energy function. This enables systematic architecture design with the energy function serving as a guiding principle [ 11 ,12 ], as well as the development of machine learning methods [ 13 –15 ]. In particular, modern Hopfield networks (MHNs) form a family of models that has been actively studied since the rise of deep learning and has been extended to deep learning architectures [ 10 , 16 ]. Crucially, a growing literature  

> 1Graduate School of Arts and Science, The University of Tokyo, Tokyo, Japan
> 2Artificial Intelligence Research Center, AIST, Tokyo, Japan
> *Corresponding author, Email: karakida.ryo@aist.go.jp

1

> arXiv:2605.27975v1 [cs.LG] 27 May 2026

ｚｚ v          

> Task 1landscape E(ξ|X
> ₁)
> Task 2landscape E(ξ|X
> ₂)
> broad basin
> (typical / cluster)
> sharp basin
> (atypical / outlier)
> ΔE (xc)
> (small)
> ΔE(xo)
> (large)

ｚｚ v 

> state ξ
> Hopfield energy  E( ξ )

Figure 1: Conceptual picture. The Hopfield energy E(ξ | X) of associative memory / diffusion models carves two qualitatively different basins: broad and low-energy basins around typical samples, and sharp and high-energy basins around outliers. A task change shifts the landscape (red dashed). The resulting energy rise at the Task-1 memory, ∆E(x) = E(x | X2) − E(x | X1), referred to as intrinsic forgetting. We prove ∆E(xo) ≫ ∆E(xc): Memories in high-energy basins are more susceptible to forgetting. identifies an underlying connection between MHNs and diffusion models: the score function of a Gaussian kernel-density estimator is exactly a Hopfield update [ 17 , 18 ], and backward diffusion dynamics can be read as gradient flows on an implicit log-sum-exp energy [19, 20]. In this work, we reveal that the Hopfield energy serves as a theoretically grounded measure for quantitatively characterizing forgetting and prioritizing which samples to replay. Figure 1 illustrates the core idea of quantifying forgetting in generative models admitting the Hopfield energy, whose details we explain in Section 4. For such models, changes in generation probability can be tracked by observing the energy. By characterizing the forgetting of knowledge as a decrease in its generation probability, we can reduce the problem of quantifying forgetting to measuring an increase in the corresponding energy. Regions with densely clustered memory patterns form low energy, less sharp landscapes. Therefore, even if memory vectors shift from Task 1 to Task 2, the energy increase remains relatively small, so the corresponding states are robust to forgetting. By contrast, a Task-1 memory lying in a region with sparse or isolated memories undergoes a much larger energy increase and forgetting. This picture is analogous to the view that associates flat minima in parameter space with better generalization to test samples and out-of-distribution (OOD) samples [21, 22]. Main contributions are summarized as follows: 

• Hopfield energy view of forgetting supported by theoretical analysis (Theorem 4.1). We introduce intrinsic forgetting as the increase in Hopfield energy caused by a task transition. This provides an internal measure to quantify how the learned generative landscape changes, without relying solely on external classifiers or distributional metrics such as FID. To formalize the intrinsic forgetting shown in Figure 1, we analyze a modern Hopfield network with structured memories. The analysis shows that outlier-like memories, corresponding to the high-energy state and sharp basin, undergo a larger energy increase under a task perturbation than typical cluster memories. 

• Analysis of energy-dependent replay (Theorem 5.1). We analyze how Hopfield energy controls the effect of memory replay. The analysis shows that high-energy samples can produce 2larger replay gains than low-energy samples, revealing an energy dependence in the effect of replay. This suggests that Hopfield energy may provide a useful signal for replay-buffer design. Controlled experiments with an MHN and its Boltzmann-machine counterpart support this energy-dependent replay picture. 

• Empirical examination in diffusion models (Section 4.3, 5.3) . We empirically confirm these theoretical predictions using two diffusion models in continual-learning settings: Stable Diffusion v1.5 fine-tuned on split CIFAR-10 and a pixel-space DDPM trained on the same one. In both settings, Hopfield energy tracks reconstruction-based forgetting, and replay experiments show energy-dependent changes in reconstruction error consistent with the MHN analysis. Thus, we expect that this work will serve as a theoretical and empirical foundation for exploring forgetting and continual learning in modern generative models. 

# 2 Related Work 

Modern Hopfield networks. Recent work on MHNs has developed various extensions of the classical Hopfield network [ 9 ], including energy functions beyond pairwise correlations [ 23 , 24 ] and architectural generalizations [ 10 ]. In particular, Ramsauer et al. [16] showed that a Hopfield network with a log-sum-exp energy function corresponds to softmax attention in modern deep learning architectures. The term MHN is also commonly used to refer specifically to the model of [ 16 ], and we follow this convention. A distinctive feature of MHNs is that they admit an explicit energy function of states. Recent studies have increasingly exploited this energy as an intrinsic model quantity, for example, to systematically design architectures from its gradient [ 10 , 11 ], to detect out-of-distribution (OOD) samples [ 13 , 14 ], and to derive biologically plausible algorithms [ 15 ]. In this work, we reveal that the energy also works as a useful indicator of forgetting in generative models. 

Connection between MHNs and diffusion models. Raya and Ambrogioni [19] showed that diffusion trajectories can be viewed as dynamics on an implicit potential landscape, with attractor-like behavior toward the data manifold. Ambrogioni [17] further made the connection to MHNs explicit by showing that, for finite normalized patterns, the low-noise diffusion energy is asymptotically identical to the MHN energy. Recent work has more clarified this view [ 20 ] and extended it to memorization/generalization behavior in diffusion models [ 18 , 20 ]. Our work uses this established MHN–diffusion correspondence in a new setting: continual learning of generative models. 

Continual learning in generative models. Continual learning in generative models has mainly been studied from a practical perspective, with the aim of mitigating catastrophic forgetting, rather than from the viewpoint of theoretically understanding its mechanism. Early studies considered replay algorithms in Hopfield networks, while much of the recent work has focused on VAEs and GANs, including teacher–student generative modeling, memory-replay GANs, latent space consolidation, and distillation for conditional generation [ 25 –28 ]. More recently, continual learning has also been studied for diffusion models, including experimental demonstrations of forgetting and its mitigation by replay [ 5 ], a regularization method tailored to diffusion models [ 29 ], and parameter-efficient adaptation and distillation in text-to-image diffusion models [6, 8]. In contrast to these application-oriented studies, our work uses MHNs as a theoretical bridge to understand 3the mechanism of forgetting and to provide a basis for developing principled methods to mitigate forgetting. 

# 3 Preliminaries 

3.1 Modern Hopfield Energy 

Let X = {xi}Ni=1 be a set of stored memories and let ξ ∈ Rd denote a state at which the memory landscape is evaluated. The continuous modern Hopfield energy is defined as 

E(ξ | X; β) = − 1

β log 

> N∑
> i=1

exp( βx ⊤ 

> i

ξ) + 12 ∥ξ∥2, (1) where β > 0 is an inverse-temperature parameter controlling the sharpness of the energy landscape [10, 16, 30]. For ∇ξE(ξ | X; β) = 0 , its fixed points satisfy 

ξ =

> N∑
> i=1

pi(ξ | X)xi, pi(ξ | X) = exp( βx ⊤ 

> i

ξ) 

> ∑Nj=1

exp( βx ⊤ 

> j

ξ) . (2) Throughout this paper, E(ξ | X) denotes the Hopfield energy evaluated with respect to the memory set X, omitting β when it is clear from context. The energy is low when the state ξ lies near a cluster of memories, and high when it lies near an isolated memory. It is the scalar quantity we use below to characterize how the generative landscape changes across tasks. 

3.2 Boltzmann Machine Formulation 

Define the Boltzmann distribution of the MHN by 

pβ (ξ | X) = exp( −βE (ξ | X; β)) /Z, (3) where Z is the partition function. One can easily see that, for ∥xi∥ = 1 , Z is explicitly given by Z = N (2 π/β )d/ 2, and this Boltzmann distribution is a Gaussian mixture pβ (ξ | X) = 

> ∑
> i

exp 

> (

− β 

> 2

∥ξ − xi∥2)

/Z. In the following section, as one of our synthetic experiments, we perform unsupervised learning of an MHN by maximum-likelihood estimation of this Boltzmann distribution. Specifically, we regard this distribution as a Boltzmann machine (BM) with a trainable weight matrix X, and refer to it as MHN-BM. Since Z is analytically tractable, the model can be trained easily by gradient descent for maximum-likelihood learning. Note that a similar Boltzmann machine counterpart of MHNs was investigated by [ 31 ], but their treatment of the inverse temperature differs from our focus. 

3.3 Diffusion Models and Implicit Energy 

Diffusion models are not usually written as explicit energy-based models, but their score can be viewed as the negative gradient of an implicit, time-dependent energy. Let ps(x) be the noisy data marginal at diffusion time s, and write 

EDM ,s (x) := − log ps(x), ∇x log ps(x) = −∇ xEDM ,s (x).

Thus, roughly speaking, reverse sampling moves a noisy sample xs by noisy descent on EDM ,s ; under conservative-drift assumptions, this energy also includes the drift potential [ 19 ]. Its stable equilibria 4therefore act as attractors for generation and memory recall [ 17 , 18 ]. For a finite set of patterns corrupted by additive Gaussian noise, 

ps(x) ∝ ∑

> i

exp 

> (

−∥ x − yi∥2/2τ 2

> s
> )

.

Here τs > 0 denotes the standard deviation of the Gaussian corruption at diffusion time s. Expanding the square shows that, for normalized memories, EDM ,s reduces up to constants and positive rescaling to 

EDM,s (x) ∝ − 1

βs

log 

> N∑
> i=1

exp( βsy⊤ 

> i

x) + 12 ∥x∥2, βs = τ −2 

> s

,

which is the modern Hopfield energy in Eq. (1). We therefore use Hopfield energy as a tractable proxy for the implicit diffusion energy landscape. A more explicit derivation, including the variance-preserving case, is given in Appendix A. 

# 4 Energy as an Indicator of Forgetting 

We first introduce forgetting with the internal Hopfield energy. Defining “forgetting” in an un-supervised generative model is subtle: label-based metrics via external classifiers are heuristic measures [ 5 , 6], but the generative objective of our interest is essentially unsupervised and class information is not necessarily relevant to the input data. We need an internal, model-native measure applicable to pure unsupervised settings. 

Intrinsic forgetting via Hopfield energy. Let Xold and Xnew denote the stored memory set at task 1 and task 2. For any state x, we define the intrinsic forgetting at x as 

∆E(x) = E(x | X new ) − E(x | X old ). (4) Large positive ∆E(x) means that the energy landscape at x has risen, so the model assigns a smaller probability to that state after the task transition. Under the assumption that the score function has sufficient expressive capacity to approximate the data distribution of each task nearly perfectly, this quantity captures forgetting induced by the intrinsic and unavoidable distributional differences between tasks. It is noteworthy that, for a fixed number of memories, 

pβ (ξ | X new )/p β (ξ | X old ) = exp [−β∆E(ξ)] . Thus, intrinsic forgetting naturally arises when distributional changes between tasks are measured in terms of probability density ratios. 

4.1 Theoretical analysis: equal-angular cluster plus outlier under small rotation 

We derived an analytical formulation of intrinsic forgetting within a toy setting. 

Setup (Fig. 2). Consider memory vectors (corresponding to training samples) x1, . . . , x N , x o ∈

Sd−1 satisfying 

x⊤ 

> i

xj = c (i̸ = j; i, j ≤ N ), x⊤ 

> i

xo = 0 ( i ≤ N ), 0 < c < 1, (5) so {xi}i≤N is an equal-angular cluster and xo is the orthogonal outlier . A canonical realization in 

d ≫ 1 is the latent-Gaussian construction xi = √c μ + √1 − c z i with a fixed unit centroid μ and i.i.d. unit-variance Gaussian residuals zi ⊥ μ, for which ∥xi∥2 = 1 and x⊤ 

> i

xj = c + O(d−1/2) concentrate 5at the desired geometry; the results below depend only on (5). Task 1 stores X = {x1, . . . , x N , x o};Task 2 is provided by its rational domain shift: 

X2 = V X, with V = exp( εΩ) , Ω = ( W − W ⊤)/2. (6) 

Figure 2: Memory configura-tion. 

Note that the rotation identity E(ξ | V X ) = E(V ⊤ξ | X)

converts a domain shift into a change of argument at the original landscape. This transformation facilitates our analysis. In the analysis, we assume a sufficiently small random rotation with 

Wij iid 

∼ N (0 , 1) and ε ≪ 1. In addition, we work at finite N ,large β, where individual memories are still fixed points [ 30 , 32 ]. 

Fixed points nearby each memory vector. Let ξ∗

> cl

, ξ ∗

> o

denote the cluster-side and outlier-side Task-1 fixed points. By the symmetry of (5), the softmax weights at each Task-1 fixed point reduce to scalar self-consistency conditions whose large-

β asymptotics (proved in Appendix B) give the closed-form expansions 

ξ∗ 

> o

∼ xo + e−β (s − N x o), ξ∗ 

> cl

∼ xcl + e−β(1 −c)( ∑  

> j≥2

xj − (N − 1) xcl 

> )

, (7) with s := ∑ 

> i≤N

xi. For clarity, here and below we use the informal shorthand “ ∼” to show only the leading-order term; the precise lower-order terms and error bounds are given in Appendix B (Eqs. (52), (54) for the fixed points and Eqs. (71)–(72) for the energy rises). The dominant off-memory weights are therefore e−β(1 −c) on the cluster branch and e−β on the outlier branch, with e−β(1 −c) ≫ e−β for 0 < c < 1. Geometrically, the cluster fixed point is supported by N − 1 near-neighbors at cosine c, while the outlier sees only orthogonal competitors. Appendix C shows that these same off-memory weights also control the local energy level and the local sharpness of the basin, as measured by the Hessian trace of the energy landscape. This justifies the shorthand used below: the cluster branch is low-energy and broad, whereas the outlier branch is high-energy and sharp. 

Intrinsic forgetting at the fixed points. For a Task-1 fixed point ξ∗, write ⟨ · ⟩ V := EΩ[ · ] for the expectation over the small-rotation ensemble of (6). We absorb this average into the definition and express the (rotation-averaged) intrinsic forgetting at ξ∗ as 

∆Efp  

> ∗

:= 〈E(ξ∗ | V X ) − E(ξ∗ | X)〉 

> V

. (8) Since ξ∗ is stationary, Taylor expansion combined with the rotation identity gives, at leading order, 

∆Efp  

> ∗

∼ ε2

> 4
> (

∥ξ∗∥2 tr H∗ − ξ⊤∗ H∗ξ∗

> )

, H∗ = I − β 

> ( ∑
> i

p∗ 

> i

xix⊤ 

> i

− ξ∗ξ⊤∗

> )

. (9) Writing the off-memory weights as p∗ 

> k

= 1 − ∑  

> j̸=k

ηj , p∗ 

> j

= ηj ≪ 1 (with αj := x⊤ 

> j

xk) gives the following: 

∆Efp  

> ∗

∼ ε2

> 4
> [

(d − 1) − ∑ 

> j̸=k

ηj Λ( αj )

> ]

, Λ( α) := 2( d − 1)(1 − α) + β(1 − α2). (10) Specializing (10) to the two representative Task-1 fixed points, namely the outlier ( N competitors at α = 0 , η = e−β ) and the cluster ( N − 1 competitors at α = c, η = e−β(1 −c) plus one competitor at α = 0 , η = e−β ), we obtain the following. 6Theorem 4.1 (High-energy patterns are forgotten more) . Assume 0 < c < 1 and β ≫ log N . For sufficiently small ε and large β,

∆Efp  

> o

− ∆Efp cl ∼ ε2 

> 4

(N − 1) [e−β(1 −c) Λ( c) − e−β Λ(0) ] > 0. (11) 

Proof sketch. e−β(1 −c) ≫ e−β at large β and Λ( c) > 0 for 0 < c < 1, so the first term dominates. The genuine-cluster regime 0 < c < 1 is essential: at c = 0 both branches coincide at leading order, while c < 0 can flip the sign of (11). The precise lower-order terms and the full derivation are in Appendix B. □

4.2 Synthetic verification in MHN and Boltzmann-machine learning 

We verify Theorem 4.1 in synthetic cluster-plus-outlier systems. The closed-form MHN uses an equal-angular cluster and an orthogonal outlier with N = 12 , d = 50 , c = 0 .35 , β = 8 , ε = 0 .05 , averaged over 2000 random rotations. The outlier fixed point has a larger rotation-induced energy rise than the cluster fixed points, as predicted by Theorem 4.1; details are in Appendix E.1. The same ordering also appears after likelihood training in the MHN-BM with N = 12 , d = 50 , c = 0 .35 , β = 16 , ε rot = 0 .02 ,shown in Fig. 3a. 

4.3 Application to Diffusion models 

We next examine whether the prediction of Theorem 4.1 extends beyond the tractable MHN setting to continual learning in diffusion models . The main text focuses on incremental class learning with split CIFAR-10 , which is a standard benchmark for continual learning. We also report results for 

domain-incremental learning with rotated MNIST in Appendix F. This setting most closely matches the rotation-based analysis in Sections 4.1 and 5.1, and yields the same qualitative conclusions. We evaluate two practically relevant diffusion-model systems on split CIFAR-10: latent-space 

Stable Diffusion v1.5 fine-tuned on the task split (Fig. 3b), and a pixel-space DDPM trained from scratch on the same split (Fig. 3c). Following prior work [ 17 – 19 ], we define the per-sample Hopfield energy of a diffusion model using Eq. (1), with the stored memories {xi} given by the Task-1 training images. 

Reconstruction metric for forgetting. In the diffusion-model experiments, we additionally use a reconstruction-based measure of forgetting, which evaluates how well a Task-1 sample can still be recovered by the model after Task-2 training. Specifically, we corrupt a Task-1 sample x to a fixed diffusion timestep t⋆, run the post-Task-2 reverse process from t⋆ back to 0, and measure 

Ft⋆ (x) = ∥x − ˆx0,t ⋆ ∥22. (12) A larger Ft⋆ (x) indicates that the post-Task-2 reverse dynamics is less able to reconstruct the original Task-1 sample. We use this heuristic metric to examine whether the energy-based tendency predicted by intrinsic forgetting is also reflected in the behavior of trained diffusion models. Details of this metric and robustness checks across other timesteps are given in Appendix G. We also report a perturbation-based sharpness analysis on CIFAR-10 in Appendix C.4, showing how local sharpness relates to both Hopfield energy and reconstruction-based forgetting. 

Stable Diffusion on split CIFAR-10 (Fig. 3b). We fine-tune Stable Diffusion v1.5 ( runwayml/ stable-diffusion-v1-5 , VAE frozen, only the UNet trained) on a split CIFAR-10 protocol where each task is composed of different 5 classes. After Task 1 we assign every Task-1 image a scalar 7(a) MHN-BM. (b) Stable Diffusion. (c) Pixel diffusion. 

Figure 3: Energy predicts per-sample forgetting across diffusion models and a trainable synthetic model. (a) In the trainable MHN-BM, the same ordering emerges after likelihood training on the rotated two-task setup. The MHN with synthetic clustering memory is reported in Appendix E.1. (b) Stable Diffusion v1.5 on split CIFAR-10: per-sample reconstruction MSE Ft⋆ (x) after Task 2 training vs. internal Hopfield energy measured on Task 1. Higher-energy images suffer larger reconstruction forgetting. (c) Pixel-space DDPM on the same split reproduces the same monotone relation. Hopfield energy Eθ(x) computed via the log-sum-exp expression (1) over all training set of Task-1 images. This is our operationalization of the diffusion model’s intrinsic energy at x. After Task 2 training, we measure the forgetting by the per-sample reconstruction MSE at noise level t = 0 .8.This is the main diffusion-model instantiation of Theorem 4.1. 

Pixel-space diffusion (Fig. 3c). To check that the relation is not specific to the latent space learned by Stable Diffusion, we repeat the same protocol with a DDPM trained directly in pixel space on 32 × 32 split CIFAR-10 from scratch. The same energy-vs-forgetting ordering is reproduced: per-sample reconstruction MSE after Task 2 is a monotone-increasing function of the Task-1 Hopfield energy. Together, Fig. 3b and Fig. 3c show that Hopfield energy is a model-agnostic, sample-level forgetting indicator for diffusion CL. 

# 5 Energy-guided replay prioritization 

Given that high-energy memories are forgotten stronger, the natural question is whether they should be replayed more. We answer this affirmatively with a second theorem, then turn it into a selection rule of replay samples. 

5.1 Theoretical analysis: baseline-subtracted replay gain 

We add one Task-1 replay sample xr to the rotated Task-2 dataset, X+r 

> 2

(V ) := V X ∪ { xr}, with the replayed sample counted as an additional term in the log-sum-exp. Define the raw replay gain at the Task-1 fixed point ξ∗ 

> k

as 

Rr→k(V ) := E(ξ∗ 

> k

| X2(V )) − E(ξ∗ 

> k

| X+r 

> 2

(V )) . (13) Writing ∆Efp  

> k

(V ) := E(ξ∗ 

> k

| X2(V )) − E(ξ∗ 

> k

| X) for the intrinsic forgetting (8), the gain decomposes as 

Rr→k(V ) = ∆Efp  

> k

(V )  

> ︸︷︷ ︸
> forgetting w/o replay

− [E(ξ∗ 

> k

| X+r 

> 2

(V )) − E(ξ∗ 

> k

| X)]︸ ︷︷ ︸

> forgetting w/ replay

,

8so Rr→k(V ) measures the reduction of intrinsic forgetting due to replay. With the softmax weight 

pr|k = eβx ⊤  

> rξ∗
> k

/ ∑ 

> μ∈I

eβx ⊤  

> μξ∗
> k

, the log-sum-exp form of E gives Rr→k(V ) = 1 

> β

log (1+ pr|k eβ∆Efp   

> k(V)

), of which Rr→k(I) = 1 

> β

log (1 + pr|k) is the trivial duplicate-memory baseline. Subtracting this baseline and Taylor-expanding in ∆Efp  

> k

(V ) = O(ε2) gives, with ρr|k := pr|k/(1 + pr|k) (full derivation in Appendix D), 

∆r→k := 〈Rr→k(V ) − R r→k(I)〉 

> V

∼ ρr|k ∆Efp  

> k

. (14) We call ρr|k a replay susceptibility because it is the linear response of the baseline-subtracted replay gain to the energy rise ∆Efp  

> k

. Replay gain = replay susceptibility × rotation-induced energy rise. 

The first factor asks how much softmax weight the replay point already has at the target fixed point (geometry); the second is exactly the rotation-averaged rise ∆Efp  

> k

produced by Theorem 4.1 (drift). Let xcl 1 be a cluster memory and let xcl 2̸ = xcl 1 be another member of the same cluster. The replay susceptibilities satisfy 

ρcl 1|cl 1 , ρ o|o ∼ 12 , ρcl 1|cl 2 ∼ e−β(1 −c), ρcl 1|o, ρ o|cl 1 ∼ e−β .

The precise lower-order corrections are in Appendix D (Eqs. (97), (98)). Combining these with the inequality of ∆Efp  

> k

in Theorem 11, we obtain the following. 

Theorem 5.1 (Replay ordering) . Under the assumptions of Theorem 4.1, the baseline-subtracted replay gain factorises as (14). The five canonical gains obey 

∆o→o > ∆cl 1→cl 1 ≫ ∆cl 1→cl 2 ≫ { ∆cl 1→o, ∆o→cl 1 }, ∆cl 1→o ≍ ∆o→cl 1 . (15) 

Intuition. Equation (14) says that a replay sample reduces forgetting in proportion to its softmax weight at the target fixed point. The first inequality, ∆o→o > ∆cl 1→cl 1 , compares self-replay: both have susceptibility ρ ≃ 1/2, but the outlier target has the larger energy rise by Theorem 4.1, so high-energy self-replay gives the largest per-sample rescue. The gap ∆cl 1→cl 1 ≫ ∆cl 1→cl 2 says that replacing self-replay by another cluster member loses the small overlap factor e−β(1 −c), although it can still help nearby low-energy states. Finally, ∆cl 1→cl 2 ≫ { ∆cl 1→o, ∆o→cl 1 } and ∆cl 1→o ≍ ∆o→cl 1

mean that cross-type replay between the cluster and the outlier is exponentially weaker, because their softmax overlap is only e−β . Thus high-energy replay gives a tall-but-narrow rescue, while low-energy cluster replay gives a shallower-but-broader rescue over neighboring low-energy states. 

5.2 Synthetic verification of replay ordering 

Figure 4: MHN-BM corroboration of the replay ordering. We verify Theorem 5.1 on the same synthetic systems. In the closed-form MHN with N = 12 , d = 50 , c = 0 .35 , β = 8 , ε = 0 .05 ,the replay-gain shows the predicted hierarchy: outlier self-replay is largest, cluster self-replay is next, within-cluster cross replay is smaller, and cross-type replay between the cluster and the outlier is weakest. The full result is reported in Appendix E.2. The trained MHN-BM with N = 12 , d = 50 , c = 0 .35 , β =16 , ε rot = 0 .02 shows the almost same qualitative ordering in Fig. 4. 

5.3 Application to Diffusion models 

Theorem 5.1 delivers a concrete selection rule of replay samples: among Task-1 samples, replay those that maximize their own energy (thereby inheriting a large ∆Efp  

> k

) and that self-replay (so that ρk|k ≈ 1/2). The simplest realization is energy-guided top-K replay .9(a) High-energy replay. (b) Low-energy replay. (c) Mean Reduction. (d) Sum Reduction. 

Figure 5: Energy-guided replay on Stable Diffusion / split CIFAR-10. (a) Per-sample reconstruction MSE after Task 2 with the top-K high-energy replay buffer: MSE drops sharply for high-energy images (strong but narrow rescue). (b) With the bottom-K low-energy buffer the per-sample drop is smaller but spread across a wider energy range (weaker but broader rescue). (c,d) Per-bin MSE reduction (baseline − replay) versus energy, averaged (c) and summed (d) over the bin: the high-energy buffer concentrates its rescue at high energy, the low-energy buffer distributes a smaller per-sample rescue over many more samples, exactly as Theorem 5.1 predicts via ∆r→k ≈ ρr|k ∆Efp  

> k

.

Replay buffer. We pick the top-K highest-energy samples of Task 1 as the buffer, B =TopK x∈D 1 Eθ(x), with K = 5000 in all experiments. During Task-2 fine-tuning we augment each mini-batch with replay samples drawn from B, so that the effective training set is D2 ∪ B (25,000 new + 5,000 replay images per epoch). We compare against random replay (uniform top-K) and no replay baselines. We again focus on the two diffusion-model systems of Section 4.3 as the main empirical test. 

Stable Diffusion (Fig. 5). On split CIFAR-10, we compare three replay policies with iden-tical buffer size K = 5000 : energy-guided top-K (the K highest-energy Task-1 images), random ,and energy-guided bottom-K (the K lowest-energy images). After Task-2 fine-tuning, per-sample reconstruction MSE at t = 0 .8 is measured for every Task-1 image and binned by energy. Eq. (14) predicts a mean–range tradeoff in the replay effect: ∆r→k ∼ ρr|k ∆Efp  

> k

. The energy-rise term ∆Efp  

> k

makes the per-sample replay gain larger for high-energy targets, whereas the relevance factor ρr|k determines how broadly a replay sample affects other targets. Thus, high-energy replay is expected to produce a large but localized reduction in the high-energy tail, while low-energy replay is expected to produce a smaller reduction spread over a broader set of low-energy samples. This is what we observe: the high-energy buffer (Fig. 5a) yields a sharply peaked bin-mean reduction at high energy (Fig. 5c), but its bin-sum reduction is confined to a narrow energy range (Fig. 5d). In contrast, the low-energy buffer (Fig. 5b) gives a smaller bin-mean reduction at low energy, but the affected range is broader, so the bin-sum reduction extends across a wider energy interval. This complementary pattern is consistent with the replay ordering in Eq. (15). 

Pixel-space diffusion (Fig. 6). We repeat the three-policy comparison with the pixel-space DDPM of Section 4.3. Every qualitative conclusion from Stable Diffusion is reproduced: (i) high-energy replay protects high-energy images, (ii) low-energy replay only helps low-energy images, (iii) aggregated reductions place energy-guided top-K above random above low-energy. The replay benefit is again concentrated in the high-energy tail, as predicted. Together, Fig. 5 and Fig. 6 constitute our main empirical case that Hopfield energy is a practical, diffusion-agnostic replay criterion. 10 (a) High-energy replay. (b) Low-energy replay. (c) Mean Reduction. (d) Sum Reduction. 

Figure 6: The same energy-guided replay analysis on a pixel-space DDPM (trained from scratch on split CIFAR-10) reproduces every qualitative conclusion of Fig. 5: high-energy replay rescues high-energy samples, low-energy replay rescues only low-energy samples, and the total reduction is dominated by the high-energy tail. The finding is not tied to the Stable Diffusion latent space. 

# 6 Conclusion and Limitations 

Through theoretical analyses of modern Hopfield networks and empirical confirmation on diffusion models, we find that Hopfield energy serves as an indicator of forgetting and also provides a criterion for selecting replay samples. These results suggest that forgetting in unsupervised generative models can be evaluated not only through external evaluation metrics, but also through the geometry of the learned state landscape. We expect that this work will serve as a theoretical and practical foundation for future studies of knowledge forgetting, replay analysis, and energy-based diagnostics of generative models. 

Limitations. (i) Forgetting through the learning of score functions. We consider intrinsic for-getting, that is, an unavoidable factor that arises when a generative model perfectly approximates task-dependent input distributions. In practical diffusion models, however, the score function is represented by a neural network, and additional forgetting may appear even for similar inputs. In supervised learning, theoretical analyses of such forgetting have been developed [ 33 ], and extending the analyses to the score function is an important direction for future work. (ii) Finite sample sizes. Although our theory can handle structured data, it is currently restricted to the case of finitely many memories. Quantifying forgetting in regimes where the number of memories grows with the input dimension, and where the memories encode richer data manifolds [ 32 ], remains an open problem. (iii) Broader experimental validation. For future practical development, it would also be useful to examine scenarios not covered in this study, such as domains beyond images and online continual learning without explicit task boundaries. A more elaborated indicator of internal states (e.g., probability-flow ODE marginals) could tighten the correlation of Fig. 3b, 3c. 

# References 

[1] Jonathan Ho, Ajay Jain, and Pieter Abbeel. Denoising diffusion probabilistic models. In 

Advances in Neural Information Processing Systems , 2020. [2] Jiaming Song, Chenlin Meng, and Stefano Ermon. Denoising diffusion implicit models. arXiv preprint arXiv:2010.02502 , 2020. [3] Prafulla Dhariwal and Alex Nichol. Diffusion models beat GANs on image synthesis. arXiv preprint arXiv:2105.05233 , 2021. 11 [4] Robin Rombach, Andreas Blattmann, Dominik Lorenz, Patrick Esser, and Björn Ommer. High-resolution image synthesis with latent diffusion models. arXiv preprint arXiv:2112.10752 ,2021. [5] Michał Zaj¸ ac, Kamil Deja, Anna Kuzina, Jakub M. Tomczak, Tomasz Trzciński, Florian Shkurti, and Piotr Miłoś. Exploring continual learning of diffusion models. arXiv preprint arXiv:2303.15342 , 2023. [6] James Seale Smith, Yen-Chang Hsu, Lingyu Zhang, Ting Hua, Zsolt Kira, Yilin Shen, and Hongxia Jin. Continual diffusion: Continual customization of text-to-image diffusion with C-LoRA. Transactions on Machine Learning Research , 2024. ISSN 2835-8856. [7] Rui Gao and Weiwei Liu. DDGR: Continual learning with deep diffusion-based generative replay. In International Conference on Machine Learning , volume 202, pages 10744–10763. PMLR, 2023. [8] Sergi Masip, Pau Rodriguez, Tinne Tuytelaars, and Gido M van de Ven. Continual learning of diffusion models with generative distillation. In Third Conference on Lifelong Learning Agents (CoLLAs) , 2024. [9] John J. Hopfield. Neural networks and physical systems with emergent collective computational abilities. Proceedings of the National Academy of Sciences , 79(8):2554–2558, 1982. [10] Dmitry Krotov and John J Hopfield. Large associative memory problem in neurobiology and machine learning. In International Conference on Learning Representations , 2021. [11] Benjamin Hoover, Hendrik Strobelt, Dmitry Krotov, Judy Hoffman, Zsolt Kira, and Duen Horng Chau. Memory in plain sight: Surveying the uncanny resemblances of associative memories and diffusion models. In NeurIPS 2023 Workshop on Associative Memory and Hopfield Networks ,2023. [12] Nima Dehmamy, Benjamin Hoover, Bishwajit Saha, Leo Kozachkov, Jean-Jacques Slotine, and Dmitry Krotov. NRGPT: An energy-based alternative for GPT. In International Conference on Learning Representations , 2026. [13] Jinsong Zhang, Qiang Fu, Xu Chen, Lun Du, Zelin Li, Gang Wang, Xiaoguang Liu, Shi Han, and Dongmei Zhang. Out-of-distribution detection based on in-distribution data patterns memo-rization with modern hopfield energy. In International Conference on Learning Representations ,September 2022. [14] Claus Hofmann, Simon Schmid, Bernhard Lehner, Daniel Klotz, and Sepp Hochreiter. Energy-based hopfield boosting for out-of-distribution detection. In Advances in Neural Information Processing Systems , volume 37, 2024. [15] Benjamin Scellier and Yoshua Bengio. Equilibrium propagation: Bridging the gap between energy-based models and backpropagation. Frontiers in computational neuroscience , 11:24, 2017. [16] Hubert Ramsauer, Bernhard Schäfl, Johannes Lehner, Philipp Seidl, Michael Widrich, Thomas Adler, Lukas Gruber, Markus Holzleitner, Milena Pavlović, Geir Kjetil Sandve, Victor Greiff, David Kreil, Michael Kopp, Günter Klambauer, Johannes Brandstetter, and Sepp Hochreiter. Hopfield networks is all you need. In International Conference on Learning Representations ,2021. 12 [17] Luca Ambrogioni. In search of dispersed memories: Generative diffusion models are associative memory networks. Entropy , 26(5):381, 2024. doi: 10.3390/e26050381. [18] Bao Pham, Gabriel Raya, Matteo Negri, Mohammed J Zaki, Luca Ambrogioni, and Dmitry Krotov. Memorization to generalization: Emergence of diffusion models from associative memory. arXiv preprint arXiv:2505.21777 , 2025. [19] Gabriel Raya and Luca Ambrogioni. Spontaneous symmetry breaking in generative diffusion models. In Advances in Neural Information Processing Systems , volume 36, pages 66377–66389, 2023. [20] Luca Ambrogioni. The statistical thermodynamics of generative diffusion models: Phase transitions, symmetry breaking, and critical instability. Entropy , 27(3):291, 2025. doi: 10.3390/ e27030291. [21] Nitish Shirish Keskar, Dheevatsa Mudigere, Jorge Nocedal, Mikhail Smelyanskiy, and Ping Tak Peter Tang. On large-batch training for deep learning: Generalization gap and sharp minima. In International Conference on Learning Representations , 2017. [22] Junbum Cha, Sanghyuk Chun, Kyungjae Lee, Han-Cheol Cho, Seunghyun Park, Yunsung Lee, and Sungrae Park. SWAD: Domain generalization by seeking flat minima. In Advances in Neural Information Processing Systems , volume 34, pages 22405–22418, 2021. [23] Dmitry Krotov and John J Hopfield. Dense associative memory for pattern recognition. In 

Advances in Neural Information Processing Systems , June 2016. [24] Mete Demircigil, Judith Heusel, Matthias Löwe, Sven Upgang, and Franck Vermet. On a model of associative memory with huge storage capacity. Journal of Statistical Physics , 168:288–299, 2017. [25] Jason Ramapuram, Magda Gregorova, and Alexandros Kalousis. Lifelong generative modeling. 

Neurocomputing , 404:381–400, 2020. doi: 10.1016/j.neucom.2020.02.115. [26] Chenshen Wu, Luis Herranz, Xialei Liu, Yaxing Wang, Joost van de Weijer, and Bogdan Raducanu. Memory replay GANs: Learning to generate new categories without forgetting. In 

Advances in Neural Information Processing Systems , volume 31, 2018. [27] Mengyao Zhai, Lei Chen, Frederick Tung, Jiawei He, Megha Nawhal, and Greg Mori. Lifelong GAN: Continual learning for conditional image generation. In Proceedings of the IEEE/CVF International Conference on Computer Vision , pages 2759–2768, 2019. [28] Kamil Deja, Paweł Wawrzyński, Wojciech Masarczyk, Daniel Marczak, and Tomasz Trzciński. Multiband VAE: Latent space alignment for knowledge consolidation in continual learning. 

arXiv preprint arXiv:2106.12196 , 2021. [29] Zekun Wang, Anant Gupta, Zihan Dong, and Christopher J MacLellan. Avoid catastrophic forgetting with rank-1 fisher from diffusion models. In International Conference on Learning Representations , 2026. [30] Carlo Lucibello and Marc Mézard. Exponential capacity of dense associative memories. Phys. Rev. Lett. , 132(7):077301, February 2024. 13 [31] Toshihiro Ota and Ryo Karakida. Attention in a family of Boltzmann machines emerging from modern Hopfield networks. Neural Computation , 35:1463–1480, 2023. doi: 10.1162/neco_a_ 01597. [32] Beatrice Achilli, Luca Ambrogioni, Carlo Lucibello, Marc Mézard, and Enrico Ventura. The capacity of modern hopfield networks under the data manifold hypothesis. arXiv preprint arXiv:2503.09518 , 2025. [33] Sebastian Lee, Stefano Sarao Mannelli, Claudia Clopath, Sebastian Goldt, and Andrew Saxe. Maslow’s hammer in catastrophic forgetting: Node re-use vs. node activation. In International Conference on Machine Learning , pages 12455–12477. PMLR, 2022. 14 Appendices 

# A Diffusion–Hopfield Correspondence 

We summarize the correspondence between diffusion energies and modern Hopfield energies. Consider a forward noising SDE 

dY s = f (Ys, s ) ds + g(s) d̂ Ws,

and let ps(x) be the marginal density of Ys. For additive noise and conservative drift, the reverse-time SDE can be written as 

dX t =

[

g2(T − t)∇x log pT −t(Xt) − f (Xt, T − t)

]

dt + g(T − t)dW t.

Equivalently, 

dX t = −∇ xET −t(Xt) dt + g(T − t)dW t,

where the implicit energy is 

Es(x) = −g2(s) log ps(x) + 

∫ x

> 0

f (z, s ) · dz, 

up to an additive constant [ 19 ]. To examine the attractor structure at a fixed diffusion time s,we freeze the time-dependent coefficients g(s) and f (·, s ) and study the deterministic drift part of the reverse SDE. That is, we drop only the Brownian increment g(s)dW t, while keeping the 

g2(s)∇x log ps term in the drift. Under the conservative-drift assumption above, f (·, s ) = ∇Φs(·) for a scalar potential Φs, so the line integral in Es is path independent. Along the deterministic flow 

dx λ

dλ = −∇ xEs(xλ),

the energy decreases as ddλ Es(xλ) = −∥∇ xEs(xλ)∥2 ≤ 0,

so stable equilibria satisfying ∇xEs(x) = 0 define the attractor structure of the reverse dynamics. Now assume that the data distribution is the empirical distribution over a finite set of patterns, 

p0(y) = 1

M

> M

∑

> i=1

δ(y − yi).

In the variance-exploding case, 

dY s = g(s)d̂Ws, f = 0 , τ 2 

> s

=

∫ s

> 0

g2(r) dr. 

Thus 

Ys | yi ∼ N (yi, τ 2 

> s

I),

and the noisy marginal is the Gaussian mixture 

ps(x) ∝ ∑

> i

exp 

(

− ∥x − yi∥2

2τ 2

> s

)

.

15 Expanding the square gives 

ps(x) ∝ exp 

> (

− ∥x∥2

2τ 2

> s
> ) ∑
> i

exp 

> (

τ −2 

> s

y⊤ 

> i

x − ∥yi∥2

2τ 2

> s
> )

.

When the memories are normalized, ∥yi∥ = r, the last term is independent of i and can be absorbed into the normalization. Since f = 0 , we have Es(x) = −g2(s) log ps(x). Multiplying by the positive factor τ 2 

> s

/g 2(s), which does not change the equilibria, yields 

−τ 2 

> s

log ps(x) = −τ 2 

> s

log ∑

> i

exp( τ −2 

> s

y⊤ 

> i

x) + 12 ∥x∥2 + const .

With βs = τ −2 

> s

, this becomes 

EMHN (x; βs) = − 1

βs

log 

> M∑
> i=1

exp( βsy⊤ 

> i

x) + 12 ∥x∥2 + const ,

which is the continuous modern Hopfield energy [ 17 ]. Thus, for discrete normalized patterns, the Gaussian-mixture marginal induced by the forward diffusion defines the same log-sum-exp energy landscape as a modern Hopfield network. For variance-preserving diffusions, the forward SDE is typically 

dY s = − 12 b(s)Ys ds +

> √

b(s) d̂Ws.

The transition kernel is still Gaussian, 

Ys | yi ∼ N (θsyi, τ 2 

> s

I), θs = exp 

> (

− 12 

> ∫s
> 0

b(r) dr 

> )

, τ 2 

> s

= 1 − θ2 

> s

.

Therefore ps is again a Gaussian mixture, now centered at the shrunk memories θsyi. Substituting this marginal into the implicit energy gives, for normalized memories, 

Es(x)

b(s) = − log ∑

> i

exp  

> (

θs

τ 2

> s

y⊤ 

> i

x

> )

+ 

> (

12τ 2

> s

− 14

> )

∥x∥2 + const .

Thus the variance-preserving case differs at finite noise by pattern shrinkage and time-dependent coefficients. In the low-noise regime, θs → 1 and τs → 0, this reduces to the same log-sum-exp plus quadratic structure, with effective inverse temperature proportional to θs/τ 2 

> s

. More generally, for additive SDEs with conservative drift, the low-noise transition kernel is locally Gaussian around each pattern, so the same Gaussian-mixture argument gives the Hopfield fixed-point structure asymptotically [17]. 

# B Full derivation of Theorem 4.1 

We organize the proof as follows. Subsection B.1 states the equal-angular cluster-plus-outlier model and the rotation identity. Subsection B.2 reduces the fixed-point equations by symmetry. Subsection B.3 separates the existence of pattern-specific fixed points from the stronger localization condition used to control leakage corrections. Subsection B.4 derives the large-β fixed-point asymptotics. Subsection B.5 collects the Hessian calculation, the rotation average, and the first-order expansion in the off-memory weights that produce the formula for ∆Efp  

> ∗

. Finally, Subsection B.6 specializes that formula to the outlier and cluster branches and proves Theorem 4.1. 16 B.1 Problem setup and rotation identity 

Let x1, . . . , x N , x o ∈ Sd−1 satisfy (5); we assume 0 < c < 1 and that the ambient dimension d is large enough for the configuration to exist. A canonical large-d realization is the latent-Gaussian construction xi = √c μ + √1 − c z i with a fixed unit centroid μ ∈ Sd−1 and i.i.d. Gaussian residuals 

zi ⊥ μ of unit variance, for which ∥xi∥2 = 1 and x⊤ 

> i

xj = c + O(d−1/2) concentrate at the equal-angular geometry; the analysis below depends only on (5) and applies to any realization. The MHN energy and update rule are 

E(ξ | X) = − 1 

> β

log  

> N+1

∑

> i=1

eβx ⊤  

> iξ

+ 12 ∥ξ∥2, T (ξ) = ∑

> i

pi(ξ)xi, pi(ξ) = eβx ⊤ 

> iξ

∑ 

> k

eβx ⊤  

> kξ

, (16) so that a fixed point ξ∗ = T (ξ∗) satisfies ξ∗ = Xp ∗ with p∗ = softmax (βX ⊤ξ∗). The rotation 

X2 = V X induces 

E(ξ | V X ) = E(V ⊤ξ | X), (17) reducing Task-2 evaluation at ξ to Task-1 evaluation at V ⊤ξ.

B.2 Symmetry reduction of the fixed-point equations 

Define 

s := 

> N

∑

> i=1

xi, A := 1 + ( N − 1) c. (18) A direct calculation using (5) gives 

x⊤ 

> i

s = A (i ≤ N ), x⊤ 

> o

s = 0 , ∥s∥2 = N A. (19) 

Outlier-like branch. The outlier-like fixed point is invariant under permutations of the cluster members, so its softmax weights are 

pout = ( u, . . . , u, v ), N u + v = 1 , (20) and ξout = u s + v x o. The logits read x⊤ 

> i

ξout = Au for i ≤ N and x⊤ 

> o

ξout = v. Substituting into the softmax, 

u = eβAu 

N e βAu + eβv , v = eβv 

N e βAu + eβv , (21) and taking the log-ratio together with u = (1 − v)/N yields the scalar self-consistency 

log N v 

1 − v = β

(

v − AN (1 − v)

)

. (22) 

Cluster-like branch near x1. Permuting x2, . . . , x N leaves the cluster-like fixed point invariant, so 

pcl = ( a, b, . . . , b, q ), a + ( N − 1) b + q = 1 , (23) and ξcl = a x 1 + b ∑ 

> j≥2

xj + q x o. The three distinct logits are 

z1 = a + ( N − 1) cb, z− = ca + {1 + ( N − 2) c}b, zo = q. (24) 17 The softmax equations and the identity z1 − z− = (1 − c)( a − b) give the two pattern-selection equations 

log ab = β(1 − c)( a − b), log aq = β(z1 − q). (25) The first is a Curie–Weiss-type fixed-point equation with effective inverse temperature 

βeff := β(1 − c), (26) matching the equal-angular capacity scale of Ramsauer et al. [16], Achilli et al. [32]. 

B.3 Existence threshold and localization condition 

We separate two requirements: (i) existence of pattern-specific cluster fixed points, and (ii) the stronger localization condition that keeps the leakage corrections small in the Hessian expansion below. 

B.3.1 Existence threshold 

Drop the outlier coordinate temporarily and consider the symmetric cluster sector pcl = ( a, b, . . . , b )

with a + ( N − 1) b = 1 . Introduce the order parameter 

m := a − b, a = 1 + ( N − 1) mN , b = 1 − mN . (27) The pattern-selection equation (25) becomes 

log 1 + ( N − 1) m

1 − m = βeff m, βeff = β(1 − c). (28) The function on the left is convex in m ∈ (0 , 1) , so (28) admits a non-trivial root iff the line of slope 

βeff touches the convex graph. Tangency at some mc ∈ (0 , 1) defines the critical inverse temperature 

βc(N ) := min 

> m∈(0 ,1)

1

m log 1 + ( N − 1) m

1 − m , (29) characterised by 

log 1 + ( N − 1) mc

1 − mc

= N m c

(1 − mc){1 + ( N − 1) mc} , βc(N ) = N

(1 − mc){1 + ( N − 1) mc} . (30) Equivalently, with tc := {1 + ( N − 1) mc}/(1 − mc),

N t c log tc = ( tc − 1)( tc + N − 1) , βc(N ) = (tc + N − 1) 2

N t c

. (31) The closed form is implicit; the asymptotics is explicit. Elementary bounds on log tc yield 

log N + 1 ≤ βc(N ) ≤ log N + log(1 + log N ) + 1 + log(1 + log N )log N , (32) so βc(N ) = Θ(log N ), and as N → ∞ ,

βc(N ) = log N + log log N + 1 + o(1) . (33) Pattern-specific cluster fixed points therefore exist iff 

β(1 − c) > β c(N ). (34) 18 B.3.2 Leakage and the localization condition 

Existence does not by itself bound the size of corrections in the Hessian expansion below. We use a single auxiliary quantity, the leakage : the total softmax mass assigned to memories other than the dominant one. For the outlier and cluster branches, define 

ηout := 1 − v = N u, ηcl := 1 − a = ( N − 1) b + q. (35) Using s ⊥ xo for the outlier and the explicit form of the cluster fixed point, 

ξout − xo = u s − (1 − v) xo = ηout 

( sN − xo

)

, ξcl − x1 = b

> N

∑

> j=2

(xj − x1) + q(xo − x1), (36) so ∥ξout − xo∥ ≤ Cout ηout and ∥ξcl − x1∥ ≤ Ccl ηcl with c-dependent constants. The condition must control βη , not only η, because the leakage enters the Hessian multiplied by β. Indeed, if 

p∗ 

> k

= 1 − ∑  

> j̸=k

ηj and p∗ 

> j

= ηj , then 

∑

> i

p∗ 

> i

xix⊤ 

> i

− ξ∗ξ⊤∗ = ∑ 

> j̸=k

ηj (xj − xk)( xj − xk)⊤ + O(η2), (37) and hence H∗ = I − β ∑  

> j̸=k

ηj (xj − xk)( xj − xk)⊤ + O(βη 2). Thus the working localization condition is 

β η out ≪ 1, β η cl ≪ 1. (38) The next two paragraphs derive the corresponding outlier and cluster forms of this same condition. 

Outlier branch. Set δout := 1 − v = ηout so u = δout /N . Substituting into (22), 

log N (1 − δout )

δout 

= β

[

1 − (1 + AN

)δout 

]

, (39) or in fixed-point form δout 

1 − δout 

= N e −β exp [β(1 + AN ) δout 

]. (40) If βN e −β ≪ 1, the right-hand side is the contraction N e −β (1 + O(βδ out )) and the leading balance reads δout = N e −β (1 + o(1)) . Hence the outlier form of the localization condition is 

βN e −β ≪ 1, (41) or, for N → ∞ , β − log N − log β → +∞. Under (41), 

v = 1 − N e −β (1 + o(1)) , u = e−β (1 + o(1)) . (42) 

Cluster branch. Equation (25) gives b/a = exp [−β(1 − c)( a − b)] . Writing a = 1 − ηcl and 

a − b = 1 − ηcl − b,

b = a e −β(1 −c) exp [β(1 − c)( ηcl + b)]. (43) Similarly, from log( a/q ) = β(z1 − q) with z1 = a + ( N − 1) cb ,

q = a e −β exp [β{(1 − c)( N − 1) b + 2 q}]. (44) 19 The leading values of these off-memory weights are therefore (N − 1) e−β(1 −c) from the other cluster memories and e−β from the outlier. The exponential feedbacks in (43)–(44) are negligible iff the cluster form of the localization condition holds: 

β(1 − c)( N − 1) e−β(1 −c) ≪ 1, β e −β ≪ 1. (45) Under (45), 

b = e−β(1 −c)(1 + o(1)) , q = e−β (1 + o(1)) ,a = 1 − (N − 1) e−β(1 −c) − e−β + o((N − 1) e−β(1 −c) + e−β ). (46) Equations (41) and (45) are simply the outlier and cluster forms of the same localization condition 

βη ≪ 1. Since e−β(1 −c) ≫ e−β for 0 < c < 1, the binding cluster constraint is β(1 − c)( N −

1) e−β(1 −c) ≪ 1, equivalently β(1 − c) − log N − log β(1 − c) → +∞, i.e. 

β(1 − c) ≥ log N + log log N + ω(1) . (47) 

B.3.3 Working assumption β ≫ log N

The capacity threshold βc(N ) = log N + log log N + 1 + o(1) obtained in (33) is the order at which the pattern-specific phase transition occurs. The boundary scale β ∼ log N is mathematically interesting and has been the focus of single-task associative-memory analyses, which study how patterns emerge as β crosses this threshold. The present paper, however, is about CL — a more involved setting in which we want to study forgetting and replay at a Task-1 fixed point that is already cleanly localized. We therefore simplify by working in the strictly stronger regime 

β ≫ log N, (48) which directly implies the working condition 

β N e −β ≪ 1. (49) For fixed 0 < c < 1, β ≫ log N also implies β(1 − c) ≫ log N , hence β(1 − c)( N − 1) e−β(1 −c) ≪ 1,and we obtain the inclusion chain 

> {

β ≫ log N } ⊂ {β(1 − c) ≥ log N + log log N + ω(1) } ⊂ {β(1 − c) > β c(N )}. (50) The strict inclusions are useful: the outermost set is the existence threshold; the middle set enforces the localization condition; the innermost set is what we adopt here. Under (48), every Task-1 pattern-specific fixed point is localized around its corresponding memory and the Hessian expansion in the leakage variables is valid. 

Assumption B.1 (Working regime) . We work throughout in the regime 0 < c < 1 and β ≫ log N .

B.4 Large-β asymptotics of the Task-1 fixed points 

We collect the closed-form expansions used in the body. 

Proposition B.2 (Outlier-like fixed point) . Under (41), 

v = 1 − N e −β + O(β e −2β ), u = e−β + O(β e −2β ), (51) 

hence 

ξout = xo + e−β (s − N x o) + O(β e −2β ). (52) 20 Proof. Set v = 1 −δ and substitute into (22): log {N (1 −δ)/δ } = β{1−δ−Aδ/N }. As β → ∞ , δ → 0,so the left-hand side equals log (N/δ ) + O(δ) and the leading balance reads log (N/δ ) = β(1 − O(δ)) ,giving δ = N e −β (1 + o(1)) . The next-order correction is O(βδ 2) = O(β e −2β ). Substituting u = δ/N 

into ξout = u s + v x o yields (52). 

Proposition B.3 (Cluster-like fixed point) . Under Condition B.1, 

b = e−β(1 −c)(1+ o(1)) , q = e−β (1+ o(1)) , a = 1 −(N −1) e−β(1 −c) −e−β +o(e−β(1 −c)), (53) 

hence 

ξcl = x1 + e−β(1 −c)( N∑

> j=2

xj − (N − 1) x1

)

+ e−β (xo − x1) + o(e−β(1 −c)). (54) 

Proof. Since a → 1 and b, q → 0 under the localization condition, the first equation of (25) gives 

log (a/b ) = β(1 − c)( a − b) = β(1 − c)(1 + o(1)) , so b = a e −β(1 −c)(1 + o(1)) = e−β(1 −c)(1 + o(1)) .The second gives log (a/q ) = β(a + ( N − 1) cb − q) = β(1 + o(1)) , so q = e−β (1 + o(1)) . The sum rule a + ( N − 1) b + q = 1 closes the argument. Substituting into ξcl = a x 1 + b ∑ 

> j≥2

xj + q x o

yields (54). 

Remark B.4 . The two leading off-memory weights differ exponentially: the outlier sees u ∼ e−β ,while the cluster sees b ∼ e−β(1 −c), and e−β(1 −c)/e −β = eβc → ∞ . Geometrically, the cluster fixed point is exponentially broader than the outlier fixed point ; this is the geometric origin of Theorem 4.1. 

B.5 Hessian expansion for the rotation-averaged energy rise 

This subsection collects the Hessian-related calculations used in the proof: the Taylor expansion at a fixed point, the average over small random rotations, and the first-order expansion in the off-memory weights. 

Taylor expansion at a Task-1 fixed point. Because ξ∗ is stationary ( ∇E(ξ∗ | X) = 0 ), Taylor expansion gives 

∆Efp  

> ∗

(V ) = 12 (V ⊤ξ∗ − ξ∗)⊤H∗(V ⊤ξ∗ − ξ∗) + O(ε3), H∗ := ∇2E(ξ∗ | X). (55) Direct differentiation of (16) yields 

H∗ = I − β

( N +1 ∑

> i=1

p∗ 

> i

xix⊤ 

> i

− ξ∗ξ⊤∗

)

. (56) Using V ⊤ξ∗ − ξ∗ = −εΩξ∗ + O(ε2),

∆Efp  

> ∗

(V ) = ε2 

> 2

(Ω ξ∗)⊤H∗(Ω ξ∗) + O(ε3). (57) The covariance term in Eq. (56) also explains the βη scale used in the localization condition. Near a pattern-specific fixed point with p∗ 

> k

= 1 − ∑  

> j̸=k

ηj and p∗ 

> j

= ηj , it has the expansion 

∑

> i

p∗ 

> i

xix⊤ 

> i

− ξ∗ξ⊤∗ = ∑ 

> j̸=k

ηj (xj − xk)( xj − xk)⊤ + O(η2). (58) Thus leakage contributes to the Hessian at order βη , which is why Eq. (38) controls βη rather than merely η.21 Rotation average. Lemma B.5. For any u ∈ Rd, ⟨(Ω u)(Ω u)⊤⟩V = 12 (∥u∥2I − uu ⊤).Proof. From Ω = 12 (W − W ⊤) with Wij iid 

∼ N (0 , 1) , E[Ω ab Ωcd ] = 12 (δac δbd − δad δbc ). Hence 

⟨(Ω u)i(Ω u)j ⟩V = 12 (δij ∥u∥2 − uiuj ).Substituting into (57) and using the main-text convention ∆Efp  

> ∗

:= ⟨∆Efp  

> ∗

(V )⟩V from (8), 

∆Efp  

> ∗

= ε2 

> 4

(∥ξ∗∥2 tr H∗ − ξ⊤∗ H∗ξ∗) + O(ε3). (59) 

First-order expansion near a pattern. Suppose ξ∗ is concentrated at a single stored pattern 

xk, with weights 

p∗ 

> k

= 1 − ∑ 

> j̸=k

ηj , p∗ 

> j

= ηj ≪ 1, αj := x⊤ 

> j

xk. (60) The fixed-point identity ξ∗ = Xp ∗ then reads 

ξ∗ = xk + ∑ 

> j̸=k

ηj (xj − xk). (61) We compute the three quantities entering (59) step by step, retaining all O(η) terms and dropping 

O(η2).

(i) ∥ξ∗∥2. Using ∥xk∥2 = 1 , x⊤ 

> k

(xj − xk) = αj − 1, and (xi − xk)⊤(xj − xk) = O(1) ,

∥ξ∗∥2 = 1 + 2 ∑ 

> j̸=k

ηj (αj − 1) + O(η2) = 1 − 2 ∑ 

> j̸=k

ηj (1 − αj ) + O(η2). (62) 

(ii) tr H∗. From (56), using ∥xi∥2 = 1 and ∑ 

> i

p∗ 

> i

= 1 ,

tr H∗ = d − β

( N +1 ∑

> i=1

p∗ 

> i

∥xi∥2 − ∥ ξ∗∥2)

= d − β(1 − ∥ ξ∗∥2), (63) and substituting (62), 

tr H∗ = d − 2β ∑ 

> j̸=k

ηj (1 − αj ) + O(η2). (64) 

(iii) ξ⊤∗ H∗ξ∗. From (56), 

ξ⊤∗ H∗ξ∗ = ∥ξ∗∥2 − β

( ∑ 

> i

p∗ 

> i

(x⊤ 

> i

ξ∗)2 − ∥ ξ∗∥4)

. (65) Using (61), x⊤ 

> k

ξ∗ = 1 − ∑  

> j̸=k

ηj (1 − αj ), and x⊤ 

> j

ξ∗ = αj + O(η) for j̸ = k. Therefore 

∑

> i

p∗ 

> i

(x⊤ 

> i

ξ∗)2 =

(

1 − ∑ 

> j̸=k

ηj

)( 

1 − ∑ 

> j̸=k

ηj (1 − αj )

)2

+ ∑ 

> j̸=k

ηj α2 

> j

+ O(η2)= 1 − 2 ∑ 

> j̸=k

ηj (1 − αj ) − ∑ 

> j̸=k

ηj (1 − α2 

> j

) + O(η2),

(66) where in the last step we used − ∑ 

> j

ηj + ∑ 

> j

ηj α2 

> j

= − ∑ 

> j

ηj (1 − α2 

> j

) and absorbed cross-products of η’s into O(η2). Combining (66) with ∥ξ∗∥4 = 1 − 4 ∑  

> j̸=k

ηj (1 − αj ) + O(η2) from (62) gives 

∑

> i

p∗ 

> i

(x⊤ 

> i

ξ∗)2 − ∥ ξ∗∥4 = ∑ 

> j̸=k

ηj

{2(1 − αj ) − (1 − α2 

> j

)} + O(η2) = ∑ 

> j̸=k

ηj (1 − αj )2 + O(η2), (67) 22 using the algebraic identity 2(1 − α) − (1 − α2) = (1 − α)2. Substituting (67) into (65), 

ξ⊤∗ H∗ξ∗ = 1 − 2 ∑ 

> j̸=k

ηj (1 − αj ) − β ∑ 

> j̸=k

ηj (1 − αj )2 + O(η2). (68) Substituting (62), (64), and (68) into (59), at order η,

∥ξ∗∥2 tr H∗ − ξ⊤∗ H∗ξ∗ = ( d − 1) − 2( β + d − 1) ∑ 

> j̸=k

ηj (1 − αj ) + β ∑ 

> j̸=k

ηj (1 − αj )2 + O(η2)= ( d − 1) − ∑ 

> j̸=k

ηj

{

2( d − 1)(1 − αj ) + β(1 − αj )[2 − (1 − αj )]}

+ O(η2)= ( d − 1) − ∑ 

> j̸=k

ηj

{2( d − 1)(1 − αj ) + β(1 − α2 

> j

)} + O(η2), (69) where the last line uses the identity (1 − α) [2 − (1 − α)] = (1 − α)(1 + α) = 1 − α2. Therefore 

∆Efp  

> ∗

= ε2

> 4

[

(d − 1) − ∑ 

> j̸=k

ηj Λ( αj )

]

+ O(ε2η2 + ε3), Λ( α) := 2( d − 1)(1 − α) + β(1 − α2). (70) Each competitor at cosine αj shaves ηj Λ( αj ) off the bare (d − 1) . The geometric weight Λ( α)

vanishes at α = 1 (perfect alignment, no help) and is maximal at α = 0 (orthogonal competitor, maximum reduction). 

B.6 Specialization and proof of Theorem 4.1 

This subsection applies the Hessian formula to the two Task-1 branches and then compares the resulting energy rises. 

Outlier fixed point. For the outlier-like fixed point all N competitors are orthogonal: αj = 0 

and ηj = u = e−β (1 + o(1)) by Proposition B.2. Substituting into (70), 

∆Efp  

> o

= ε2

> 4

[(d − 1) − N e −β {2( d − 1) + β}] + O(ε2e−2β + ε3). (71) 

Cluster fixed point. For the cluster-like fixed point near x1 there are two competitor types (Proposition B.3): the other N − 1 cluster members with α = c, η = b = e−β(1 −c)(1 + o(1)) , and the outlier with α = 0 , η = q = e−β (1 + o(1)) . Substituting into (70), 

∆Efp cl = ε2

> 4

[

(d − 1) − (N − 1) e−β(1 −c){2( d − 1)(1 − c) + β(1 − c2)}− e−β {2( d − 1) + β}

]

+ O(ε2 e−2β(1 −c) + ε3).

(72) 

Comparison. Subtracting (72) from (71) with Λ( α) = 2( d − 1)(1 − α) + β(1 − α2),

∆Efp  

> o

− ∆Efp cl = ε2 

> 4

(N − 1) [e−β(1 −c) Λ( c) − e−β Λ(0) ] + o(ε2 e−β(1 −c)). (73) For 0 < c < 1 and β ≫ log N we have e−β(1 −c)/e −β = eβc → ∞ , while Λ( c) = 2( d − 1)(1 − c) + 

β(1 − c2) > 0. Hence the right-hand side is strictly positive at leading order, proving Theorem 4.1. □

Remark B.6 (Edge cases) . If c = 0 , the cluster degenerates into N mutually orthogonal points and both branches collapse: e−β(1 −c) = e−β , the bracket in (73) vanishes, and the inequality becomes an equality at leading order. If c < 0, the cluster members are anti-correlated, Λ( c) may become negative, and the sign of (73) can reverse. Hence the inequality is genuine to the cluster regime 

0 < c < 1.23 C Energy–Sharpness Relation 

In the main text, we describe isolated memories as forming high-energy and sharp basins, and cluster-supported memories as forming low-energy and broad basins. This appendix makes that statement precise in the equal-angular cluster-plus-outlier model. The key point is that both the local energy level and the local sharpness of a fixed point are controlled by the same off-memory softmax weights. 

C.1 Definition of local sharpness 

Let ξ∗ be a Task-1 fixed point of the Hopfield energy 

E(ξ | X) = − 1

β log ∑

> i

exp( βx ⊤ 

> i

ξ) + 12 ∥ξ∥2.

The Hessian at ξ∗ is 

H∗ = ∇2 

> ξ

E(ξ∗ | X) = I − β

> (∑
> i

p∗ 

> i

xix⊤ 

> i

− ξ∗ξ⊤∗

> )

,

where p∗ 

> i

= pi(ξ∗ | X). We define the local sharpness of the basin by the average Hessian trace in random tangential directions induced by an infinitesimal rotation, 

S∗ := ∥ξ∗∥2 tr H∗ − ξ⊤∗ H∗ξ∗. (74) Indeed, for V = exp( εΩ) with Ω = ( W − W ⊤)/2 and Wij ∼ N (0 , 1) , one has 

EΩ

> [

(Ω ξ∗)⊤H∗(Ω ξ∗)

> ]

= 12

> (

∥ξ∗∥2 tr H∗ − ξ⊤∗ H∗ξ∗

> )

= 12 S∗.

Thus S∗ is the curvature factor that appears in the rotation-induced energy increase: 

EΩ

> [

E(V ⊤ξ∗ | X) − E(ξ∗ | X)

> ]

= ε2

4 S∗ + O(ε3).

Larger S∗ means that the basin rises more rapidly under small displacements, and we therefore use it as the local sharpness of the Hopfield basin. 

C.2 First-order expansion of energy and sharpness 

Consider a fixed point concentrated near a stored memory xk. Write the softmax weights as 

p∗ 

> k

= 1 − ∑ 

> j̸=k

ηj , p∗ 

> j

= ηj , ηj ≪ 1,

and denote the cosine similarity between the competitor xj and the dominant memory xk by 

αj := x⊤ 

> j

xk.

The fixed-point condition gives 

ξ∗ = xk + ∑ 

> j̸=k

ηj (xj − xk) + O(η2).

24 First, the energy at the fixed point has the expansion 

E(ξ∗ | X) = − 12 − 1

β

∑ 

> j̸=k

ηj + O(η2). (75) Thus a fixed point supported by more nearby memories has lower energy. In this sense, the total leakage ∑  

> j̸=k

ηj measures how much additional support the dominant memory receives from neighboring memories. Second, the sharpness defined in Eq. (74) satisfies 

S∗ = ( d − 1) − ∑ 

> j̸=k

ηj Λ( αj ) + O(η2), (76) where 

Λ( α) := 2( d − 1)(1 − α) + β(1 − α2). (77) Since Λ( α) > 0 for α < 1, leakage from neighboring memories lowers the local curvature. Therefore the same off-memory softmax mass that lowers the energy also flattens the basin. This is the precise local sense in which low-energy basins are broad, whereas high-energy basins are sharp. 

C.3 Cluster-plus-outlier specialization 

We now apply Eqs. (75) and (76) to the equal-angular configuration of Section 4.1. For the outlier fixed point, all N competitors are orthogonal, so α = 0 and η = e−β (1 + o(1)) . Therefore, 

E∗ 

> o

= − 12 − Nβ e−β + o(e−β /β ), (78) and 

So = ( d − 1) − N e −β Λ(0) + o(e−β ). (79) For the cluster fixed point near x1, the N − 1 other cluster memories have cosine c and leakage 

e−β(1 −c), while the outlier has cosine 0 and leakage e−β . Thus, 

E∗ 

> cl

= − 12 − 1

β

{

(N − 1) e−β(1 −c) + e−β }

+ o(e−β(1 −c)/β ), (80) and 

Scl = ( d − 1) − (N − 1) e−β(1 −c)Λ( c) − e−β Λ(0) + o(e−β(1 −c)). (81) Subtracting Eq. (80) from Eq. (78) gives 

E∗ 

> o

− E∗ 

> cl

= N − 1

β

(

e−β(1 −c) − e−β )

+ o(e−β(1 −c)/β ) > 0, (82) because 0 < c < 1 implies e−β(1 −c) ≫ e−β at large β. Hence the outlier fixed point has higher Hopfield energy than the cluster fixed point. Likewise, subtracting Eq. (81) from Eq. (79) gives 

So − S cl = ( N − 1) 

[

e−β(1 −c)Λ( c) − e−β Λ(0) 

]

+ o(e−β(1 −c)) > 0. (83) Thus the same outlier fixed point is also sharper. Equations (82) and (83) justify the shorthand used in the main text: in this model, high-energy memories correspond to sharp isolated basins, whereas low-energy memories correspond to broad cluster-supported basins. 25 (a) Energy vs. perturbation sharpness.  

> (b) Perturbation sharpness vs. reconstruction forgetting.

Figure S.1: Perturbation-based sharpness in the diffusion-model experiment on CIFAR-10. (a) Higher-Hopfield-energy samples tend to have larger perturbation sharpness ̂ St(x). (b) Samples with larger perturbation sharpness tend to have larger reconstruction-based forgetting Ft⋆ (x) after Task-2 training. 

C.4 Sharpness in diffusion-model experiments 

We further examine the relation between Hopfield energy, local sharpness, and reconstruction-based forgetting in the diffusion-model experiments. Here we focus on CIFAR-10 and compute all quantities sample-wise for Task-1 images. For each Task-1 sample x, we first compute its Hopfield energy with respect to the Task-1 reference set D1,

Et(x | D 1),

using the same energy computation as in the main diffusion-model experiments. To quantify local sharpness, we do not explicitly form the Hessian. Instead, we use a perturbation-based finite-difference estimate: for each sample x, we draw M small random perturbations {δm}Mm=1 and measure how much the Hopfield energy increases around x,̂

St(x) = 1

M

> M∑
> m=1

[Et(x + δm | D 1) − Et(x | D 1)] , (84) up to the fixed perturbation-scale normalization used in the implementation. In the experiments, we use M = 100 random perturbations for each sample. Thus, ̂ St(x) measures how steeply the Hopfield energy changes under local random perturbations: larger values indicate that the sample lies in a sharper local region of the energy landscape. Figure S.1 summarizes the result. First, we compare the perturbation-based sharpness ̂ St(x) with the Hopfield energy Et(x | D 1). Higher-energy samples tend to have larger perturbation sharpness, consistent with the theoretical picture that weakly supported samples lie in sharper regions of the landscape. Second, we compare ̂ St(x) with the reconstruction-based forgetting metric Ft⋆ (x) defined in Eq. (12) . Samples with larger perturbation sharpness also tend to show larger reconstruction error after Task-2 training. These results suggest that the energy-based ordering observed in the main text is accompanied by a corresponding difference in local sharpness, and that this difference is reflected in reconstruction behavior after continual learning. 26 D Full derivation of Theorem 5.1 

We derive the exact replay identity, isolate the duplicate-memory baseline, expand the baseline-subtracted gain to leading order in the small rotation, evaluate the five canonical gains, and prove the ordering of Theorem 5.1. 

D.1 Exact replay identity 

Let I = {1, . . . , N, o }, X = {xμ : μ ∈ I} , and X2(V ) = V X . When a replay point xr ∈ X is added, write X+r 

> 2

(V ) := X2(V ) ∪ { xr}, with the replayed sample counted as an additional term in the log-sum-exp. For a Task-1 fixed point ξ∗ 

> k

, the energy rise without replay was already defined in Section B.5 as 

∆Efp  

> k

(V ) = E(ξ∗ 

> k

| V X ) − E(ξ∗ 

> k

| X), ∆Efp  

> k

(I) = 0 . (85) We additionally need the Task-1 softmax weight of the replay point at ξ∗ 

> k

,

pr|k := eβx ⊤ 

> rξ∗
> k

∑ 

> μ∈I

eβx ⊤ 

> μξ∗
> k

. (86) The raw replay-induced energy decrease is 

Rr→k(V ) := E(ξ∗ 

> k

| X2(V )) − E(ξ∗ 

> k

| X+r 

> 2

(V )) . (87) Using the log-sum-exp form (16) of E, the difference of the two log-sum-exp’s is 

Rr→k(V ) = 1 

> β

log 

(

1 + eβx ⊤ 

> rξ∗
> k

∑ 

> μ∈I

eβ(V x μ)⊤ξ∗

> k

)

. (88) Now observe that, by the definition (85) and the log-sum-exp form, 

eβ∆Efp    

> k(V)

=

∑ 

> μ

eβx ⊤ 

> μξ∗
> k

∑ 

> μ

eβ(V x μ)⊤ξ∗

> k

. (89) Multiplying numerator and denominator inside (88) by eβ∆Efp    

> k(V)

converts the rotated denominator back into the Task-1 partition function: 

eβx ⊤ 

> rξ∗
> k

∑ 

> μ

eβ(V x μ)⊤ξ∗

> k

= eβx ⊤  

> rξ∗
> k

eβ∆Efp   

> k(V)

∑ 

> μ

eβx ⊤ 

> μξ∗
> k

= pr|k eβ∆Efp   

> k(V)

. (90) Hence the exact identity 

Rr→k(V ) = 1 

> β

log (1 + pr|k eβ∆Efp   

> k(V)

). (91) At V = I, ∆Efp  

> k

(I) = 0 and (91) reduces to the trivial duplicate-memory baseline 

Rr→k(I) = 1 

> β

log(1 + pr|k), (92) which is the energy decrease one obtains merely from replicating an already-stored Task-1 memory. Subtracting this baseline and using log( a) − log( b) = log( a/b ),̂

∆r→k(V ) := Rr→k(V ) − R r→k(I) = 1 

> β

log 1 + pr|k eβ∆Efp   

> k(V)

1 + pr|k

. (93) This separates the two effects: pr|k measures the relevance of the replay point (geometry), and 

∆Efp  

> k

(V ) measures how much the rotated Task-2 dataset raises the energy at ξ∗ 

> k

(drift) — exactly the same object that drives Theorem 4.1. 27 D.2 Small-rotation expansion and master formula 

Define 

ρ(p) := p

1 + p . (94) This factor determines the response of forgetting mitigation to the energy rise, because 

∂∂∆E

{ 1

β log 1 + p e β∆E

1 + p

}∣ ∣∣∣∣∆E=0 

= ρ(p). (95) We therefore call ρ(p) the replay susceptibility . This name refers to the response to an energy change in Eq. (95). Taylor-expanding (93) in ∆Efp  

> k

(V ) = O(ε2),̂

∆r→k(V ) = ρ(pr|k) ∆ Efp  

> k

(V ) + β 

> 2

ρ(pr|k)(1 − ρ(pr|k)) ∆ Efp  

> k

(V )2 + O(β2ρ(pr|k) |∆Efp  

> k

(V )|3). (96) Averaging over V and setting ∆r→k := ⟨̂∆r→k(V )⟩V , and recalling ∆Efp  

> k

:= ⟨∆Efp  

> k

(V )⟩V from (8), 

∆r→k = ρ(pr|k) ∆ Efp  

> k

+ O(βρ (pr|k)ε4). (97) The factor ∆Efp  

> k

is exactly the rotation-averaged energy rise computed in Section B.6 (Eqs. (71),(72)). 

D.3 Susceptibility asymptotics 

From Appendix B, 

v = po|o = 1 − N e −β + O(βe −2β ),a = p1|1 = 1 − (N − 1) e−β(1 −c) − e−β + o(e−β(1 −c)),b = p1|2 = e−β(1 −c)(1 + o(1)) , u = p1|o = e−β (1 + o(1)) , q = po|1 = e−β (1 + o(1)) .

Applying ρ(p) = p/ (1 + p),

ρ(v) = 12 − N e −β 

> 4

+ O(β e −2β ),ρ(a) = 12 − (N −1) e−β(1 −c)+e−β 

> 4

+ o(e−β(1 −c)),ρ(b) = e−β(1 −c)(1 + o(1)) , ρ(u) = e−β (1 + o(1)) , ρ(q) = e−β (1 + o(1)) .

(98) Hence 

ρ(v) > ρ (a) ≫ ρ(b) ≫ ρ(u) ≍ ρ(q).

D.4 Five replay gains 

Recall from (71)–(72), with Λ( α) = 2( d − 1)(1 − α) + β(1 − α2),

∆Efp  

> o

= ε2

> 4

[(d − 1) − N e −β Λ(0) ] + O(ε2 e−2β + ε3), (99) 

∆Efp cl = ε2

> 4

[(d − 1) − (N − 1) e−β(1 −c) Λ( c) − e−β Λ(0) ] + O(ε2 e−2β(1 −c) + ε3), (100) 

∆Efp  

> o

− ∆Efp cl = ε2 

> 4

(N − 1) [e−β(1 −c) Λ( c) − e−β Λ(0) ] + o(ε2 e−β(1 −c)) > 0. (101) 28 Multiplying (98) by ∆Efp  

> o

or ∆Efp cl via the master formula (97) yields 

∆o→o = 12 ∆Efp  

> o

(1 + o(1)) = ε2 

> 8

(d − 1) + O(ε2e−β + ε3 + βε 4), (102) 

∆cl 1→cl 1 = 12 ∆Efp cl (1 + o(1)) = ε2 

> 8

(d − 1) + O(ε2e−β(1 −c) + ε3 + βε 4), (103) 

∆cl 1→cl 2 = e−β(1 −c) ∆Efp cl (1 + o(1)) = ε2 

> 4

(d − 1) e−β(1 −c)(1 + o(1)) , (104) 

∆cl 1→o = e−β ∆Efp  

> o

(1 + o(1)) = ε2 

> 4

(d − 1) e−β (1 + o(1)) , (105) 

∆o→cl 1 = e−β ∆Efp cl (1 + o(1)) = ε2 

> 4

(d − 1) e−β (1 + o(1)) . (106) 

D.5 Ordering 

1. ∆o→o > ∆cl 1→cl 1 .

∆o→o − ∆cl 1→cl 1 = {ρ(v) − ρ(a)}∆Efp cl + ρ(v){∆Efp  

> o

− ∆Efp cl } + o(ε2e−β(1 −c)).

From (98), ρ(v) − ρ(a) = (N −1)( e−β(1 −c)−e−β )4 + o(e−β(1 −c)) > 0, and Theorem 4.1 gives 

∆Efp  

> o

> ∆Efp cl > 0. Thus ∆o→o > ∆cl 1→cl 1 .2. ∆cl 1→cl 1 ≫ ∆cl 1→cl 2 .

∆cl 1→cl 2

∆cl 1→cl 1

= ρ(b)

ρ(a) (1 + o(1)) = 2 e−β(1 −c)(1 + o(1)) → 0.

3. ∆cl 1→cl 2 ≫ ∆cl 1→o.

∆cl 1→o

∆cl 1→cl 2

= ρ(u)

ρ(b)∆Efp 

> o

∆Efp cl 

= e−βc (1 + o(1)) → 0.

4. ∆cl 1→cl 2 ≫ ∆o→cl 1 .

∆o→cl 1

∆cl 1→cl 2

= ρ(q)

ρ(b) = e−βc (1 + o(1)) → 0.

Moreover, 

∆o→cl 1

∆cl 1→o

= ρ(q)

ρ(u)∆Efp cl 

∆Efp 

> o

= 1 + o(1) ,

so ∆o→cl 1 and ∆cl 1→o have the same exponential order. Combining the four comparisons gives 

∆o→o > ∆cl 1→cl 1 ≫ ∆cl 1→cl 2 ≫ { ∆cl 1→o, ∆o→cl 1 }, ∆cl 1→o ≍ ∆o→cl 1 ,

which proves Theorem 5.1. □

Remark D.1 . The subtraction of Rr→k(I) = 1 

> β

log (1 + pr|k) removes the trivial gain from duplicating an already-stored memory. What is left is the fraction ρ(pr|k) = pr|k/(1+ pr|k) of the rotation-induced rise ∆Efp  

> k

(V ) that the replay point can recover. Self-replay ( p → 1) recovers 12 ; within-cluster cross-replay recovers e−β(1 −c); cross-type replay between cluster and outlier recovers e−β . The ordering then follows from 12 ≫ e−β(1 −c) ≫ e−β and ∆Efp  

> o

> ∆Efp cl .29 Figure S.2: Closed-form MHN corroboration of Theorem 4.1. The outlier fixed point exhibits a larger rotation-induced energy rise than the cluster fixed points. 

# E Synthetic experiments on MHNs 

E.1 Intrinsic forgetting in synthetic MHNs 

To complement the theory in Section 4.1, we instantiate the equal-angular MHN at moderate finite values ( N = 12 , d = 50 , c = 0 .35 , β = 8 , ε = 0 .02 , 2000 random rotations) – well outside the asymptotic regime β ≫ log N , ε ≪ 1 used in the proof – and compute ∆Efp in closed form. Even in this finite-parameter regime the outlier fixed point has a strictly larger rotation-induced energy rise than the cluster fixed points, matching the qualitative ordering predicted by Theorem 4.1. The corresponding learned MHN-BM result is shown in the main text in Fig. 3a. 

E.2 Replay ordering in synthetic MHNs 

For completeness, we also verify Theorem 5.1 in the exact equal-angular regime it was proved in. In the closed-form MHN, the full replay-gain {∆r→k}r,k shows the predicted ordering: the outlier self-entry ∆o→o is largest, cluster self-gains ∆cl 1→cl 1 are next, within-cluster cross entries ∆cl 1→cl 2

are substantially smaller, and cross-type entries ∆cl 1→o, ∆o→cl 1 are smallest. The corresponding learned MHN-BM corroboration is shown in the main text in Fig. 4. 

# F Domain-incremental learning experiment: Rotated MNIST 

The theoretical analysis of Sections 4.1 and 5.1 models continual learning as a small rotation V of the dataset, which corresponds most directly to a domain-incremental setting in which the same content is presented under a perturbation of the input distribution. The main-text experiments focus on the more widely used incremental-class-learning benchmark with split CIFAR-10. To complement them, we report here the corresponding domain-incremental experiment on rotated 30 Figure S.3: Closed-form MHN corroboration of the replay ordering in Theorem 5.1. The replay-gain reproduces the ranking ∆o→o > ∆cl 1→cl 1 ≫ ∆cl 1→cl 2 ≫ { ∆cl 1→o, ∆o→cl 1 }.MNIST – the setup that most literally matches the rotated-dataset assumption underlying our theorems. 

Setup. Task 1 is standard MNIST. Task 2 is the same MNIST images rotated by 30 ◦, instantiating the rotated-dataset construction of Section 4.1 on real images. We train a pixel-space DDPM from scratch on Task 1 and continue training on Task 2. After Task 1 we assign every Task-1 image a scalar Hopfield energy Eθ(x) via the log-sum-exp expression (1), using all Task-1 training images as stored memories. As in the main-text diffusion-model experiments, forgetting is measured by the per-sample reconstruction MSE Ft⋆ (x) defined in Eq. (12). For the replay analysis we use the same three policies as in Section 5.3 – no replay (baseline), energy-guided top-K (high-energy buffer), and energy-guided bottom-K (low-energy buffer) – with an identical buffer size K = 5000 . Full hyperparameters are listed in Appendix H. 

Forgetting indicator (Fig. S.4). After Task 2 fine-tuning, per-sample reconstruction MSE is a monotone-increasing function of the Task-1 Hopfield energy, reproducing the same qualitative relationship observed for split CIFAR-10 in Fig. 3b and Fig. 3c. Higher-energy Task-1 images suffer larger reconstruction forgetting, exactly as predicted by Theorem 4.1. 

Energy-guided replay (Fig. S.5). The mean–range tradeoff predicted by Eq. (14) – ∆r→k ∼

ρr|k ∆Efp  

> k

– is reproduced on rotated MNIST. The high-energy buffer (Fig. S.5a) yields a sharply peaked bin-mean reduction concentrated in the high-energy tail (Fig. S.5c), whereas the low-energy buffer (Fig. S.5b) gives a smaller per-sample reduction spread over a wider low-energy range. The bin-summed reduction (Fig. S.5d) is dominated by the high-energy tail under the high-energy buffer, exactly as in the CIFAR-10 results (Fig. 5, 6). Together with the main-text experiments, this confirms that the energy-guided replay rule predicted by Theorem 5.1 is robust across both incremental-class and domain-incremental continual learning of diffusion models. 

# G Reconstruction metric for diffusion-model forgetting 

The theory in Section 4 defines forgetting as an intrinsic change in the Hopfield energy, 

∆E(x) = E(x | X new ) − E(x | X old ).

31 Figure S.4: Rotated MNIST (domain-incremental learning): per-sample reconstruction MSE Ft⋆ (x)

after Task 2 training versus internal Hopfield energy measured on Task 1, without replay. The same monotone energy–forgetting relation observed for split CIFAR-10 in Fig. 3 is recovered.    

> (a) High-energy replay. (b) Low-energy replay. (c) Mean Reduction. (d) Sum Reduction.

Figure S.5: Energy-guided replay on rotated MNIST (domain-incremental learning). The same pattern as Fig. 5 and Fig. 6 is reproduced: (a) high-energy replay produces a sharply peaked, narrow rescue at high energy, (b) low-energy replay produces a smaller but broader rescue at low energy, and (c,d) the per-bin reductions follow the theoretical replay ordering in Eq. (15). The replay benefit is again dominated by the high-energy tail. For an explicit modern Hopfield network this quantity can be evaluated directly. For a trained diffusion model, however, the corresponding implicit energy is not available as a normalized scalar function for each sample and each training stage. Although the score determines the local reverse dynamics, measuring the exact sample-wise energy increase before and after Task-2 training would require reconstructing or integrating the implicit energy landscape itself. We therefore use a reconstruction-based quantity as an operational proxy for intrinsic forgetting. For each Task-1 image x, we first corrupt it to a fixed diffusion timestep t⋆ and then run the reverse process of the model obtained after Task-2 training. Let ˆx0,t ⋆ denote the resulting reconstruction. We define the reconstruction forgetting score as 

Ft⋆ (x) = ∥x − ˆx0,t ⋆ ∥22.

In Stable Diffusion experiments, the image is first encoded into the VAE latent space, noise is added to the latent, the UNet denoises the latent trajectory, and the final latent is decoded back to pixel space. We report the pixel-space reconstruction MSE in the main figures. In the pixel-space diffusion experiments, the same procedure is performed directly in pixel space. When multiple stochastic reconstructions are used, we average the MSE over generations. 32 This metric should be interpreted as a proxy rather than an exact estimator of ∆E(x). If Task-2 training raises or deforms the implicit basin supporting a Task-1 sample, then a noisy version of that sample is less likely to be mapped back to the original point by the post-Task-2 reverse dynamics. This leads to a larger reconstruction error. Conversely, samples that remain well supported by the post-Task-2 landscape should remain easier to reconstruct. Thus, while Ft⋆ (x) is not itself an energy difference, it probes the same local loss of support that intrinsic forgetting is intended to capture. 

# H Hyperparameters 

Synthetic MHN experiment. N = 12 cluster memories, d = 50 , cosine c = 0 .35 , inverse temperature β = 8 , rotation size ε = 0 .02 , nrot = 2000 random antisymmetric Ω, fixed-point tolerance 10 −13 (scripts/mhn_equal_angler_replay.py ). 

Synthetic MHN-BM experiment. N = 12 cluster memories, d = 50 , c = 0 .35 , β = 16 

(consistent in loss, update, and energy), data scale 2.0, rotation εrot = 0 .02 , nhidden = N + 1 , Adam with learning rate 0.02 , batch size 64 .

Stable Diffusion split CIFAR-10. Backbone: runwayml/stable-diffusion-v1-5 ; only the UNet is fine-tuned, VAE frozen. Task 1 classes: bird, cat, deer, dog, horse. Task 2 classes: airplane, automobile, frog, ship, truck. 25,000 samples per task, 32 ×32 images. Replay buffer K = 5000 ;replay selection ∈ { no replay, random, high_energy }. Forgetting metric: reconstruction MSE at 

t = 0 .8.

Pixel-space diffusion split CIFAR-10. PixelCNN-UNet trained from scratch in pixel space (32 × 32 RGB), “standard” channels (128 , 256 , 512) (∼45M params). Same living/non-living split as the Stable Diffusion setup, 25 ,000 samples per task. AdamW (lr 2 × 10 −4, weight decay 

10 −4, 500 warmup steps), fp16, batch 512 , 2000 epochs/task, EMA decay 0.9999 . Linear noise schedule ( T = 1000 , β ∈ [10 −4, 2 × 10 −2]), 50 inference steps. Replay buffer K = 5000 ; replay selection ∈ { no replay, random, high_energy, low_energy }. Forgetting metric: reconstruction MSE at t = 400 .

Pixel-space diffusion rotated MNIST. Same PixelCNN-UNet adapted to 28 × 28 grayscale. Two tasks defined by rotation_angles = [0 ◦, 30 ◦]. Optimizer, EMA, and noise schedule identical to the pixel-space CIFAR-10 setup; batch 256 for 200 epochs/task, no augmentation. Replay buffer 

K = 5000 ; replay selection ∈ { no replay, random, high_energy, low_energy }. Forgetting metric: reconstruction MSE at t = 400 .33
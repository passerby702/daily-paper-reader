---
title: "A Tutorial on Diffusion Theory: From Differential Equations to Diffusion Models"
title_zh: 扩散理论教程：从微分方程到扩散模型
authors: "Jiayi Fu, Yuxia Wang"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22586v3"
tags: ["query:diff-gen"]
score: 9.0
evidence: 从微分方程视角统一介绍扩散模型的教程
tldr: 扩散模型已成为生成建模的主导框架，但其数学基础常被分散为扩散概率模型、得分建模和随机微分方程等独立视角。本教程以微分方程为统一视角，从条件高斯加噪过程推导常微分方程和随机微分方程表示，进而得到逆向时间SDE和概率流ODE实现生成。通过边际得分和噪声预测参数化，阐明得分匹配如何成为标准去噪目标，并统一了DDPM、DDIM、流匹配与基于得分的SDE。该工作为扩散过程分析与现代生成算法搭建桥梁，并扩展至连续空间语言模型与离散掩码扩散。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22586v3/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1691, \"height\": 311, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22586v3/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1700, \"height\": 642, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22586v3/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1711, \"height\": 586, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22586v3/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1548, \"height\": 665, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22586v3/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1697, \"height\": 979, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.22586v3/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1490, \"height\": 434, \"label\": \"Table\"}]"
motivation: 扩散模型虽为主流生成框架，其数学基础常被拆分为扩散概率模型、得分建模和SDE等独立视角，亟需统一自包含的教程。
method: 从条件高斯加噪过程出发，推导正向和逆向SDE及概率流ODE表示，使生成成为可能。
result: 通过边际得分和噪声预测参数化，统一DDPM、DDIM、流匹配和得分SDE，阐明得分匹配为去噪目标，并讨论实际采样与引导。
conclusion: 为扩散过程理论与现代生成算法搭建桥梁，涵盖连续嵌入空间扩散语言模型及离散掩码扩散。
---

## 摘要
扩散模型已成为生成建模的主导框架，但其数学基础常通过扩散概率模型、基于分数的建模、随机微分方程和数值采样方法分别阐述。我们撰写本教程，旨在从微分方程的视角，对这些观点进行统一且自包含的阐述。从一个条件高斯加噪过程出发，我们推导了常微分方程（ODE）和随机微分方程（SDE）的表示形式，过渡到相应的边际前向动态，进而得到使生成成为可能的逆向时间 SDE 和概率流 ODE。我们表明，逆向采样中的核心未知量是边际得分，解释了在噪声预测参数化下得分匹配如何成为标准的去噪目标，并讨论了实用的逆向时间采样方法和引导技术。我们进一步将 DDPM、DDIM、流匹配和基于分数的 SDE 置于一个统一框架中，最后讨论了连续嵌入空间中的扩散语言模型，并简要探讨了离散掩码令牌扩散。本教程旨在充当扩散过程的分析基础与基于其构建的现代生成算法之间的桥梁。

## Abstract
Diffusion models have emerged as a dominant framework for generative modeling, but their mathematical foundations are often presented separately through diffusion probabilistic models, score-based modeling, stochastic differential equations, and numerical sampling methods. We write this tutorial to provide a unified and self-contained account of these viewpoints from the perspective of differential equations. Starting from a conditional Gaussian noising process, we derive ordinary differential equation (ODE) and stochastic differential equation (SDE) representations, pass to the corresponding marginal forward dynamics, and then obtain the reverse-time SDE and probability-flow ODE that make generation possible. We show that the central unknown quantity in reverse sampling is the marginal score, explain how score matching becomes the standard denoising objective under a noise-prediction parameterization, and discuss practical reverse-time sampling and guidance. We further place DDPM, DDIM, flow matching, and score-based SDEs in a common framework, and conclude with diffusion language models in continuous embedding space together with a brief discussion of discrete masked-token diffusion. The tutorial is intended as a bridge between the analytical foundations of diffusion processes and the modern generative algorithms built upon them.

---

## 论文详细总结（自动生成）

# 扩散理论教程：从微分方程到扩散模型 - 详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：扩散模型虽已成为生成建模的主流框架，但其数学基础常被分割成独立且碎片化的视角进行阐述，如扩散概率模型、得分建模、随机微分方程（SDE）以及相关的数值采样技术。这种碎片化给全面理解扩散模型带来了困难。

- **研究动机**：本文旨在提供一个**统一、自包含的教程**，从**微分方程**的视角，将这些分散的数学要素整合为一个连贯的叙事。它致力于弥合扩散过程的严谨数学理论与现代生成算法之间的鸿沟。

- **整体含义**：论文不是提出新方法，而是构建一座“桥梁”，通过系统推导，展示DDPM、DDIM、流匹配（Flow Matching）和基于分数的SDE等不同形式之间的内在数学联系。它服务于希望深入理解扩散模型数学本质的研究者和实践者。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：以**连续时间扩散过程**为主线，统一解释各种扩散模型。核心逻辑是从一个**条件高斯前向过程**出发，推导其ODE和SDE表示，然后进行**边际化**以模拟数据分布，接着研究**逆向时间动态**，并阐明逆向采样中的核心未知量是**边际得分函数**，最终通过**得分匹配**（转化为噪声预测）来学习该得分。

- **关键技术流程与推导逻辑**:
  - **前向过程**: 定义条件高斯路径 `Xt = αtx0 + σtε`，并推导出生成该路径的**条件前向ODE**和**条件前向SDE**。
  - **边际化过程**: 对数据 `x0` 积分，得到边际密度 `pt(x)`。相应地，边际前向ODE的速度场为条件速度场的条件期望 `ut(x) = E[ut(x|X0) | Xt=x]`，它可写成 `ftx - 1/2 * g_t^2 * ∇log pt(x)`。边际前向SDE即为标准形式 `dXt = ft Xt dt + gt dWt`。
  - **逆向过程与动态**:
    - 定义逆向过程 `Yτ = X1-τ`。
    - 得到**逆向SDE**和**逆向概率流ODE**。两者均包含未知的**边际得分 `∇log pt(x)`**。
  - **学习得分**:
    - **得分匹配目标**: 学习一个神经网络 `sθ(x, t)` 来逼近 `∇log pt(x)`。
    - **关键转换**: 利用高斯过程的性质，得分和目标可重参数化为**噪声预测**：`ε*(x, t) = -σt ∇log pt(x) = E[ε | Xt=x]`。这导出了标准的**去噪损失函数** `L(θ) = 1/2 ∫ ω(t) E[||εθ(αt X0 + σt ε, t) - ε||^2] dt`。
  - **逆向采样**: 用学习到的噪声预测器 `εθ` 替代得分，代入逆向SDE和ODE，即可进行采样。
  - **引导扩散**: 阐述了**分类器引导**（利用分类器梯度修正得分）和**无分类器引导**（通过单一网络同时学习条件和无条件预测器进行修正）的原理。
  - **统一框架**:
    - **DDPM**: 将其离散前向过程**连续化**，表明其本质上是方差保持（VP）SDE `dXt = -1/2 β(t) Xt dt + sqrt(β(t)) dWt` 的离散化。DDPM的训练目标正是网格上的逆向SDE损失，其采样过程是该逆向SDE的一阶近似。
    - **DDIM**: 确定性DDIM是**逆向概率流ODE**的一阶离散化。
    - **流匹配**: 与流匹配进行对比，流匹配直接学习生成ODE的速度场，而扩散模型学习得分并以此构建逆向动态。
    - **基于分数的SDE**: 从生成时间（噪声→数据）的视角，重新推导条件SDE `dXt = (ft Xt + g_t^2 ∇log pt(Xt|x1)) dt + gt dWt` 及其边际形式，展示其与扩散模型框架的对偶性。
  - **扩散语言模型**: 将此框架扩展到连续嵌入空间中的文本生成，通过冻结提示词嵌入、对响应词嵌入加噪和去噪，并引入**舍入损失**以保证离散令牌的恢复。推理时使用DDIM风格的随机展开。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **无特定实验设计**: 本文是一篇**理论教程**，而非提出新算法的原始研究。因此，它**没有进行传统意义上的对比实验、基准测试或使用具体数据集**。
- **场景与案例**: 教程在第9节中以**扩散语言模型**为例，展示了如何将连续状态扩散框架应用于文本生成（如Diffusion-LM、DiffuSeq等方法），但这是方法论的应用阐述，而非新的实验验证。

## 4. 资源与算力：如果文中有提到，请总结使用了多少算力

- 本文为纯理论推导和统一框架阐述，**未提及任何模型训练、数据集规模或所用算力（如GPU型号、数量、训练时长）**。

## 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、是否客观、公平

- **不适用**: 作为一篇教程，它**没有进行实验**。其价值在于逻辑推导、公式连贯性和框架的统一性，而非实验验证。论文的充分性体现在其从条件前向过程到采样方法、再到统一DDPM/DDIM/流匹配的数学推导链条的完整性上。

## 6. 论文的主要结论与发现

- **数学统一性**: 成功展示了扩散模型的核心要素——加噪前向过程、去噪逆向动态、得分匹配、噪声预测损失——如何**自然地从连续时间微分方程（ODE和SDE）中涌现**。
- **框架等价性**: 揭示了DDPM是逆向SDE的特定离散化，而DDIM是逆向概率流ODE的离散化。将流匹配和基于分数的SDE模式纳入同一微分方程叙事。
- **核心角色**: 强化了**边际得分函数**作为连接前向扩散和逆向生成的核心桥梁角色，解释了为什么得分匹配或去噪损失是有效的。
- **领域拓展**: 展示了连续扩散框架可以无缝扩展到**条件文本生成**任务，只需处理嵌入空间和离散化的舍入。

## 7. 优点：方法或实验设计上有哪些亮点

- **高度结构化和自包含**: 从基础假设出发，逻辑清晰地递进推导，无需读者频繁查阅外部文献。附录提供了必要的数学工具（如Itô积分、福克-普朗克方程等）。
- **视角独特**: 以**微分方程作为主线**，将概率、随机过程和生成模型统一起来，比单纯介绍算法更深刻。
- **拨乱反正**: 清晰地区分了路径条件得分 `∇log pt(x|x0)` 和引导条件得分 `∇log pt(x|c)`，避免了常见混淆。
- **桥接理论缺口**: 详细比对DDPM、DDIM、DPM-Solver等具体迭代与连续反向SDE/ODE步骤，展示了离散算法背后的连续数学本质。
- **叙述清晰**: 解释公式的动机和物理意义（如得分与条件期望噪声的关系），使复杂的数学易于理解。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **缺乏实证验证**: 作为纯教程，没有通过任何基准实验来展示或验证所阐述理论的性能表现。
- **对新手可能仍有门槛**: 尽管自认为教程，但涉及随机分析、偏微分方程等较深的数学知识，可能对一些仅想使用模型的实践者不够“友好”。
- **应用聚焦有限**: 离散扩散语言模型仅被简要提及，该教程的主要权重仍放在连续空间（如图像）的扩散模型上，对离散状态扩散的数学框架着墨不多。
- **计算需求未讨论**: 教程未涉及扩散模型的一个主要局限，即迭代采样带来的高计算成本和较慢的推理速度。

（完）

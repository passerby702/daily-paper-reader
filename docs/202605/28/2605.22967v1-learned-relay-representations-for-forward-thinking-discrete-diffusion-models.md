---
title: Learned Relay Representations for Forward-Thinking Discrete Diffusion Models
title_zh: 面向前向思考离散扩散模型的学习中继表示
authors: "Benjamin Rozonoyer, Jacopo Minniti, Dhruvesh Patel, Neil Band, Avishek Joey Bose, Tim G. J. Rudner, Andrew McCallum"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22967v1"
tags: ["query:diff-gen"]
score: 8.0
evidence: 通过传播潜在信息改进掩码扩散模型以完成生成任务
tldr: 针对掩码扩散模型在迭代去噪过程中丢弃内部表示、重复计算的问题，提出Relay方法，显式学习在去噪步骤间传递潜在信息。通过可微分令牌通道和时间截断反向传播训练，该方法能有效提升模型效率和性能，在多种任务上达到先进水平。
source: arxiv
selection_source: carryover_cache
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22967v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 668, \"height\": 433, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22967v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1502, \"height\": 476, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22967v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1285, \"height\": 642, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22967v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1211, \"height\": 607, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.22967v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1535, \"height\": 458, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.22967v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1573, \"height\": 328, \"label\": \"Table\"}]"
motivation: 通过传播潜在信息改进掩码扩散模型以完成生成任务。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
当掩码扩散模型通过迭代细化生成序列时，掩码位置上丰富的内部计算被丢弃，迫使每个后续细化步骤重新计算以模型表示形式存储的有价值的内部信息。为了避免去噪轮次之间的硬重置，我们提出了学习中继表示（Relay），一种允许MDM在去噪时具有前向思考能力的方法，通过显式学习如何传播潜在信息以利于未来的去噪步骤。Relay引入了一个可微的逐令牌通道，在前向传递之间传递信息，并通过截断的时间反向传播进行训练。我们表明该框架可以扩展到最先进的扩散语言模型，并与块扩散和KV缓存等技术无缝兼容。我们首先在具有挑战性的基于数独的规划任务上对Relay的设计选择进行了详尽的论证。然后，我们将Relay扩展到最先进的DLM Fast-dLLM v2，在编码任务上优于标准的有监督微调，同时将推理延迟降低高达32%。我们的实证结果表明，最先进的DLM可以被显式训练为在解码步骤之间向前传递潜在信息，从而推进性能-延迟的帕累托前沿。我们为所有实验提供代码。

## Abstract
When Masked Diffusion Models (MDMs) generate sequences through iterative refinement, the rich internal computation over masked positions is discarded, forcing every subsequent refinement step to recompute the valuable internal information stored as model representations. To avoid a hard reset between denoising rounds, we propose Learned Relay Representations (Relay), a method that allows MDMs to be forward-thinking when denoising by explicitly learning how to propagate latent information for the benefit of future denoising steps. Relay introduces a differentiable per-token channel that passes information between forward passes and is trained via truncated backpropagation through time (BPTT). We show that this framework can be scaled to state-of-the-art Diffusion Language Models (DLMs), and is seamlessly compatible with techniques like block diffusion and KV caching. We first provide a thorough justification of the design choices in Relay on a challenging Sudoku-based planning task. We then scale Relay to Fast-dLLM v2, a state-of-the-art DLM, outperforming standard supervised finetuning on coding tasks while reducing inference latency by up to 32%. Our empirical results demonstrate that state-of-the-art DLMs can be explicitly trained to relay latent information forward across decoding steps, advancing the performance-latency Pareto frontier. We provide code for all our experiments.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义
- **核心问题：** 论文针对**掩码扩散模型（MDMs）** 在迭代生成序列时的**“硬重置”问题（Hard Reset Problem）**。在标准MDM的多个去噪步骤间，模型只传递已解码的离散令牌，而将前一步计算出的所有丰富的连续隐藏状态全部丢弃，导致下一步必须从头计算，无法复用中间计算结果。
- **整体含义：** 作者提出应让MDM具备 **“前向思考”（Forward-Thinking）** 的能力，即通过显式地学习如何在去噪步骤间传播有用的潜在信息（而非仅仅传递令牌），将迭代过程转变为一种有效的循环计算，以提升模型的推理性能和效率。

### 2. 论文提出的方法论
- **核心思想：** 提出**学习中继表示（Learned Relay Representations），简称Relay**。该方法在MDM的每个去噪步骤间，除了传递新揭露的令牌外，还额外传递一个连续的、可微的潜在状态“中继”，为下一步提供前一步的中间计算结果。
- **关键技术细节：**
    - **中继状态传递：** 在每个推理步骤 `k`，模型 `fθ` 的输入是当前部分掩码序列 `xtk` 的嵌入与前一步的中继状态 `hk` 经过一个中继模块 `Rθ` 投影后的加和。模型输出新的隐藏状态 `hk+1`，该状态一方面通过解嵌头生成令牌logits，另一方面作为下一个步骤的 `hk+1` 继续传递。
    - **训练方式：** 采用**截断的时间反向传播（Truncated BPTT）** 进行端到端训练。训练时，从一个全掩码序列开始，展开 `K` 步的推理轨迹。在每个内部步骤，根据模型置信度选择揭露哪些位置的令牌（教师强制使用真实令牌），并将交叉熵损失沿着这 `K` 步轨迹通过中继状态 `h` 进行反向传播。这使得模型被显式训练去学习生成对未来预测有帮助的隐藏状态。
    - **算法流程：** `Algorithm 1` 描述了训练过程。核心循环包含：1) 使用上一轮中继状态 `h` 和当前掩码序列 `z` 通过模型得到 `h` 和 logits `ℓ`；2) 计算对真实令牌 `x0` 的掩码位置损失；3) 根据logits选择要揭露的令牌，并用真实值强制更新 `z`；重复 `K` 步后，通过梯度下降更新参数。
- **架构无关性：** Relay作为一种方法，其核心思想是架构无关的，并且兼容主流的DLM加速技术，如块扩散（Block Diffusion）和KV缓存。

### 3. 实验设计
- **数据集与场景：**
    1.  **数独（Sudoku）：** 作为一个小规模但具有挑战性的规划与推理任务。使用了**Sudoku-Extreme**数据集，并衍生出一个仅包含可通过推理策略（非暴力回溯）解决的难题子集，用于精细化分析。
    2.  **代码生成：** 在一个1.5B参数的先进DLM **Fast-dLLM v2** 上进行适配。训练数据是**OpenCodeInstruct**和**OpenMathInstruct-2**混合而成的60k样本。在代码评测基准`HumanEval`和`MBPP`（及它们的Plus版本）上进行评估。
- **对比的基准与方法：**
    - **数独任务：** 进行了详尽的消融实验，逐步开启Relay的组件进行对比，包括：1）标准掩码语言模型训练（`MLM`）；2）仅使用推理策略的轨迹展开（`Rollout`，不传递隐藏状态）；3）传递隐藏状态但不进行BPTT训练（`Relay (sg)`）；4）完整的Relay方法（进行BPTT）。此外，还对比了权重绑定与非绑定嵌入的影响。
    - **代码生成任务：** 将`Relay`与 `Vanilla SFT`（标准有监督微调）以及`Relay (sg)`在`Fast-dLLM v2`上进行了适配对比。

### 4. 资源与算力
- **数独实验：** 使用单个GPU，bf16混合精度，训练30万步。
- **Fast-dLLM v2实验：** 使用两台**NVIDIA A100 80GB** GPU，通过DeepSpeed ZeRO-3、bf16混合精度和梯度检查点进行全参数微调。有效批大小为32，训练200个优化器步骤。
- **显存分析：** 论文对Relay训练引入的额外显存开销进行了精细分析。尽管`K=2`步的BPTT使训练时的活动显存有所增加，但其峰值显存（约20.1 GiB）与标准微调（约21.2 GiB）基本持平，并未显著增加。

### 5. 实验数量与充分性
- **实验组数：** 论文进行了多组实验，覆盖了不同规模的任务（小规模推理和大规模语言模型）和不同类型的分析。
    - **消融实验：** 在数独任务上系统地进行了多个对比实验，分离了轨迹展开（Rollout）、状态传递（Relay sg）和BPTT训练的独立贡献。还探讨了嵌入层权重绑定的影响。
    - **主实验：** 在Fast-dLLM v2上与标准SFT基线进行对比。
    - **可解释性分析：** 在数独任务上对中间状态进行分析，通过棋盘“合法性”指标合理解释了Relay性能提升的原因。
    - **显存分析：** 提供了详细的显存足迹分析，打消了BPTT会带来过大显存开销的疑虑。
- **充分性与公平性：** 实验设计**充分且客观**。消融实验清晰揭示了每个组件的贡献；模型适配实验使用了公认的代码基准和相同的训练数据、硬件设置进行公平对比；并且提供了深入的分析来支撑实验结论，如BPTT如何使模型在相同去噪步数下做出更准确、更自洽的预判。

### 6. 论文的主要结论与发现
- **有效性得到验证：** Relay方法在两个不同规模和任务上均被证明有效。在数独任务上，它实现了最佳的准确率-函数评估次数帕累托前沿；在Fast-dLLM v2上，它超越了标准SFT的准确率，同时将推理延迟最高**降低了32%**。
- **各组件具有建设性作用：** 消融实验表明，构成Relay的三个部分——基于推理轨迹的训练、传递隐藏状态、以及通过BPTT训练该状态——各自都能提升性能-延迟边界，且三者结合效果最佳。
- **BPTT是关键：** 通过BPTT训练的中继状态（Relay）相比于仅传递状态但不进行时间反向传播（Relay sg），能带来显著提升。分析表明，BPTT教会了中继状态在更激进地揭露令牌的同时，维持部分预测结果的自洽性（在数独中表现为棋盘合法性更高），从而在更少的前向传播次数内达到更高准确率。

### 7. 优点
- **方法简洁通用：** 提出了一种架构无关的方法，解决MDM中普遍存在的“硬重置”问题，其核心思想清晰且能无缝集成到现有先进DLM中。
- **实验设计严谨：** 通过细致的消融研究精确量化了方法各组件的价值，并通过“合法性”分析为性能提升提供了直观的解释。
- **工程实践性强：** 专门分析了BPTT带来的显存开销，并证明其峰值可控，打消了实际应用中的一个主要顾虑，展示了方法的实用性。
- **性能提升显著：** 不仅在准确率上取得优势，更重要的是在推理延迟上实现了显著提升，切中了DLM在效率方面的核心痛点。

### 8. 不足与局限
- **额外训练开销：** 尽管显存峰值可控，但`K=2`步的BPTT无疑会增加模型训练的总计算量和时间，相较于标准掩码扩散模型训练。
- **每步推理开销：** 在推理时，读写中继状态本身会引入极微小的计算开销，虽然其带来的步数减少通常能产生净收益，但在极端追求单步低延迟的场景下仍需考量。
- **长序列的BPTT稳定性：** 论文仅使用了`K=2`的截断长度，对于更长的推理步骤链，BPTT可能面临梯度消失/爆炸等稳定性挑战，这一点未进行探索。
- **应用领域有限：** 实验集中在文本推理（数独）和代码生成上，虽然作者提出可扩展到图像、分子等其他模态，但目前缺乏相关实证。

（完）

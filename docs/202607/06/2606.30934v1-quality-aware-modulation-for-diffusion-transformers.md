---
title: Quality-Aware Modulation for Diffusion Transformers
title_zh: 扩散变压器的质量感知调制
authors: "Luke Budny, Yuhong Guo, Kevin Cheung"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30934v1"
tags: ["query:diff-gen"]
score: 8.0
evidence: 改进扩散Transformer用于文本到图像生成，属于多模态扩散模型
tldr: 现有扩散变换器（DiT）依赖时间步或提示嵌入来调节去噪强度，缺乏质量感知信息，导致生成图像不对齐、视觉不一致。本文提出质量表示模块（QRM），一种轻量级transformer模块，利用现有模型输入学习质量感知向量M_qrm，调整DiT中的自适应层归一化调制，注入质量敏感信号。实验表明，QRM在保持原有采样流程和扩散骨架不变的前提下，显著提升DiT基模型的图像质量，在多个指标上优于基线。该方法无需修改扩散骨干，即插即用，为扩散模型的质量控制提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30934v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1113, \"height\": 748, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30934v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1524, \"height\": 565, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30934v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1703, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30934v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1624, \"height\": 1088, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.30934v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1108, \"height\": 791, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30934v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 974, \"height\": 259, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30934v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 950, \"height\": 256, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30934v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 930, \"height\": 211, \"label\": \"Table\"}]"
motivation: 现有DiT调制方式缺乏质量感知信息，导致生成图像不对齐、视觉不一致且保真度低。
method: 提出质量表示模块（QRM），它从现有输入学习质量感知向量M_qrm，并注入DiT的自适应层归一化调制中。
result: QRM在不改变采样调度或扩散骨架的情况下，显著提升DiT基模型的图像质量，一致优于基线。
conclusion: QRM是一种轻量、即插即用的质量感知调制方法，有效改善了扩散变换器的生成保真度。
---

## 摘要
现代文生图扩散模型，例如扩散变压器（DiT），依赖于时间步或提示嵌入来调制每个时间步中降噪过程的强度。虽然这种调制传达了当前的噪声水平，但它不提供任何质量感知信息，这可能导致生成的图像不对齐、视觉上不一致且缺乏保真度。在本文中，我们提出了质量表示模块（QRM），这是一个轻量级变压器模块，基于现有模型输入学习质量感知表示，并生成一组向量$M_{qrm}$。这些向量调整DiT变压器块内的自适应层归一化调制，从而向降噪参数注入质量敏感信号。QRM没有对采样调度或扩散主干引入显著变化。实验包括对QRM训练损失和架构的消融研究，以及经验结果，表明与基线基于DiT的模型相比，图像质量持续改进。

## Abstract
Modern text-to-image diffusion models, such as diffusion transformers (DiT), rely on timestep or prompt embeddings to modulate the strength of the denoising process in each timestep. While this modulation communicates the current noise level, it does not provide any quality-aware information, which can lead to generated images that are unaligned, visually inconsistent, and lacking in fidelity. In this paper, we propose the Quality Representation Module (QRM), a lightweight transformer module that learns a quality-aware representation based on existing model inputs, and produces a set of vectors $M_{qrm}$. These vectors adjust the adaptive LayerNorm modulation within the DiT transformer blocks, thereby injecting a quality-sensitive signal into the denoising parameters. The QRM introduces no significant changes to the sampling schedule or diffusion backbone. Experiments include ablations on QRM training losses and architectures, as well as empirical results demonstrating consistent image quality improvements over baseline DiT-based models.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：当前主流的文本到图像扩散模型（如DALL·E、Midjourney、Stable Diffusion）采用扩散变压器（DiT）架构，通过时间步嵌入（timestep embedding）和提示嵌入（prompt embedding）来调节去噪过程。这种调制机制仅传递当前噪声级别和语义信息，但**缺乏对中间生成图像质量或保真度的感知**。
- **问题**：由于去噪过程中可能累积误差、错误解释提示或产生幻觉特征，导致生成图像出现不对齐、视觉不一致、保真度低等问题。而基线调制信号仅依赖于时间步和提示，无法根据潜变量状态的演化直接修正这些问题。
- **意义**：本文旨在探索如何将质量感知信息注入调制路径，从而在不修改扩散骨干网络和采样流程的前提下提升生成质量。该研究为扩散模型的质量控制提供了一种轻量级、即插即用的新思路。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：提出**质量表示模块（QRM）**，一个轻量级transformer模块，基于当前潜变量、基线调制参数、提示和时间步，预测一组调制偏移量 $M_{qrm}$，将其加到基线AdaIN调制参数 $M_{base}$ 上，从而注入质量敏感信号。

- **关键技术细节**：

  - **输入**：潜变量 $x_t$、池化提示嵌入 $p$、时间步嵌入 $t$、基线调制参数 $M_{base}$（由各AdaIN层的缩放、平移、门控向量拼接而成）。
  - **特征提取**：
    - 从潜变量 $x_t$ 经 $1\times1$ 卷积投影后自适应池化为 $S\times S$ 网格，再展平得到潜令牌。
    - 每个AdaIN层对应一个调制查询令牌：将对应段 $u_i$ 归一化后线性投影并加偏置，再加位置嵌入。
    - 提示和时间步嵌入经线性投影得到键-值记忆。
  - **架构**：采用预层归一化的transformer解码器堆栈（含交叉注意力），输出经最终层归一化后线性投影回原始AdaIN宽度，得到偏移量 $M_{qrm}$。
  - **最终调制**：$M = M_{base} + M_{qrm}$，用于每块AdaIN的缩放、平移、门控。

- **训练方法（ReFL策略）**：
  - 冻结扩散骨干、文本编码器、VAE和奖励模型，仅更新QRM参数。
  - 从预定义的活动步集合 $\mathcal{K}_{qrm}$（通常为早期高噪声步）中采样一个步 $k$。
  - 正常扩散到步 $k$，应用QRM调制后执行一步去噪，解码得到图像 $\hat{I}_{k-1}$，由冻结的奖励模型（如HPSv2.1或CLIPScore）打分。
  - 优化两种损失之一：
    - **奖励最大化损失**：$\mathcal{L}_{reward} = \mathbb{E}[ (m - s^{qrm})_+ ]$
    - **间隔追求损失**：$\mathcal{L}_{margin} = \mathbb{E}[ (m - (s^{qrm} - s^{base}))_+ ]$，推动QRM图像超过基线图像一个间隔 $m>0$。

- **推理过程**：仅在活动步 $\mathcal{K}_{qrm}$ 上启用QRM，其他步退化为基线。采样器为DPM-Solver++(2M)。

## 3. 实验设计：数据集、基准、对比方法

- **训练数据集**：ImageReward提示集（10,000个多样化文本提示）。
- **评估基准**：Parti-Prompts（1,632个提示，覆盖组合式、属性型指令）。
- **主干模型**：Stable Diffusion 3.5 Medium（冻结）。
- **对比方法**：
  - 直接推理基线：SD1.5、SD3.5
  - 提示精炼方法：BeautifulPrompt、Promptist、DNP
  - 微调方法：DDPO-Aesthetic、DDPO-Alignment、Diffusion-DPO、ReFL、TextCraftor
  - 负嵌入方法：ReNeg（全局/逐样本）
- **评估指标**：HPSv2.1（人类偏好对齐）、Aesthetic（美学质量）、CLIPScore（语义对齐）、ImageReward（人类偏好）。
- **评估公平性**：所有对比使用相同SD3.5骨干、采样器、引导设置、提示集和随机种子；仅改变是否添加QRM调制。

## 4. 资源与算力

- **训练配置**：单卡RTX 5090；使用Adam优化器，学习率2.5×10⁻⁵，梯度累积3，批次大小1；训练25个epoch，每epoch 399步（共约9,975次更新）。
- **推理开销**：采样512×512图像时，基线SD3.5 Medium约5秒/张，添加QRM后约6秒/张（约20%开销）；峰值VRAM从约10GB增至约12.5GB；QRM参数约2.5GB（fp32）。
- **训练额外开销**：每迭代额外增加25-30%时间。

## 5. 实验数量与充分性

- **实验组别**：
  - 主要定量比较（表1）：与9种基线方法对比，涵盖两维度（Aesthetic、HPSv2.1）。
  - 消融实验（表2-4）：
    - 模型大小消融（small/medium/large，分别对应(4,384)、(6,512)、(8,640)）。
    - 时间步范围消融（早期/中期/晚期）。
    - 训练奖励指标消融（CLIPScore vs HPSv2.1）。
  - 定性轨迹可视化（图3）：展示不同时间步范围下解码潜变量的演化。
  - 补充材料（附录A）：基线图像、计算效率分析。
- **充分性评估**：
  - **客观性**：严格控制变量，使用相同种子和超参数，采用标准基准和公开指标，结果可复现。
  - **公平性**：对比基线方法时明确说明部分方法基于SD1.5，与本研究SD3.5不可直接比较，但作为参考；主要对比是SD3.5 baseline vs SD3.5+QRM，保证了公平。
  - **充分性**：消融覆盖架构、时间步、奖励信号三个关键设计维度；定性可视化强化了时间步选择的重要性；计算效率分析增加了实用性。但缺少对更多DiT架构（如FLUX）的泛化实验，也未进行多奖励联合训练实验。

## 6. 主要结论与发现

- **核心结论**：QRM通过注入质量感知调制，在不修改扩散骨干和采样流程的前提下，显著提升生成图像的保真度和提示对齐度。
- **量化结果**：
  - 在Parti-Prompts上，SD3.5+QRM（HPSv2.1训练）达到最高Aesthetic 6.22、HPSv2.1 29.97，相比基线分别提升+0.20和+0.82。
  - 训练奖励信号选择直接影响模型偏好的质量维度（CLIPScore训练提升CLIP，HPSv2.1训练提升HPS）。
  - 早期时间步（高噪声）应用QRM效果最佳，后期应用改善有限。
  - 更大模型容量更多优化奖励信号，但可能牺牲其他指标。

## 7. 优点

- **轻量即插即用**：无需修改预训练骨干，仅通过添加AdaIN偏移量工作，可轻易集成到现有DiT流水线。
- **高效性**：只活动于部分时间步，引入的推理开销（20%）和参数增量（~2.5GB）适中。
- **模块化设计**：可独立训练QRM，训练后冻结使用，奖励模型可灵活替换（CLIPScore、HPSv2.1等）。
- **消融充分**：系统分析了架构大小、时间步选择、奖励信号三个关键因素对性能的影响，增强了方法可解释性。
- **公平比较**：明确控制变量，使用相同种子和评估设置，避免选择性展示。
- **定性验证**：通过可视化中间潜变量演化，直观展示QRM对早期语义结构的积极影响。

## 8. 不足与局限

- **泛化性有限**：实验仅基于SD3.5 Medium，未测试其他DiT架构（如FLUX、SD3.5 Large等），缺乏跨架构泛化证据。
- **奖励偏差风险**：QRM继承训练所用奖励模型的偏见，例如使用HPSv2.1训练可能过度优化特定偏好，对未覆盖的提示类型可能不够鲁棒。
- **计算开销**：尽管相对骨干较小，但推理需额外~20%时间，且QRM参数存储2.5GB，对资源受限场景可能不友好。
- **时间步选择固定**：活动时间步范围是预设子集，未探索自适应或动态选择策略，可能限制最优性能。
- **无多奖励联合训练**：当前仅单奖励信号训练，多奖励融合可能提升综合质量但未被研究。
- **训练复杂度**：ReFL策略需每一步采样、解码、评分，训练迭代比简单微调多25-30%时间，且每一步需完整前向扩散骨干，可能增大训练成本。
- **未与直接后训练方法（如LoRA）对比**：尽管声称不修改骨干，但未与同等计算量的微调方法（如LoRA）在相同骨干上进行公平比较，无法充分论证调制干预的优势。

（完）

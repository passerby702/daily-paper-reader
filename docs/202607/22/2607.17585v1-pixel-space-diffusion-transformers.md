---
title: Pixel-Space Diffusion Transformers
title_zh: 像素空间扩散变换器
authors: "Renye Yan, Jikang Cheng, You Wu, Ling Liang, Wei Peng, Athanasios V. Vasilakos, Qingyu Zhao, Yu Zhang, Ehsan Adeli, Kilian M. Pohl, Guoying Zhao"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17585v1"
tags: ["query:diff-gen"]
score: 8.0
evidence: 像素空间扩散模型用于多模态生成
tldr: 潜在扩散模型在VAE压缩空间去噪，但固定分词器会丢失纹理细节，且表示与扩散训练目标不匹配。像素空间扩散直接建模原始像素，移除VAE瓶颈，实现端到端优化，但面临高维建模挑战。本文综述像素空间扩散Transformer（pDiTs）的架构、生成机制与统一多模态建模，总结代表性方法、技术挑战和未来方向，旨在推动高保真、整合生成与理解的视觉基础模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17585v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1391, \"height\": 1092, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17585v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1853, \"height\": 919, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17585v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 897, \"height\": 938, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17585v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 879, \"height\": 922, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17585v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1871, \"height\": 1200, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17585v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1861, \"height\": 1240, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17585v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1894, \"height\": 1299, \"label\": \"Table\"}]"
motivation: 潜在扩散模型因固定分词器丢失细节且训练目标不匹配，需探索像素空间扩散以提升保真度和端到端优化。
method: 综述像素空间扩散Transformer，涵盖模型架构、连续生成机制和统一多模态建模，总结pDiTs代表性方法。
result: 揭示了像素空间扩散在高维建模中的噪声调度、损失加权等挑战，并指出其在统一多模态系统中的潜力。
conclusion: 像素空间扩散为高保真生成和理解提供了新方向，但仍需解决架构和效率问题，未来可构建端到端视觉基础模型。
---

## 摘要
潜在扩散模型（LDMs）通过在VAE压缩的潜在空间中进行去噪，实现了高效的高分辨率图像合成。然而，固定视觉分词器会丢弃精细纹理和结构细节，而分离的表示与扩散训练导致重建与生成目标之间的不匹配。这些限制重新激发了人们对像素空间扩散的兴趣，它直接建模原始像素，移除VAE瓶颈，并支持端到端优化。这种公式更好地满足了高保真生成的需求，但在高维建模中引入了挑战，包括噪声调度、损失加权、分词效率和可扩展架构设计。像素空间建模也为统一多模态系统提供了有前景的基础：原始像素、文本和任务条件可以在共享的标记空间中表示，并由单个Transformer联合处理，从而缩小视觉理解与生成之间的差距。本文从模型架构、连续生成机制和统一多模态建模的角度回顾了像素空间扩散变换器（pDiTs）。我们总结了代表性方法，识别了关键技术挑战，并讨论了迈向集成生成与理解的高保真、端到端视觉基础模型的未来方向。

## Abstract
Latent diffusion models (LDMs) enable efficient high-resolution image synthesis by denoising in a VAE-compressed latent space. However, fixed visual tokenizers can discard fine textures and structural details, while separate representation and diffusion training creates a mismatch between reconstruction and generation objectives. These limitations have renewed interest in pixel-space diffusion, which models raw pixels directly, removes the VAE bottleneck, and supports end-to-end optimization. This formulation better matches the demands of high-fidelity generation but introduces challenges in high-dimensional modeling, including noise scheduling, loss weighting, token efficiency, and scalable architecture design. Pixel-space modeling also offers a promising basis for unified multimodal systems: raw pixels, text, and task conditions can be represented in a shared token space and jointly processed by a single Transformer, narrowing the gap between visual understanding and generation. This paper reviews Pixel-Space Diffusion Transformers (pDiTs) from the perspectives of model architecture, continuous generative mechanisms, and unified multimodal modeling. We summarize representative methods, identify key technical challenges, and discuss future directions toward high-fidelity, end-to-end vision foundation models that integrate generation and understanding.

---

## 论文详细总结（自动生成）

# 像素空间扩散变换器（Pixel-Space Diffusion Transformers）论文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **现有范式瓶颈**：潜在扩散模型（LDMs）采用“压缩后扩散”两阶段策略，利用预训练VAE将图像压缩到低维潜在空间进行去噪，极大降低了计算开销。然而，这一范式存在结构性天花板：
  - 固定视觉分词器（VAE/VQ-VAE）的有损压缩会丢弃高频纹理、锐利边界和微小局部结构，形成不可逆的信息瓶颈。
  - 两阶段训练目标不匹配：自动编码器的重建目标与扩散模型的生成目标存在差异，阻碍端到端优化。
  - 潜在空间的“黑盒”特性使内部几何与语义结构不透明，增加了优化不确定性。
- **像素空间扩散的复兴**：近期研究重新关注在原始像素空间直接建模，通过可扩展的Transformer架构、连续生成公式（Flow Matching）和大型分布式计算，消除外部VAE瓶颈，实现端到端的高保真视觉生成。
- **对统一多模态模型的意义**：像素空间框架提供了一种自然方式，将文本、像素和任务条件映射到共享标记空间，由同一Transformer联合建模，有望弥合视觉理解与生成之间的表示冲突。

## 2. 方法论：核心思想、关键技术细节

本文为系统综述，未提出单一新方法，但系统总结了pDiT的理论基础和架构分类。

- **理论基础**：
  - **DDPM**：离散时间马尔可夫逆扩散过程，噪声预测损失 \( L_{\text{DDPM}} = \mathbb{E}[\|\epsilon - \epsilon_\theta(x_t, t)\|^2] \)。
  - **Flow Matching**：连续概率传输，学习时间相关速度场 \( v_\theta(x_t, t) \)，条件流匹配损失 \( L_{\text{CFM}} = \mathbb{E}[\|v_\theta(x_t, t) - (x_1 - x_0)\|^2] \)，线性插值路径 \( x_t = (1-t)x_0 + tx_1 \)。
- **核心操作流程**：给定噪声图像 \( x_t \)，经补丁化（patchify）和可学习嵌入得到视觉标记序列 \( z_t \)，然后由Transformer主干建模，最后通过端到端解码头直接预测干净图像 \( \hat{x}_0 \)。
- **架构分类**（见表I）：
  - 单流大补丁Transformer（如JiT）
  - 分层和沙漏pDiT（如HDiT）
  - 全局-局部解耦架构（如DiP、PixelDiT）
  - 频率解耦架构（如DeCo、Spectral Forcing）
  - 隐式神经场解码架构（如PixNerd）
  - 跨尺度语义锚定架构（如HyperDiT）
  - 共享标记统一多模态架构（如HiDream-O1-Image）
- **结构化解决方案应对核心挑战**（见表II）：
  - 二次复杂度：大补丁、分层聚合、局部注意力。
  - 粒度冲突：全局-局部解耦、双级Transformer。
  - 高维轨迹优化：直接干净图像预测、视觉基础模型引导。
  - 高频信号与噪声干扰：频谱滤波、频域损失分解。
  - 语义初始化：冻结VFM特征对齐（如REPA、SGA）。

## 3. 实验设计

本文为综述，不包含原创实验。但文中引用了大量代表性方法的实验结果：

- **数据集/场景**：
  - 类条件生成：ImageNet（256×256、512×512等）。
  - 文本到图像：开放域文本-图像生成（如HiDream-O1-Image）。
  - 图像编辑/修复：Palette等任务。
  - 视频生成：Kinetics-600。
  - 3D生成：PixGS、PixWorld、PointDiT等。
  - 医学图像：CT体积生成（MedSyn、PRDiT）、图像翻译（PPDM）。
- **Benchmark**：主要评估指标包括FID、CLIP Score、Inception Score、FVD（视频）、OCR准确率、结构保持率等。
- **对比方法**：与潜在扩散模型（LDM、DiT、SDXL）、早期像素扩散（DDPM）、其他pDiT变体（Simple Diffusion、SiD2、HDiT、JiT、PixelDiT等）进行比较。

## 4. 资源与算力

- **文中未明确给出具体算力消耗数据**（如GPU型号、数量、训练时长）。作者主要从计算复杂度角度进行分析：
  - 指出pDiT面临二次复杂度挑战：对于分辨率H×W、补丁大小P，标记数 \( N = \frac{HW}{P^2} \)，全局自注意力复杂度 \( O(N^2 d) \)。
  - 举例：1024×1024图像、补丁16×16时，标记数4096，单层单头注意力矩阵约1.68×10^7元素；2048×2048时标记数16384，矩阵约2.68×10^8元素（增长16倍）。
  - 提及低层次优化如FlashAttention可缓解存储压力，但不改变二次计算本质。
- **隐含信息**：现代pDiT依赖大规模分布式计算，但综述未提供量化数据。

## 5. 实验数量与充分性

- **作为综述**，不进行新实验，而是综合大量既有研究的实验结果。
- **充分性评价**：
  - 架构分类覆盖7大类，每类均有代表性方法及其关键特征、优势与局限（表I）。
  - 对核心挑战列出5类结构化解决方案（表II）。
  - 应用覆盖图像生成、编辑/修复/端侧部署、视频、3D、医学图像等广泛领域。
  - 讨论了统一多模态模型的挑战（优化困难、计算成本、任务梯度干扰）。
  - 实验覆盖全面，但缺乏对各个方向具体性能数据的统一对比（FID、计算成本等），这是综述性质决定的。
- **公平性**：作者承认pDiT和潜在扩散各有优势，不主张绝对替代，分析相对客观。

## 6. 主要结论与发现

- **像素空间扩散的复兴是有依据的**：通过更好的噪声调度、直接干净图像预测、Transformer可扩展性，端到端像素扩散可以缩小与潜在扩散的差距。
- **pDiT的优势在于消除固定分词器瓶颈**，在细节保真度、文本渲染、局部编辑、精细结构恢复等方面具有更高上限。
- **面临系统性挑战**：计算成本高（二次注意力）、多尺度表示冲突、优化困难、高频噪声干扰、语义初始化不足。
- **统一多模态模型中，像素空间提供共享表示基础**：文本、像素、任务条件可在同一Transformer中联合建模，但需应对多任务梯度干扰和灾难性遗忘。
- **未来方向**：动态标记粒度、原生像素轨迹设计、非侵入式结构引导、区分计算-保真度帕累托前沿、从共享标记走向真正统一的生成与理解。
- **核心观点**：pDiT不是要完全取代潜在扩散，而是在信息保真度、计算效率和统一建模之间寻求新的平衡。

## 7. 优点

- **全面系统性**：首次专门针对像素空间扩散变换器进行综述，覆盖架构、理论、应用、挑战和未来展望，结构清晰（图1给出了完整路线图）。
- **分类合理**：对pDiT架构进行7分类（表I），对核心挑战与结构化解决方案进行5类总结（表II），便于研究者快速定位技术面貌。
- **理论深度**：详细对比DDPM与Flow Matching，分析高维像素空间的优化特性、频率干扰、表示对齐冲突等深层问题。
- **实践关联**：讨论了应用场景（图像、视频、3D、医学），并指出pDiT在需要精细保真度的任务（文本渲染、局部编辑、3D属性预测）中的独特价值。
- **前瞻性讨论**：第六、七节深入探讨了计算-保真度折衷、非侵入式指导、遗忘缓解、动态标记、后训练等开放问题，为后续研究提供了方向。

## 8. 不足与局限

- **缺乏新实验结果**：作为综述，不提供原始实验对比，读者无法直接评估不同pDiT方法的相对性能。
- **量化算力分析不足**：虽从复杂度角度讨论，但未提供实际训练/推理GPU小时、模型参数量等具体数据，对实际部署参考价值有限。
- **性能数据分散**：文中提到的方法在各自论文中采用了不同数据集、评估指标和设置，综述未进行统一标准化比较，难以直接判断孰优孰劣。
- **对潜在扩散优势的覆盖相对薄弱**：虽然承认潜在扩散的效率优势，但对VAE改进（如VFM-VAE、RAE）等可以缓解信息瓶颈的近期进展着墨较少。
- **应用验证不均衡**：大部分pDiT应用仍集中在2D图像生成，视频、3D、医学领域的实证规模较小，泛化性有待进一步验证。
- **可能存在作者立场偏差**：论文由pDiT相关研究者撰写，虽保持客观，但可能倾向于强调pDiT的潜力而弱化其实际部署瓶颈。

（完）

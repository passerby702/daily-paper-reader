---
title: "Twins: Learn to Predict Unified Representations with Focal Loss"
title_zh: Twins：使用焦点损失学习预测统一表示
authors: "Kaixiong Gong, Xin Cai, Bin Lin, Hao Wang, Yunlong Lin, Mingzhe Zheng, Bohao Li, Jian-Wei Zhang, Miles Yang, Zhao Zhong, Liefeng Bo, Xiangyu Yue"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22531v1"
tags: ["query:diff-gen"]
score: 8.0
evidence: 扩散Transformer用于统一多模态表示学习与生成
tldr: 统一多模态模型需要共享视觉token空间，但现有连续管线常依赖语义特征和低层潜变量两种分离表征，导致潜在空间不匹配。Twins通过通道级联ViT和VAE特征形成统一连续token空间，保持序列长度不变。针对联合建模中优化不平衡问题，采用聚焦回归目标加权大误差VAE维度。在ImageNet上无分类器引导下提升10.57 gFID，同时改善重建保真度和多模态理解，缩小了理解与生成导向表征的差距。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22531v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 835, \"height\": 542, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22531v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 767, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22531v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 764, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22531v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 761, \"height\": 494, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22531v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 852, \"height\": 436, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22531v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 758, \"height\": 576, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22531v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1761, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22531v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 524, \"height\": 630, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.22531v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 853, \"height\": 1119, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22531v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1763, \"height\": 1544, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22531v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1770, \"height\": 902, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22531v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 877, \"height\": 471, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22531v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1764, \"height\": 1617, \"label\": \"Table\"}]"
motivation: 现有统一多模态连续管线中，理解和生成使用不同表征（如ViT和VAE）导致潜在空间不匹配，影响联合建模效果。
method: Twins通过通道级联ViT和VAE特征形成统一连续token空间，并引入聚焦回归目标优化流匹配，平衡ViT与VAE组分的训练。
result: 在ImageNet上无分类器引导实现10.57 gFID提升，多模态理解和重建保真度均达到竞争力水平。
conclusion: Twins有效融合了理解与生成导向表征，为统一多模态模型提供了可行的连续空间方案。
---

## 摘要
统一多模态模型旨在寻求一个既支持多模态理解又支持图像生成的共享视觉标记空间。离散方法通过共享码本统一接口，而连续管道通常依赖两种不同的表示——用于理解的语义特征（例如ViT）和用于合成的底层潜在变量（例如VAE）——导致潜在空间不匹配。我们提出Twins，这是一种通过在相同标记网格上按通道拼接ViT和VAE特征形成的连续统一标记空间，因此序列长度不变且注意力成本不增加。然而，在扩散Transformer中联合建模Twins会暴露出严重的优化不平衡：模型能很好地拟合ViT组件，但难以匹配VAE潜在分布。我们将这种不平衡追溯至三个异质性来源：频率偏差、内在维度以及条件对齐与条件独立的不确定性。为了解决这个问题，我们为流匹配调整了焦点回归目标，该目标对具有大误差的VAE维度进行加权，从而更好地平衡ViT和VAE组件的优化。在ImageNet上，与无分类器指导下的朴素MSE损失相比，这带来了高达10.57 gFID的提升。Twins在多模态理解基准上也表现出竞争力，并提高了重建保真度，缩小了面向理解和面向生成的表示之间的差距。

## Abstract
Unified multimodal models seek a shared visual token space that supports both multimodal understanding and image generation. Discrete methods unify the interface via a shared codebook, whereas continuous pipelines often rely on two disparate representations -- semantic features (e.g., ViT) for understanding and low-level latents (e.g., VAE) for synthesis -- resulting in mismatched latent spaces. We propose Twins, a unified continuous token space formed by channel-wise concatenating ViT and VAE features on the same token grid, so the sequence length is unchanged and attention cost does not increase. However, jointly modeling Twins in a Diffusion Transformer exposes a severe optimization imbalance: the model fits the ViT component well but struggles to match the VAE latent distribution. We trace this imbalance to three sources of heterogeneity: frequency bias, intrinsic dimensionality, and condition-aligned vs condition-independent uncertainty. To address it, we adapt a focal regression objective for flow matching that upweights large-error VAE dimensions, better balancing optimization across the ViT and VAE components. On ImageNet, this yields up to 10.57 gFID gain over naive MSE loss without classifier-free guidance. Twins also performs competitively on multimodal understanding benchmarks and improves reconstruction fidelity, narrowing the gap between understanding- and generation-oriented representations.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有统一多模态模型（UMM）在连续表示空间中普遍采用“双表示”设计——理解任务使用ViT语义特征，生成任务使用VAE潜变量。这导致两个潜在空间不匹配，无法形成真正的共享表示，系统在理解和生成之间需要额外的编码-解码步骤，增加计算复杂度和表示不一致性。
- **背景与动机**：语言模型（如GPT）在理解和生成中使用相同的token空间，形成连贯接口。而视觉连续模型尚未实现这一目标。现有工作（如UniFlow、UniLIP、RAE）尝试通过维度压缩或直接使用语义特征来统一，但面临重建质量差或表示能力下降等问题。作者将此难题总结为“不可能三角”：一个单一的表示难以同时支持强理解、高质量重建和生成友好可预测性。
- **整体含义**：本文旨在通过简单有效的通道级联构建统一连续表示（Twins），并解决联合建模中的优化不平衡问题，从而向真正的共享视觉表示迈进一步。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  - 构造统一表示：将ViT（SigLIP2）特征与VAE（Flux.2）特征在同一token栅格上进行通道级联，形成Twins表示。由于共享相同空间尺度，序列长度不变，注意力计算成本不增加。
  - 缓解优化不平衡：针对扩散Transformer（DiT）联合建模时优先拟合ViT而忽视VAE的问题，提出使用焦点损失（Focal Loss）重新加权VAE通道上的回归任务，避免模型陷入只能拟合语义特征的次优解。

- **关键技术细节**：
  - **Twins构造**：对于图像 I，ViT编码器输出 \(f_{vit}(I) \in \mathbb{R}^{L \times d_{vit}}\)，VAE编码器输出 \(f_{vae}(I) \in \mathbb{R}^{L \times d_{vae}}\)，拼接得 \(z = [f_{vit}(I), f_{vae}(I)]\)，维度 \(L \times (d_{vit}+d_{vae})\)。
  - **生成训练**：采用流匹配（Flow Matching）框架，线性插值得到中间样本 \(x_t = (1-t)x_0 + t x_1\)，训练网络预测速度场 \(v = x_1 - x_0\)，使用MSE损失。
  - **不平衡原因分析**：三种异质性：
    - 频谱偏差：ViT特征能量集中于低频，VAE特征含丰富高频，网络自然先学习低频（频谱偏置理论）。
    - 内在维度：Two-NN估计显示SigLIP内在维度约14.7，VAE约35.1；低维流形更容易学习。
    - 条件依赖性：给定条件，ViT特征有效维度急剧下降（条件对齐），而VAE特征保持高维（条件独立的不确定性大）。
  - **焦点损失**：在VAE通道上应用权重 \(w_i = |v_i - v_{\theta}(z,t)_i|^{2\gamma}\)，其中 \(\gamma=0.5\)，形成加权MSE损失。这惩罚了误差大的通道，迫使网络关注难学的VAE细节。

- **算法流程**：
  1. 预处理：对每张图像用SigLIP2和Flux.2 VAE编码，拼接得到Twins表示z。
  2. 流匹配前向：采样噪声x1，线性插值得到xt，计算真实速度v = x1 - x0。
  3. 网络前向：DiT预测v_theta(xt, t, c)。
  4. 损失计算：对ViT通道使用标准MSE，对VAE通道使用加权MSE（权重由当前残差确定）。
  5. 反向传播更新。
  6. 测试时采样：从噪声开始，通过ODE求解器生成Twins表示，再分别用VAE解码器或SigLIP解码器得到图像。

## 3. 实验设计：数据集、场景、benchmark、对比方法

- **数据集**：
  - 图像生成：ImageNet-1K，256×256和512×256分辨率。使用类别条件生成。
  - 重建评测：ImageNet-1K验证集。
  - 多模态理解：常用基准包括POPE、GQA、TQA、MMB、MME（S+Perception）等。

- **场景与Benchmark**：
  - 重建性能：PSNR、SSIM、rFID（重建FID）。
  - 生成质量：gFID（生成FID）、IS、Precision、Recall（有条件无分类器指导或带指导）。
  - 理解任务：标准VLM评估。

- **对比方法**：
  - 重建对比：大量tokenizer（Cosmos-DI、LlamaGen、Open-MAGVIT2、BSQ-ViT、SD-VAE系列、FLUX.1-VAE、VA-VAE、Wan2.2、SelfTok等）以及统一tokenizer（Show-o、QLIP、VILA-U、TokenFlow、UniTok、UniFlow、RAE等）。
  - 生成对比：RAE、DiT、VA-VAE、REPA、VAR、MAR等。
  - 理解对比：InstructBLIP、LLaVA-1.5、Qwen-VL-Chat、Janus、UniFlow等，以及统一MLLM。
  - 主要基线：SigLIP2单独（用于理解）、Flux.2 VAE单独（用于生成）、Twins+MSE损失。

## 4. 资源与算力

论文未明确提及使用的GPU型号、数量和训练时长。仅在附录中提及“所有实验在相同网络架构、学习率、噪声偏移等超参数下进行”。未报告计算成本细节。因此需指出：文中未提供资源与算力信息。

## 5. 实验数量与充分性

- **实验数量**：相对充分。
  - 重建：一张完整表格（表1），与超过20种方法对比。
  - 生成：表3（ImageNet 256x256，不同epoch、有/无指导）、表4（ImageNet 512x256）、表5（附录更详细比较）。包括两个基线（Flux.2 VAE和SigLIP2单独）以及Twins+MSE的消融。
  - 理解：表2，与多种理解模型和统一模型对比，并提供了SigLIP2单独作为baseline。
  - 消融：在生成实验中对比了MSE损失 vs. Focal损失（图2、图8）；分析了频率分布（图4）、内在维度（图5）、PCA条件依赖性（图6）。
- **充分性**：实验设计合理，对比公平。对不平衡原因进行了定量分析。理解任务使用标准微调管道（LLaVA-558k + Cambrian-737k），设置一致。但缺少更广泛的数据集（如COCO、CIFAR）的验证，且未在不同架构（如AR模型）上测试Twins。整体上实验较为充分，结论可信。

## 6. 论文的主要结论与发现

- **主要发现**：
  - 简单的通道级联ViT+VAE即可构建统一表示，序列长度不变，保持注意力成本。
  - 联合建模时存在严重优化不平衡：DiT优先拟合易学的ViT，而忽视VAE细节。
  - 不平衡根源：频谱偏置（低频优先）、内在维度差异（低维易学）、条件依赖性（ViT条件对齐，VAE条件独立）。
  - 焦点损失能有效缓解不平衡，显著提升VAE建模性能（VAE FID下降5.36），同时略微改善SigLIP特征预测。
- **结论**：
  - Twins表示重建质量达到SOTA（PSNR 31.46, rFID 0.11），优于单纯语义编码器（RAE）。
  - 生成质量：无指导gFID从MSE损失的14.41降至3.84（80 epoch），有指导达到1.59（256x256）和1.79（512x512）。
  - 多模态理解：与强基线SigLIP2相比，Twins在GQA、TQA、MME等任务上略有提升，证明低层细节有助于某些细粒度理解。
  - 总体证实了统一连续表示的可行性，缩小了理解与生成表征之间的差距。

## 7. 优点

- **简洁高效**：无需额外训练或复杂对齐，直接拼接现有编码器。
- **理论分析深入**：系统分析了优化不平衡的三个异质性来源，提供了频谱分析、内在维度估计、PCA条件依赖性等定量证据。
- **方法有效**：焦点损失设计简单（仅基于残差平方加权），却显著改善联合建模。
- **实验全面**：覆盖重建、生成、理解三个角度，消融实验充分，对比方法多且评价指标标准。
- **开源贡献**：提供了GitHub仓库，可复现。

## 8. 不足与局限

- **实验覆盖局限**：仅使用ImageNet一个数据集进行生成和重建评估，未在更广泛场景（如文本到图像、视频、开放域）验证。
- **应用限制**：Twins假设ViT和VAE共享相同patch size和token网格，限制了可选的编码器组合。当两者空间分辨率不一致时需要插值或对齐，可能引入额外误差。
- **算力资源未报告**：缺乏训练时长和硬件规格，不利于其他团队复现或估计成本。
- **理解任务提升有限**：理解性能仅在部分任务上略优于纯SigLIP基线，且提升幅度不大（GQA +0.39, TQA +1.97），验证了低层细节的辅助作用但并非突破性。
- **焦点损失调参**：只测试了γ=0.5，未讨论不同γ值的影响或自适应权重方案。
- **潜在偏差风险**：模型依赖于预训练编码器（SigLIP2和Flux.2 VAE）的质量，若编码器存在偏差（如数据分布不均衡），可能传递至Twins。
- **与离散统一方法的对比**：论文强调连续表示的优势，但未在同一基准上系统对比离散统一tokenizer（如UniTok、TokenFlow）的生成和理解性能，公平性有待细化。

（完）

---
title: Reinforcing the Generation Order of Multimodal Masked Diffusion Models
title_zh: 强化多模态掩码扩散模型的生成顺序
authors: "Yidong Ouyang, Zhe Wang, Sourav Bhabesh, Dmitriy Bespalov"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08056v1"
tags: ["query:diff-gen"]
score: 8.0
evidence: 多模态扩散模型的生成顺序优化
tldr: "针对多模态掩码扩散模型，本文发现文本到图像生成与多模态理解中的生成顺序优化不能仅依赖模型logits。为此，提出基于GRPO训练的可学习控制模块来动态决定生成顺序。该方法在GenEval和VLMEvalKit上分别取得4.08%和4.85%的相对提升，有效增强了细粒度空间关系捕捉与多模态推理能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08056v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 719, \"height\": 723, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08056v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1480, \"height\": 584, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08056v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1484, \"height\": 392, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08056v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1653, \"height\": 485, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08056v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1643, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08056v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1646, \"height\": 268, \"label\": \"Table\"}]"
motivation: 现有扩散语言模型在结构化任务中受益于自适应生成顺序，但文本到图像生成与多模态理解中logits不足以确定最优顺序，需设计可学习机制。
method: 引入可学习控制模块，通过组相对策略优化（GRPO）训练，以动态决定多模态掩码扩散模型的生成顺序。
result: "在GenEval上提升4.08%，在VLMEvalKit上提升4.85%，显著改善图像空间关系与多模态理解。"
conclusion: 学习生成顺序控制块能有效提升多模态扩散模型的对齐与推理性能，验证了顺序优化在非结构化任务中的有效性。
---

## 摘要
扩散语言模型（DLMs）近期在自然语言生成任务中取得了显著进展。最近的研究表明，自适应令牌生成顺序可以显著提高数学推理和代码合成应用的性能。在这项工作中，我们研究了文本到图像合成和多模态理解的生成顺序优化。我们首先确认，与语言生成中的结构化问题（如数独谜题）不同，在文本到图像生成和多模态理解中，仅凭模型logits不足以确定最优生成序列。为了解决这一挑战，我们引入了一个通过组相对策略优化（GRPO）训练的可学习控制模块，以确定生成顺序。我们的结果表明，学习这个控制模块显著改善了DLMs中的文本到图像对齐和多模态理解。特别是，它增强了模型捕捉生成图像中细粒度空间关系的能力，同时强化了多模态推理和理解任务的性能。我们在GenEval（一个面向对象的文本到图像对齐基准测试）上评估了我们的框架，实现了4.08%的相对改进。此外，在VLMEvalKit上的实验证实了多模态理解有4.85%的相对改进，突显了我们方法的广泛有效性。

## Abstract
Diffusion Language Models (DLMs) have recently achieved substantial progress in natural language generation tasks. Recent research demonstrates that adaptive token generation ordering can significantly improve performance in mathematical reasoning and code synthesis applications. In this work, we investigate the optimization of generation order for both text-to-image synthesis and multimodal understanding. We first establish that, unlike structured problems in language generation such as Sudoku puzzles, model logits alone are insufficient for determining optimal generation sequences in text-to-image generation and multimodal understanding. To address this challenge, we introduce a learnable control module trained via Group Relative Policy Optimization (GRPO) to determine the generation order. Our results demonstrate that learning this control block substantially improves both text-to-image alignment and multimodal understanding in DLMs. In particular, it enhances the model's ability to capture fine-grained spatial relationships in generated images while also strengthening performance on multimodal reasoning and comprehension tasks. We evaluate our framework on GenEval, an object-focused benchmark for text-to-image alignment, where it achieves 4.08% relative improvements. In addition, experiments on VLMEvalKit confirm 4.85% relative improvements in multimodal understanding, highlighting the broad effectiveness of our approach.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：多模态掩码扩散模型（MDM）的生成顺序（即每一步解掩码的令牌位置）对文本到图像生成和多模态理解任务是否重要？若重要，如何确定最优顺序？
- **背景**：已有的扩散语言模型在结构化任务（如数独求解）中，基于模型logits的自适应顺序策略（如Top-K、Top-K margin）能显著提升性能。然而，这些置信度启发式方法在多模态场景下无效，因为视觉令牌的空间依赖性无法仅由logits捕捉。
- **研究动机**：现有工作局限于语言生成，本文首次系统性地探索多模态设置下的生成顺序优化，并提出一种可学习的控制机制。

## 2. 论文提出的方法论

### 核心思想
- 引入一个可学习的“控制块”（Control Block），输出每个位置的重要性分数，并据此动态决定每一步的解掩码顺序。
- 该控制块通过强化学习（GRPO）直接针对任务奖励进行优化，从而超越简单的logits启发式方法。

### 关键技术细节
- **控制块结构**：使用“解掩码策略模块”（Unmask Policy Module, UPM），输入当前隐状态、时间步t和掩码指示，为每个掩码位置输出排名分数 \( h_{\theta,t}^i \)。
- **顺序采样**：根据Plackett-Luce模型，从排名分数中无放回地采样一个大小为K的解掩码集合 \( U_t \)，分数高的位置优先被选择。
- **令牌预测**：主扩散模型对 \( U_t \) 中的位置预测具体令牌值。
- **完整步骤策略**：\( \pi_{\theta,t}(x_t | x_{t-1}) = \pi^{\text{unmask}}_{\theta,t}(U_t | x_{t-1}) \cdot \pi^{\text{token}}_{\theta,t}(x_t | x_{t-1}, U_t) \)。

### GRPO优化
- 对每个多模态输入c，采样G条完整轨迹（从全掩码到最终样本）。
- 计算每条轨迹的奖励（如PickScore或任务特定规则奖励），并利用组内均值和标准差标准化得到优势值 \( A_g \)。
- 优化目标为标准GRPO损失（含重要性采样比和KL正则项），但**累积所有去噪步的梯度**（而非仅单步），使控制块能从完整轨迹中学习。
- 训练算法如Algorithm 1所示：循环采样轨迹→计算奖励→计算优势→计算每步损失并累积梯度更新θ。

## 3. 实验设计

### 数据集/场景
- **文本到图像生成**：使用**GenEval**基准，包含6个子任务（Single Object, Two Objects, Counting, Colors, Position, Color Attr.）。训练提示由官方脚本生成，并进行了去重以避免数据泄漏。
- **多模态理解**：使用**VLMEvalKit**框架，在6个数据集上评估：GQA（视觉问答）、MMMU（多学科推理）、MMB（视觉理解）、SEED（细粒度对齐）、MathVista_MINI（数学推理）、COCO_VAL（图文理解）。训练数据来自VLAA-thinker数据集的数字和多选题子集。

### 对照方法
- 基线：MMaDA-COT（默认随机顺序）。
- 对比策略：Top-K（按最高概率排序）、Top-K Margin（按前两大概率差排序）。
- **本文方法**：MMaDA-Ours，使用两种奖励设置：PickScore（泛化）和GenEval规则奖励（任务内）。

### 评估指标
- GenEval：各子任务准确率及整体平均。
- 多模态理解：各数据集上的标准指标（如准确率、得分）。

## 4. 资源与算力

- **文本到图像生成**：24块NVIDIA A100 GPU（80GB？原文未明确但附录A写A100），训练5000步，每块GPU batch size=1，梯度累积4步。采样配置：每提示G=4个完成，重复R=6轮（共24样本/提示），去噪步数16，序列长度1024 tokens，bfloat16精度。
- **多模态理解**：8块NVIDIA A100 GPU，训练5000步，同样batch size=1，累积4步。采样：G=4，R=3（共12样本/提示），去噪步数64，序列长度256 tokens，float16精度。
- **优化器**：AdamW，lr=5e-6，β1=0.9，β2=0.999，无权重衰减。裁剪系数0.2，KL惩罚系数β=0。

## 5. 实验数量与充分性

- **主要实验数量**：
  - GenEval基准：表1报告了4种方法（Top-K、Top-K Margin、Ours(PickScore)、Ours(GenEval)）在6个子任务上的结果。
  - 多模态理解：表2报告了3种方法（基线、Top-K Margin、Ours）在6个数据集上的结果。
  - 消融/分析：图2-3展示了轨迹可视化；图4展示了定性对比。
- **充分性评价**：
  - **积极方面**：覆盖了生成与理解两大模态；对比了多种基线；使用了广泛认可的基准；提供了轨迹和定性分析佐证。
  - **不足之处**：
    - 缺乏多个随机种子的实验或置信区间报告。
    - 未与其他强化学习方法（如PPO直接优化控制块）对比。
    - 消融实验较少：没有单独验证UPM模块、Plackett-Luce采样、累积梯度等设计的必要性。
    - 奖赏函数设计简单（规则+PickScore），未探索更复杂的奖励塑造。
- **公平性**：训练提示集通过官方脚本生成并去重测试集，避免泄露；使用相同基础模型MMaDA-8B进行对比。

## 6. 论文的主要结论与发现

- **现有置信度策略在多模态中失败**：Top-K、Top-K margin在文本到图像和多模态理解上未带来提升，甚至有时下降（如表2中GQA比基线差）。
- **可学习控制块有效**：使用GRPO训练的控制块显著优于随机顺序和logits启发式方法。
- **定量提升**：
  - GenEval总体得分从0.49（Top-K）提升至0.51（Ours GenEval），相对提升4.08%；其中Two Objects和Position任务提升最明显（+0.08、+0.07）。
  - 多模态理解：在MathVista_MINI和COCO_VAL上分别提升1.3和0.7，四个数据集上优于基线。
- **可视化证据**：控制块使生成轨迹中间结果更合理，多模态理解中模型能产生更长的推理链而非单词答案。
- **结论**：学习生成顺序能增强细粒度空间关系捕捉和多模态推理能力，验证了顺序优化在非结构化任务中的有效性。

## 7. 优点

- **问题新颖**：首次将生成顺序优化从语言生成扩展到多模态生成与理解，并指出了现有方法的不足。
- **方法创新**：提出通过GRPO训练一个独立的控制块来学习顺序策略，而不是依赖启发式函数；利用完整轨迹梯度更新提升了训练信号。
- **实验覆盖广泛**：同时评估了图像生成（GenEval）和视觉问答/推理（VLMEvalKit），验证方法的通用性。
- **定性分析充实**：通过轨迹可视化和样本展示，直观说明控制块如何改善中间和最终输出。
- **开源友好**：基于MMaDA开源模型，实验设置清晰可复现。

## 8. 不足与局限

- **依赖特定基础模型**：所有实验基于MMaDA-8B-MixCoT，结论可能不直接迁移到其他MDM架构（如D3PM、MDLM）。
- **计算成本高**：GRPO训练需要采样多条完整轨迹（G=4，R=3~6），硬件资源需求较大（24/8 A100）。
- **对比基线有限**：
  - 未对比随机顺序或固定顺序（如从左到右），未对比其他强化学习方法（如直接使用PPO的value function）。
  - 多模态理解中仅与Top-K Margin比较，缺少Top-K结果（表2中未列出Top-K）。
- **奖励设计简单**：GenEval任务使用规则奖励（计数、位置偏差等），PickScore作为通用奖励。缺乏对更精细奖励（如CLIP分数、人类偏好）的探索。
- **多模态理解提升幅度绝对值较小**：例如MMMU从0.27到0.31，MMB从0.24到0.24（无提升），COCO_VAL从27.6到28.3，虽然部分数据集提升明显，但整体还需进一步验证。
- **消融不充分**：未单独论证控制块是否优于简单线性层、Plackett-Luce采样相较于贪心选择的优势、累积梯度相比单步梯度的效果。
- **应用限制**：目前仅在英文数据集上验证，未涉及中文或多语言；GenEval和VLMEvalKit均为学术基准，实际场景适应性未知。

（完）

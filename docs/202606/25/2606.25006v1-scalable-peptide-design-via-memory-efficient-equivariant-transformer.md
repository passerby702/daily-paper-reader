---
title: Scalable Peptide Design via Memory-Efficient Equivariant Transformer
title_zh: 通过内存高效的等变Transformer实现可扩展的多肽设计
authors: "Rui Jiao, Xiangzhe Kong, Yinjun Jia, Yijia Zhang, Ziyi Yang, Yang Liu, Jianzhu Ma"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25006v1"
tags: ["query:diff-gen"]
score: 8.0
evidence: 使用等变变压器的靶标特异性肽生成
tldr: 肽设计需同时优化序列与结构，现有方法受限于几何骨干的内存瓶颈。本文提出MEET，一种内存高效的E(3)等变Transformer，通过全局坐标聚合初始化向量特征、增强注意力融入距离信息、稀疏键适配注入共价键，实现线性内存扩展。集成到VAE和潜在扩散模型后，生成质量在亲和力、物理有效性和多样性上均优于现有方法。实验表明MEET支持模型与数据扩展，为大规模原子级肽设计提供了可扩展方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25006v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1689, \"height\": 985, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25006v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1705, \"height\": 459, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25006v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1704, \"height\": 500, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25006v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 845, \"height\": 361, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25006v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1433, \"height\": 357, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25006v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1371, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25006v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1722, \"height\": 388, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25006v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1497, \"height\": 1400, \"label\": \"Table\"}]"
motivation: 现有肽设计方法的几何骨干内存开销大，难以扩展到全原子级别，影响生成质量和模型可扩展性。
method: 提出MEET，包含耦合标量与等变向量流，通过内存高效注意力、全局坐标聚合和稀疏键适配实现线性内存的E(3)等变建模。
result: 在AFDB数据集上，MEET在生成肽的结合亲和力、物理有效性和样本多样性上优于现有方法，并支持模型与数据的系统扩展。
conclusion: MEET作为内存高效的等变骨干，有效推动了全原子肽设计的可扩展性与生成质量提升。
---

## 摘要
针对特定靶标的多肽设计需要在全原子几何约束下进行序列和结构的协同设计。潜在生成框架通过将细粒度的原子结构压缩为块级潜在表示，并在紧凑的潜在空间中进行条件生成，为这一问题提供了有效途径。然而，此类系统的可扩展性在很大程度上取决于其编码、解码和去噪组件中使用的几何主干。我们提出了MEET（内存高效等变Transformer），一种用于可扩展原子级多肽建模的E(3)等变主干。MEET保持耦合的不变标量和等变向量特征流，同时围绕内存高效注意力重新设计几何计算。它通过全局坐标聚合初始化向量特征，通过增强的查询和键点积融入成对距离，并通过稀疏键适应注入共价键信息。将MEET集成到用于全原子多肽生成的VAE和潜在扩散管道中，可实现随原子数量线性增长的内存使用，并提升生成质量，优于现有多肽设计方法。在大规模AFDB衍生数据集上的实验进一步表明，该主干支持系统性的模型和数据扩展，从而带来更好的结合亲和力、物理有效性和样本多样性。

## Abstract
Target-specific peptide design requires sequence and structure co-design under full atom geometric constraints. Latent generative frameworks offer an effective route for this problem by compressing fine grained atomic structures into block level latent representations and performing conditional generation in a compact latent space. However, the scalability of such systems depends heavily on the geometric backbone used throughout their encoding, decoding, and denoising components. We introduce MEET (Memory Efficient Equivariant Transformer), an E(3) equivariant backbone for scalable atomistic peptide modeling. MEET maintains coupled invariant scalar and equivariant vector feature streams, while reformulating geometric computation around memory efficient attention. It initializes vector features through global coordinate aggregation, incorporates pairwise distances through augmented query and key dot products, and injects covalent bond information through sparse bond adaptation. Integrated into a VAE and latent diffusion pipeline for full atom peptide generation, \model{} achieves linear memory scaling with atom count and improves generation quality over existing peptide design methods. Experiments on large scale AFDB derived datasets further show that the proposed backbone supports systematic model and data scaling, leading to better binding affinity, physical validity, and sample diversity.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义

- **研究动机**：靶标特异性多肽设计需要同时优化肽的序列与三维结构，在全原子几何约束下协同设计。现有潜在生成框架（如VAE+潜在扩散模型）虽然有效，但其编码、解码、去噪组件中的几何主干（backbone）面临内存瓶颈。传统等变网络依赖密集距离矩阵或局部分子图，导致内存消耗随原子数二次增长，限制了模型规模与数据规模的可扩展性，进而影响生成质量和多样性。
- **整体含义**：本文旨在设计一种内存高效的E(3)等变Transformer主干（MEET），使其在全原子多肽生成任务中实现线性内存扩展，同时提升生成质量，支持模型与数据的系统性缩放，为大规模原子级药物设计提供可行架构。

#### 2. 论文提出的方法论

- **核心思想**：将几何计算重新表述为与内存高效注意力（如FlashAttention）兼容的形式，避免显式存储二次型张量。保持耦合的标量（不变）和向量（等变）特征流，通过全局坐标聚合初始化向量特征，通过增强的查询-键点积融入成对距离，并通过稀疏键适配器注入共价键信息。
- **关键技术细节**：
  - **距离感知注意力机制**：将距离惩罚项 `s_h² ||x_i - x_j||²` 通过构造 `ϕ_s(x_i)` 和 `ψ_s(x_j)` 嵌入到查询和键中（仅增加9个额外维度），无需单独存储N×N距离偏置张量。
  - **特征初始化**：标量特征通过原子属性嵌入得到；向量特征通过一轮全局距离感知多头注意力，将每个头注意力权重作用于中心化的坐标差 `(X_j - X_i)`，然后投影到模型维度。避免了局部图构造。
  - **等变自注意力**：同时处理标量和向量特征流。标量Q/K/U来自线性投影，向量Q/K/U来自无偏线性层并展平通道轴。将标量与展平向量拼接后，再嵌入距离编码得到Q/K，计算不变注意力权重。输出后拆分并投影，保持等变性。
  - **等变前馈层**：通过向量特征的通道范数（不变摘要）与标量拼接，使用SwiGLU门控混合标量与向量信息，向量更新采用逐元素门控。
  - **键适配器**：稀疏消息传递，对每条共价边（i,j），将标量特征与边属性拼接经MLP得到标量消息和门控系数，门控系数作用于源原子向量特征，实现等变消息更新。复杂度O(|E|) = O(N)。
  - **归一化**：标量用RMSNorm，向量用旋转不变版本的RMSNorm（按所有空间和通道轴计算RMS）。
- **整体流程集成**：将MEET作为VAE编码器、序列解码器、结构解码器和潜在扩散模型去噪器的统一主干。VAE将全原子结构压缩为块级潜在变量（不变特征+坐标），扩散模型在潜在空间进行条件生成，解码器恢复全原子结构。

#### 3. 实验设计

- **数据集**：
  - 从AFDB（AlphaFold蛋白质结构数据库）8.64百万个结构域出发，通过滑动窗口、结构质量过滤、界面筛选、序列重叠聚类，构建约100M候选片段、31M个聚类。最终随机采样构造了100K和1.2M两个训练集。
- **基准与对比方法**：
  - 在100K数据集上对比了PepGLAD、PepFlow、UniMoMo、DiffPepBuilder四种现有方法。
  - 评估指标：预测结合自由能（ΔG，越低越好）、PoseBuster物理有效性通过率（PB，越高越好）、形状互补性（Shape）、溶剂化归一化结合能（ΔG/ΔSASA）、序列多样性（Seq. Div.）。
- **对比设置**：
  - 自己方法有MEET-XS（extra small）和MEET-B（base）两个变体。
  - 在1.2M数据集上，固定VAE，按DiT风格缩放LDM去噪器（XS、S、B、L四种规模）。
- **内存效率对比**：与EPT（E(3)等变预训练Transformer）在不同长度合成肽链上比较峰值内存。

#### 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量和训练总时长**。仅提供了超参数（如训练步数100K-200K、学习率、batch size 1024等）。因此无法定量总结算力消耗，但可指出该信息缺失。

#### 5. 实验数量与充分性

- **实验组数**：
  - 内存效率实验：一个图（合成肽链长度扫描），对比EPT。
  - 100K数据集基准实验：对比4种基线+自己2种变体，报告5个指标。
  - 1.2M数据集缩放实验：4种LDM规模，比较训练曲线和生成质量。
- **充分性评价**：
  - **客观**：使用了多个公开基线，指标覆盖结合能、物理有效性、结构互补性、能量效率、多样性，较为全面。
  - **公平**：自己方法在同一框架下比较不同规模，并与现有最佳方法在同一数据集上比较。
  - 但缺乏明确的消融实验（例如去除键适配器、使用不同初始化方式等），对各个组件贡献的分析不够充分。
  - 多样性在100K数据集上低于PepGLAD和UniMoMo，作者通过1.2M数据实验说明这是数据规模问题而非架构缺陷，合理但未在100K上做更多消融。
  - 总体而言，实验设计较充分，但消融实验不足。

#### 6. 论文的主要结论与发现

- MEET实现了全原子内存线性缩放（验证峰值内存约O(N)），而EPT呈超线性。
- 在100K数据集上，MEET-XS和MEET-B在ΔG和PB上全面优于所有基线，MEET-B达到ΔG均值-27.40、PB 0.799，而最强基线UniMoMo仅为-21.80和0.561。
- 在1.2M数据集上，随着LDM去噪器规模增大（XS→L），ΔG和PB持续改善，且多样性恢复（MEET-L Seq. Div. 0.899 vs 100K MEET-B的0.715），表明内存高效设计支持模型与数据扩展，多样性下降源于数据不足而非架构。
- 结论：主干效率不仅仅是实现问题，更是实现全原子生成模型可扩展性的关键。

#### 7. 优点

- **架构创新**：将距离编码融入QK点积（FlashBias思想），避免二次记忆，并保持等变性；全局注意力初始化向量特征，避免局部图构造带来的内存开销；稀疏键适配器高效注入化学信息。
- **可扩展性**：支持线性内存，使得在更大模型和数据上训练成为可能，实验验证了系统缩放的有效性。
- **性能提升**：在结合亲和力和物理有效性上显著超越现有方法，且多样性在大数据下得以保持。
- **通用性**：框架可复用于编码、解码、去噪等不同角色，只需要调整层数等超参数。

#### 8. 不足与局限

- **消融实验缺失**：未单独验证各个模块（如距离感知注意力、全局初始化、键适配器）的贡献，以及它们对内存和性能的具体影响。
- **多样性问题解释**：虽然大模型加大数据恢复了多样性，但论文未深入分析为何在100K上多样性低（可能由于模型容量过大导致过拟合，但未验证）。
- **计算资源未报告**：未提供训练所需GPU型号、数量、时间，不利于可复现性和成本评估。
- **应用范围**：仅测试了多肽设计，未在更广泛的分子任务（如小分子对接、蛋白质构象预测）上验证MEET的通用性。
- **数据集来源**：训练数据来自AFDB预测结构，存在分布偏差，可能影响真实场景的泛化。
- **评估局限**：ΔG和PB均为计算预测指标，缺乏湿实验验证，实际结合活性可能不同。

（完）

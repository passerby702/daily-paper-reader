---
title: Towards Controllable Image Generation through Representation-Conditioned Diffusion Models
title_zh: 迈向基于表征条件扩散模型的可控图像生成
authors: "Nithesh Chandher Karthikeyan, Jonas Unger, Gabriel Eilertsen"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27343v1"
tags: ["query:diff-gen"]
score: 6.0
evidence: 以预训练自监督模型表示为条件的扩散模型
tldr: 扩散模型虽能生成高质量图像，但其条件控制多依赖文本或语义图，需要大量标注数据。本文初步探索用自监督预训练模型的表示作为扩散模型的条件，该自条件机制既提升了无条件图像生成的质量，又提供了可操控的表示空间。通过识别表示空间中的变化方向，发现其具有平滑与解纠缠等优良特性，为无需大量标注的可控生成开辟了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27343v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 769, \"height\": 525, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27343v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1579, \"height\": 403, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27343v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 838, \"height\": 517, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27343v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1489, \"height\": 490, \"label\": \"Figure\"}]"
motivation: 现有扩散模型的条件控制（如文本提示）依赖广泛标注的数据集，限制灵活性。
method: 采用预训练自监督模型提取的表示来条件扩散模型，并探索该表示空间的变化方向。
result: 自条件扩散模型提升了无条件生成质量，且表示空间表现出平滑和解纠缠性质，可实现方向性控制生成。
conclusion: 表示条件扩散模型展示出可控生成的潜力，未来有望减少对人工标注的依赖。
---

## 摘要
扩散模型已成为高质量图像生成和编辑的强大工具，但引导这些模型生成特定输出仍是一项挑战。传统方法依赖于条件机制，例如文本提示或语义图，这需要大量标注的数据集。在这项初步工作中，我们探索了基于预训练自监督模型表征的扩散模型。这种自我条件机制不仅提升了无条件图像生成的质量，还提供了一个可用于控制生成的表征空间。我们通过识别变化方向来探索这一条件空间，并展示了其在平滑性和解纠缠方面的良好特性。

## 速览
**TLDR**：扩散模型在高质量图像生成中表现突出，但现有控制方法依赖文本或语义图等条件，需大量标注数据。本文采用预训练自监督模型的表示作为条件，既提升了无条件生成的质量，又提供了可用于控制生成的表示空间。通过在该空间中识别变化方向，实现了生成控制，展现出平滑性和解耦性。该工作为无需昂贵标注的可控图像生成开辟了新途径。 \
**Motivation**：现有扩散模型的条件机制（如文本提示）依赖大量标注数据，限制了灵活控制生成的能力。 \
**Method**：使用预训练自监督模型的表示作为扩散模型的条件，并探索表示空间中的变化方向进行控制。 \
**Result**：表示条件机制提升了无条件图像生成质量，并发现了表示空间中具有平滑、解耦特性的可控方向。 \
**Conclusion**：本文初步验证了自监督表示条件在可控生成中的潜力，为摆脱标注依赖提供了可行思路。

---

## Abstract
Diffusion models have emerged as powerful tools for high-quality image generation and editing, but guiding these models to produce specific outputs remains a challenge. Conventional approaches rely on conditioning mechanisms, such as text prompts or semantic maps, which require extensively annotated datasets. In this preliminary work, we explore diffusion models conditioned on representations from a pre-trained self-supervised model. The self-conditioning mechanism not only improves the quality of unconditional image generation, but also provides a representation space that can be used to control the generation. We explore this conditioning space by identifying directions of variations, and demonstrate promising properties in terms of smoothness and disentanglement.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将以Markdown格式对该论文进行结构化、深入、客观的总结。

### 论文结构化总结：迈向基于表征条件扩散模型的可控图像生成

#### 1. 论文的核心问题与整体含义

*   **核心问题**：当前的扩散模型虽然在图像生成与编辑方面表现出色，但其控制生成过程的能力存在瓶颈。主流的条件控制方法（如文本提示、语义图）严重依赖大规模、高质量的人工标注数据集，这种做法成本高昂，且在无标注的非结构化数据上难以发挥作用，限制了模型控制的灵活性与通用性。
*   **整体含义与动机**：本研究旨在探索一种新的范式，即不依赖外部人工标注，而是利用图像自身通过自监督学习（SSL）提取的内在表征作为条件信号。其核心思想是“自我条件”，期望通过一个预训练的自监督模型来引导扩散模型的生成过程。这不仅能摆脱对昂贵标注的依赖，还能挖掘出这些表征空间本身在可控生成方面的潜力（如平滑性、解纠缠性），从而为无条件生成模型赋予控制能力。

#### 2. 论文提出的方法论

*   **核心思想**：表征条件扩散模型。该方法将生成过程分解为两步：首先利用一个强大的预训练自监督编码器将图像映射到一个紧凑的表征向量；然后，训练一个以该表征向量为条件的扩散模型来生成图像。
*   **关键技术细节与流程**：
    1.  **表征提取**：采用一个大规模预训练的、固定的自监督视觉模型作为编码器。具体而言，本文使用的是 **DINO** 模型，用以从输入图像中提取一个 **768 维**的表征向量 `C`。该向量浓缩了图像的语义与结构信息。
    2.  **条件扩散模型训练**：采用**潜在扩散模型 (LDM)** 架构。训练一个条件去噪U-Net，该网络在去噪过程中接收表征向量 `C` 作为条件信号。LDM 在预训练VAE的压缩隐空间中进行扩散与去噪，提升了效率。
    3.  **表征空间的控制方法**：
        *   **插值**：给定两个图像的表征`C1`和`C2`，通过线性插值 `Ĉ = αC1 + (1-α)C2` 得到新的表征，并以此生成过渡图像。
        *   **扰动**：对表征向量添加高斯噪声 `Ĉ = C + λε`，通过调整强度 `λ` 来控制生成内容的随机变化程度。
        *   **语义方向（有监督）**：在已标注数据集上，计算具有特定属性（如“金发”）的所有图像的**平均表征向量**，将其加到参考图像的表征上，以实现该属性的编辑。
        *   **语义方向（无监督）**：对整个数据集的表征矩阵应用**主成分分析 (PCA)**。通过沿特定主成分方向 `Vk` 移动 `Ĉ = C + αVk`（其中α是缩放因子），来发现和操纵与特定语义属性相关的无标注变化。

#### 3. 实验设计

*   **数据集**：
    *   **LSUN Churches**：用于场景类图像的实验，主要验证方法的生成质量和空间平滑性。
    *   **Celeb-A**：用于人脸属性编辑与控制实验，探究语义方向的发现。
*   **对比方法**：
    *   **Stable Diffusion (SD) [10]**：基于文本条件的代表性扩散模型，通过CLIP文本嵌入进行插值。
    *   **Diffusion Inversion [5]**：一种将图像反演到扩散模型隐空间的方法，通过对该隐空间进行插值和扰动实现控制。
*   **评估场景**：
    *   **插值平滑性**：通过线性混合两张图（或两个文本）的条件因子 `α`，观察生成的中间图像序列的自然过渡质量。
    *   **扰动鲁棒性**：向条件变量添加不同强度的噪声，观察生成图像的质量保持能力和内容变化的合理性。
    *   **语义控制能力**：分别在有标注（监督）和无标注（无监督）的情况下，探索能否找到并利用可解释的编辑方向。

#### 4. 资源与算力

*   论文中**并未明确提及**所使用的具体 GPU 型号、数量或训练时长等算力细节。但这作为一篇初步探索性的短文，这种省略是可以理解的。

#### 5. 实验数量与充分性

*   **实验数量**：论文属于初步探索工作，实验组数相对有限。核心包含三组定性分析实验：1）在LSUN Churches上的表征扰动分析；2）在LSUN Churches/STL-10上的插值对比；3）在Celeb-A上的监督与无监督语义方向编辑。
*   **充分性与客观性**：
    *   **定性为主**：实验均为可视化结果的定性展示，缺少定量评估指标（如FID、IS、LPIPS等）来衡量生成质量或操控精度。
    *   **对比公平性**：对比了不同范式的代表方法（SD, Diffusion Inversion），具有一定代表性。但RCDM是基于少量数据训练的特定领域模型，而Stable Diffusion是大规模预训练的通用模型，这种对比的基准并不完全对等。
    *   **结论覆盖面**：实验初步验证了表征空间在平滑性和部分语义方向上的潜力，但未能系统性地评估该方法的泛化能力、不同自监督模型的差异以及与其他先进可控生成方法的全面比较。总的来说，实验**不够充分**，但作为初步探索，其定性结论为未来研究指明了方向。

#### 6. 论文的主要结论与发现

1.  **自我条件提升质量**：使用自监督表征作为条件，能够有效提升扩散模型的无条件图像生成质量。
2.  **平滑的表示空间**：RCDM的表征空间支持平滑的语义插值，相比于Stable Diffusion的文本嵌入或Diffusion Inversion的隐空间，能够产生更连贯、自然的中间过渡图像。
3.  **鲁棒的随机控制**：通过对表征向量施加噪声扰动，可以在不破坏图像整体真实感的前提下，可控地改变生成内容，显示出该空间的鲁棒性。
4.  **语义方向的潜力**：无论是在有监督还是无监督（PCA）的设置下，都能在表征空间中找到对应于特定语义属性（如发型、背景颜色）的变化方向。尽管无监督方向的可解释性和解耦性尚不及GAN，但其初步结果显示了潜在的可能性。

#### 7. 优点

*   **摆脱标注依赖**：直接使用自监督表征作为条件，为在无标注数据上实现可控生成开辟了新路径，具有很高的应用价值。
*   **空间属性优良**：所发现的条件空间展现出优于部分经典方法的平滑性和干扰鲁棒性，这是实现精细控制的重要基础。
*   **方法简洁统一**：将生成质量控制与生成内容控制统一于同一个自监督表征框架内，思路清晰、架构简洁。
*   **兼容性潜力**：该方法基于主流的LDM架构，易于与现有的扩散模型技术进步相结合。

#### 8. 不足与局限

*   **缺乏定量评估**：所有实验均为定性结果展示，说服力有限。需要引入定量指标来客观衡量生成质量（FID）、控制能力（操控精准度）、解耦程度（属性间独立性）等。
*   **解耦能力不足**：与StyleGAN等GAN模型的隐空间相比，当前发现的语义方向解耦性较差，编辑一个属性时容易连带改变其他属性（如“秃头”属性伴随性别变化），这受限于数据集本身的分布偏差。
*   **方向发现的局限性**：无监督的PCA方法找到的可解释方向有限且随机，缺乏系统性的方法去发现和验证所有有意义的语义方向。
*   **实验规模与泛化性**：仅在两个中小规模数据集上进行了初步实验，其方法在更复杂、更多样的数据集（如ImageNet）或更高分辨率下的表现尚不清楚。
*   **依赖编码器性能**：该方法的性能上限高度依赖于所用的自监督编码器的表征能力。未探讨不同自监督模型（如MAE、SimCLR等）对结果的影响。

（完）

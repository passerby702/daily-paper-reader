---
title: Pose-Aware Diffusion for 3D Generation
title_zh: 姿态感知扩散用于三维生成
authors: "Zihan Zhou, Luxi Chen, Jingzhi Zhou, Yuhao Wan, Min Zhao, Baoyu Fan, Chongxuan Li"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00345v1"
tags: ["query:diff-gen"]
score: 9.0
evidence: 提出姿态感知扩散（PAD），端到端的姿态对齐3D生成扩散框架
tldr: 传统分步生成姿态对齐三维对象面临空间错位与姿态歧义。Pose-Aware Diffusion (PAD)直接在观测空间内，以单目深度投影的点云为锚点端到端合成几何，消除歧义，生成高保真资产。PAD在几何对齐和图像-三维一致性上达到最优，还可独立生成对象后简单联合重建组合场景，保持精确空间布局。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00345v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1264, \"height\": 1487, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.00345v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1261, \"height\": 612, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.00345v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1252, \"height\": 634, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.00345v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1275, \"height\": 1046, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.00345v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1263, \"height\": 725, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.00345v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1253, \"height\": 631, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.00345v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1262, \"height\": 1828, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.00345v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1258, \"height\": 1507, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.00345v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1057, \"height\": 380, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.00345v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 721, \"height\": 263, \"label\": \"Table\"}]"
motivation: 现有“先生成规范姿态再旋转”的解耦范式带来空间错位与变换歧义，驱动设计原生对齐的端到端框架。
method: 将单目深度反投影为部分点云作为三维锚点，直接在观测空间以扩散模型端到端合成姿态对齐几何。
result: PAD实现了领先的几何对齐与图像-三维对应，并能通过独立对象合并重建组合场景，维持精确空间布局。
conclusion: PAD为姿态对齐三维生成提供端到端原生方案，消除歧义，输出高保真资产，可扩展至组合场景重建。
---

## 摘要
生成姿态对齐的三维物体具有挑战性，原因在于解耦的“先规范后旋转”范式所固有的空间错位与变换模糊性。为此，我们提出姿态感知扩散（Pose-Aware Diffusion，PAD），一种新颖的端到端扩散框架，直接在观测空间内合成三维几何。通过将单目深度反投影为部分点云并将其显式注入为三维几何锚点，PAD摒弃了规范假设，以施加严格的空间监督。这种原生生成从本质上解决了姿态模糊，生成了高保真度的姿态对齐资产。大量实验表明，相较于现有最优方法，PAD在几何对齐度与图像到三维的对应性上取得了更优表现。此外，PAD可通过简单合并独立生成的物体，自然地扩展到组合式三维场景重建，彰显了其保持精确空间布局的鲁棒能力。

## 速览
**TLDR**：生成姿态对齐的3D对象时，传统先规范后旋转范式常导致空间错位与变换模糊。Pose-Aware Diffusion (PAD) 直接在观测空间进行扩散生成，利用单目深度反投影点云作为3D几何锚点，消除规范假设并施加严格空间监督。实验表明，PAD在几何对齐精度和图像到3D一致性上均超越现有技术，并可轻松扩展至组合式3D场景重建，为姿态感知3D生成提供了新范式。 \
**Motivation**：现有方法采用规范空间生成再旋转，导致空间不匹配与姿态模糊，难以保证高保真姿态对齐。 \
**Method**：PAD是端到端扩散框架，直接在观测空间生成，以单目深度反投影点云作为几何锚点，实现严格空间监督。 \
**Result**：PAD在几何对齐与图像-3D一致性上全面超越现有最优方法，并可组合重建复杂3D场景。 \
**Conclusion**：PAD突破了传统范式，以原生观测空间生成解决姿态模糊，展示了在姿态感知3D生成与场景合成中的潜力。

---

## Abstract
Generating pose-aligned 3D objects is challenging due to the spatial mismatches and transformation ambiguities inherent in decoupled canonical-then-rotate paradigms. To this end, we introduce Pose-Aware Diffusion (PAD), a novel end-to-end diffusion framework that synthesizes 3D geometry directly within the observation space. By unprojecting monocular depth into a partial point cloud and explicitly injecting it as a 3D geometric anchor, PAD abandons canonical assumptions to enforce rigorous spatial supervision. This native generation intrinsically resolves pose ambiguity, producing high-fidelity pose-aligned assets. Extensive experiments demonstrate that PAD achieves superior geometric alignment and image-to-3D correspondence compared to state-of-the-art methods. Additionally, PAD naturally extends to compositional 3D scene reconstruction via a simple union of independently generated objects, highlighting its robust ability to preserve precise spatial layouts.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：从单张图像生成姿态准确的 3D 物体极具挑战。现有方法多采用“规范空间生成 + 事后位姿对齐”的解耦范式（canonical‑then‑rotate），这容易导致：
  - 空间错位（生成物体与输入图像像素级不对齐）；
  - 姿态模糊（对称物体或位姿预测错误导致方向颠倒）。
- **整体含义**：提出 Pose‑Aware Diffusion (PAD)，一种端到端的扩散框架，直接在观测空间（相机坐标系）合成 3D 几何，从而根本性地消除姿态估计误差和空间不匹配问题。
- **应用场景**：姿态对齐的物体生成、组合式 3D 场景重建（无需额外位姿优化即可将多个独立生成的物体拼成完整场景）。

### 2. 论文提出的方法论
- **核心思想**：摒弃规范空间假设，将单目深度反投影得到的部分点云作为 3D 几何锚点，显式注入扩散模型的潜在空间，以施加严格的空间约束，实现端到端的观测空间生成。
- **关键技术细节**：
  - **预训练基础**：基于 Hunyuan3D‑2.1 的 Vecset VAE 与 DiT 流匹配扩散模型。VAE 将 3D 点云压缩为无序潜在标记 `z`，解码器预测有符号距离场 (SDF)，经 Marching Cubes 提取网格。
  - **几何条件注入**：
    - 使用预训练深度估计器（MoGe）从输入图像获取深度图，反投影产生部分点云 `P_partial`。
    - 用冻结的 VAE 编码器将 `P_partial` 映射到几何潜在 `z_geo`。
    - 在扩散去噪过程中，将 `z_geo` 与当前时刻的噪声潜在 `z_t` 沿序列维度拼接，再结合图像语义条件 `c_img` 送入速度场估计器：`v_θ(Concat(z_t, z_geo), t, c_img)`。
  - **训练稳定化策略**：
    - 从 Objaverse 和 3D‑Front 筛选高质量水密网格，渲染 24 个视角，并将真实网格旋转到与观测视角一致，以提供精确的 GT 部分点云。
    - **深度噪声增强**：随机将 GT 深度替换为 MoGe 估计深度，并对点云注入噪声、缩放视场角、随机平移相机目标等，防止模型过度依赖完美点云，增强推理鲁棒性。
    - **两阶段课程训练**：先纯单物体训练，稳定后引入合成遮挡数据，提升对复杂场景的泛化能力。
  - **组合场景生成流程**：
    1. 对场景图像估计全局深度图并反投影得到全局部分点云 `P_scene`。
    2. 分割各物体，提取其局部部分点云 `P_partial^(i)` 和裁剪图像块 `I^(i)`。
    3. 对每个物体独立用 PAD 生成完整的水密网格 `M^(i)`（因点云坐标固定在全局相机框架下，生成姿态天然正确）。
    4. 简单合并所有网格即得完整场景 `M_scene`。

### 3. 实验设计
- **数据集与场景**：
  - **姿态物体生成**：使用 Google Scanned Objects (GSO) 测试集（约 1000 个未见过物体），每个物体渲染一张随机视角的 RGBA 图像，真实网格已变换到该视角的相机坐标系。用 MoGe 估计深度模拟推理条件。
  - **组合场景生成**：使用 3D‑FUTURE 测试集前 200 个场景，提供场景图像及物体掩码。
- **评估指标**：
  - Chamfer distance (CD，↓)、F‑Score (阈值 0.005，↑)、体积交并比 (IoU‑B，↑)。
  - 语义对齐：Uni3D 和 ULIP 预训练模型计算图像‑3D 匹配分数。
- **对比方法**：DreamGaussian (SDS 类)、InstantMesh (大型前馈重建)、MIDI、SceneGen、SAM‑3D、ShapeR（布局条件生成类）。对于 MIDI 和 SceneGen，因它们生成规范空间物体，实验时用 FoundationPose 结合 GT 深度进行后校正旋转（用 ⋆ 标记）。

### 4. 资源与算力
- **计算硬件**：8 块 NVIDIA A100 GPU。
- **训练配置**：批大小 16，优化器 AdamW，峰值学习率 1e‑5，cosine 调度，梯度裁剪阈值 1.0。
- **训练时长**：主阶段 5 万次迭代（约 2 天）；第二阶段（注入遮挡数据）再训练 2 万次迭代。
- **推理**：50 步扩散采样，引导强度 3.0。

### 5. 实验数量与充分性
- **定量比较**：
  - 姿态物体生成：对比 6 种基线方法，PAD 在所有指标上均最优（CD 最低 48.76×10⁻³，F‑Score 0.204，IoU‑B 0.863，Uni3D 0.275，ULIP 0.213）。
  - 组合场景生成：对比 MIDI、SceneGen、SAM‑3D，PAD 在 CD、F‑Score、IoU‑B 上均取得最佳。
- **消融实验**（关键组件验证）：
  - 去除 3D 条件（仅用图像）：产生严重结构畸变和空间错位。
  - 用 2D 深度图代替点云条件：丢失精细几何细节，对齐程度下降。
  - 去除深度噪声增强：模型过拟合于完美点云，对真实噪声点云产生崩塌表面。
  - 全模型（Ours Full）：生成高保真、完美对齐的几何。
- **定性结果**：多组渲染对比图显示了 PAD 在细节保留、姿态准确性和场景布局上的优势。
- **充分性与公平性**：覆盖单物体和场景级两个任务，对比多种主流范式，进行结构化消融，评估多维度的几何与语义指标。基线方法的姿态校正均使用下游算法并注明，比较较为客观。

### 6. 论文的主要结论与发现
- PAD 通过将部分点云直接注入扩散潜在空间，实现了端到端的观测空间 3D 生成，从根源上消除了姿态模糊与空间错位。
- 该方法在姿态对齐的单物体生成及组合场景重建任务上均达到最优定量性能，且定性结果展示出更高的几何保真度和细节一致性。
- 深度噪声增强和两阶段训练有效提升了模型对不完美深度输入的鲁棒性。
- 由于生成即姿态对齐，PAD 可自然地通过物体级生成后简单合并的方式完成场景重建，无需任何后处理位姿优化。

### 7. 优点
- **范式创新**：首次在观测空间实现端到端的姿态对齐生成，完全规避了规范空间与位姿预估的级联误差。
- **强几何约束**：将 3D 点云直接编码并与扩散潜在拼接，提供像素级几何监督，大幅提升对齐精度。
- **训练策略有效**：深度噪声增强和课程训练显著缓解了训练‑推理不一致和捷径学习问题。
- **自然支持组合场景**：无需联合优化或多视角约束，直接通过物体独立生成并联合得到完整场景，保持全局空间一致性。
- **开源与可复现**：提供了代码仓库链接，方法基于公开的预训练模型和数据，易于复现。

### 8. 不足与局限
- **依赖上游模块质量**：深度估计器（MoGe）或语义分割的误差会直接传播到生成结果，在深度质量差或物体边界模糊时可能产生畸变。
- **遮挡区域处理有限**：对于严重遮挡的物体部分，部分点云提供的几何信息不足，可能导致生成的结构存在歧义或不完整。
- **训练数据规模**：训练集仅约 2 万个高质量物体，可能限制极端形状或罕见类别的泛化能力。
- **纹理依赖外部模型**：几何生成后需借助 Hunyuan3D‑Paint 独立进行纹理合成，并非完整的端到端纹理‑几何联合生成。
- **伦理风险**：作为生成模型，可能被滥用于制造虚假 3D 资产或欺骗性环境，需谨慎使用。

（完）

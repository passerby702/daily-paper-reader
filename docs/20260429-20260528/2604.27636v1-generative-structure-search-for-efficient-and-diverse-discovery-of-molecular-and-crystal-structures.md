---
title: Generative structure search for efficient and diverse discovery of molecular and crystal structures
title_zh: 生成式结构搜索：高效且多样化地发现分子与晶体结构
authors: "Yifang Qin, Yu Shi, Junfu Tan, Chang Liu, Ming Zhang, Ziheng Lu"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27636v1"
tags: ["query:diff-gen"]
score: 6.0
evidence: 使用扩散模型进行分子和晶体结构的生成式结构搜索
tldr: 该论文针对高维能量景观搜索成本高、现有深度生成模型易受训练数据影响而忽略物理相关稀有极小值的问题，提出生成式结构搜索框架GSS，将扩散模型与随机结构搜索统一为共同采样过程，通过结合学习到的打分场和物理力，实现分子和晶体结构的高效多样化发现。在分子和晶体结构预测实验中，GSS展示了加速采样和保留能量引导探索能力，为分子和材料发现提供了通用高效的结构搜索新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27636v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1177, \"height\": 862, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2604.27636v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1181, \"height\": 782, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2604.27636v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1322, \"height\": 948, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2604.27636v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1187, \"height\": 762, \"label\": \"Figure\"}]"
motivation: 使用扩散模型进行分子和晶体结构的生成式结构搜索。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
预测稳定和亚稳结构是分子和材料发现的核心，但仍受限于搜索高维能量表面的成本。深度生成模型提供了高效的结构采样，但其输出受训练数据影响，可能对稀有但物理相关的极小值探索不足。我们提出了生成式结构搜索（GSS），一个统一框架，将基于扩散的生成和随机结构搜索（RSS）表述为由学习的得分场和物理力驱动的共同采样过程的两种极限状态。通过耦合这些驱动力，GSS能够利用数据先验加速采样，同时保留能量引导的局部极小值探索。在分子和晶体系统中，GSS以比RSS低十倍的采样成本恢复了多样的亚稳结构，实现了广泛覆盖，并对训练分布外的组分依然有效。这些结果建立了一种基于物理的生成式搜索策略，用于发现仅靠数据驱动采样无法企及的结构。

## 速览
**TLDR**：预测稳定和亚稳结构是分子与材料发现的核心挑战，但高维势能面搜索成本高昂，深度生成模型虽能高效采样却受限于训练数据分布，难以覆盖稀有而物理相关的极小值区域。本文提出生成式结构搜索(GSS)，将扩散生成和随机搜索统一为共同的采样过程，耦合学习的评分场与物理力，利用数据先验加速探索同时保留能量引导。在多种分子和晶体系统中，GSS以比随机搜索低十倍以上的采样成本恢复了多样的亚稳结构，且对训练分布外的未知组分依然有效。该工作为发现数据驱动方法无法触及的结构建立了物理增强的生成式搜索范式。 \
**Motivation**：现有深度生成模型难以探索训练数据中稀有但物理相关的亚稳结构，随机搜索成本过高。 \
**Method**：提出GSS统一框架，耦合扩散生成学习的评分场与物理力，实现数据先验加速与能量引导探索。 \
**Result**：GSS以比随机搜索低十倍以上成本恢复多样亚稳结构，且对训练分布外组分有效。 \
**Conclusion**：GSS建立了物理驱动的生成式结构搜索策略，超越单纯数据驱动采样，拓展了结构发现能力。

---

## Abstract
Predicting stable and metastable structures is central to molecular and materials discovery, but remains limited by the cost of searching high-dimensional energy landscapes. Deep generative models offer efficient structure sampling, yet their outputs remain shaped by training data and can underexplore minima that are rare but physically relevant. We introduce generative structure search (GSS), a unified framework that formulates diffusion-based generation and random structure search (RSS) as limiting regimes of a common sampling process driven by learned score fields and physical forces. Coupling these drivers lets GSS use data priors to accelerate sampling while retaining energy-guided exploration of local minima. Across molecular and crystalline systems, GSS recovers diverse metastable structures with more than tenfold lower sampling cost than RSS for broad coverage and remains effective for compositions outside the training distribution. The results establish a physically grounded generative search strategy for discovering structures beyond the reach of data-driven sampling alone.

---

## 论文详细总结（自动生成）

好的，以下是根据论文内容生成的结构化详细总结。

---

### 1. 论文的核心问题与整体含义
- **研究动机**：预测分子和晶体材料的稳定与亚稳结构（即势能面上的局部极小值）是计算药物设计和材料发现的第一步。传统随机结构搜索（RSS）虽然物理上可靠，但需要大量昂贵的能量评估来充分探索构型空间，效率低下。
- **现有方法的局限**：基于扩散的深度生成模型虽能高效生成低能结构，但其输出强烈受限于训练数据的分布，往往只能捕获全局能量最低点附近的模式，忽略几何上不同但能量具有竞争力的亚稳结构（例如，对碳元素只生成石墨，漏掉金刚石）。当面对训练集之外的新系统时，统计泛化可能产生物理上不合理的结构。
- **整体含义**：本文旨在结合生成模型的先验效率与物理势能面的探索可靠性，提出一个既能加速搜索又能保证物理多样性的统一结构搜索框架。

### 2. 论文提出的方法论
- **核心思想**：将扩散生成和随机结构搜索（RSS）视作同一迭代更新过程的两种特殊极限，进而将两者的“驱动力”耦合起来。
- **关键技术细节**：
    - **统一形式**：结构更新满足 `M_{i+1} = M_i + f_i(M_i)`，扩散模型的驱动力来自学习到的得分（score）场 `sθ`, RSS的驱动力来自势能面的负梯度 `-∇E(M)`。
    - **GSS 混合更新公式**（公式2）：`M_{i+1} = M_i + α_i f_diff,i + β_i f_RSS,i`。其中 `α_i` 和 `β_i` 是时间步相关的系数，且 `β_i = 1 - α_i`。
    - **调度策略**：采用 S 型（sigmoid）调度，让 `α_i` 在采样初期接近 1（扩散主导，提供全局引导），后期衰减至 0；`β_i` 则相反，后期接近 1（能量项主导，将结构驱动至局部极小值，保证物理有效性）。
    - **力场结合**：物理驱动力由预训练的通用机器学习力场（MatterSim）提供。对于周期性晶体，巧妙地将力场输出的原子力和维里应力通过链式法则转换为对分数坐标和晶格向量的梯度，实现与扩散更新的无缝结合。
    - **无需训练**：该方法是一种训练无关的引导采样策略，直接使用预训练好的扩散模型和力场，无需任何额外训练。

### 3. 实验设计
- **数据集与场景**：
    - **代表性周期体系**：12个系统，涵盖从单质（如C, Si）到四元（如LaCuSO）的组分，特别纳入了训练数据中稀有的三元系统 AlPN 以测试分布外泛化能力。
    - **元素周期表扩展**：对 Z<89（排除气体）的元素单质晶体进行系统搜索。
    - **分子体系**：以阿司匹林分子（C₉H₈O₄）为例，验证可控构象探索能力。
- **基准（Benchmark）**：每个系统的参考稳定结构集合由穷举的 RSS（使用 AIRSS 包）生成，经过结构去重和能量筛选（`E_hull < 1.0 eV`）得到。
- **对比方法**：
    - 标准扩散生成模型（无引导）。
    - 随机结构搜索（RSS）。
    - 提出的 GSS 方法。
- **评价指标**：
    - **覆盖率（Coverage）**：生成的结构能匹配到的参考集稳定结构的比例，衡量探索多样性（类似召回率）。
    - **效率（Efficiency）**：生成结构的平均势能以及落在`E_hull + 0.1 eV`能量阈值内的低能结构比例，衡量能量精度（类似精确率）。
    - **预算成本（Budget cost）**：发现全部参考稳定结构所需的采样试验次数，归一化至系统参考集大小。

### 4. 资源与算力
论文未明确提及执行采样或所依赖预模型训练所需的具体计算资源（如 GPU 型号/数量、训练/推理时长）。该方法涉及的计算主要是推理阶段的扩散采样步骤和力场评估，其成本取决于生成结构的数量和系统的原子数，但文中未给出与计算硬件相关的详细数据。

### 5. 实验数量与充分性
- **实验规模**：总计在 12 个代表性晶体系统、大量元素单质（跨周期表）以及 1 个分子系统上进行了对比实验，每个系统生成 1024 个结构进行评估。
- **实验充分性与公平性**：
    - 对比基线明确，涵盖了物理探索（RSS）和数据驱动生成（扩散）的代表方法，形成了对帕累托前沿的分析。
    - 评价指标从“判全”（覆盖率）和“判准”（能量效率）两个维度综合考量，并用成本曲线量化实际预算收益。
    - 特别设计了分布外（OOD）系统（AlPN）和分子构象受控探索实验，验证了方法的泛化性和灵活性，实验设计较为全面、客观。

### 6. 论文的主要结论与发现
- GSS 在所有测试的周期体系上均实现了比纯扩散模型更高的结构覆盖率和比 RSS 更高的能量效率，推进了由两种基线定义的“覆盖率-效率”帕累托前沿。
- 在寻找全部参考稳定结构方面，GSS 所需的采样预算成本比 RSS 低一个数量级以上（>10倍），而纯扩散模型则因覆盖率饱和而无法找出某些系统的所有结构。
- 该优势可扩展至整个周期表，在对碳、硅等具有复杂结构景观的体系上尤为显著。
- 对于训练时完全未见过的化学组分（如 AlPN），GSS 仍能有效运作，证明了物理引导可实现超越数据分布的泛化。
- 在分子体系（阿司匹林）中，GSS 成功恢复了四种稳定构象，而纯扩散模型只集中于训练模式，RSS 则由于缺乏拓扑引导而无法形成目标分子。

### 7. 优点
- **理论统一与优雅性**：将两种主流结构探索方法统一在同一个数学框架下，概念清晰。
- **训练无关的即插即用**：无需额外训练，直接耦合预训练扩散模型与力场，易于部署和迁移。
- **双重优势结合**：有效平衡了探索的广度（物理驱动偏离全局最小）与深度（先验引导向低能区），大幅降低计算成本。
- **强泛化能力**：对训练分布外系统和分子异构体仍能保持可控、高效的探索，展示了物理信息在生成过程中的关键作用。

### 8. 不足与局限
- **调度策略固定**：Sigmoid 调度是预设的，未根据实时诊断（如能量梯度大小）自适应调整，可能在部分系统上未达到最佳平衡。
- **力场依赖与近似**：性能依赖于底层力场的精度。能量评估施加在扩散中间噪声结构上，存在近似误差（文中提到可通过估计纯净结构缓解）。
- **复杂系统成本**：应用于蛋白质等超高维、崎岖的能量景观时，生成过程中的反复能量和力评估仍可能带来高昂的计算成本，需要粗粒化等策略配合。
- **对比方法有限**：主要与 RSS 和纯扩散比较，未与进化算法、盆地跳转等其他传统强基线或更复杂的数据驱动搜索方法进行横向比较。
- **分子体系验证单一**：仅在一个简单的分子（阿司匹林）上展示，对于更复杂的分子构象搜索或反应路径探索的有效性尚待验证。

（完）
